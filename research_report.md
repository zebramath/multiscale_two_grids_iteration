# 多尺度有限 Krylov 能量插值研究方案 v4.2

## 1. 研究定位

本项目研究高对比扩散问题中，全局能量插值的有限 PCG 路径与实际两网格收敛目标
之间的非单调错位。研究对象保持为二维规则网格、固定粗点、固定对称
Gauss--Seidel 和 Galerkin 粗校正；它不是通用工业 AMG，也不再包含递归多层结构。

中心问题有两个：

1. 为什么插值能量不断接近全局极小值时，两网格循环数可能先下降后上升；
2. 能否不读取尺寸、对比度或拓扑标签，以较少 setup 找到有限路径上的优势区间。

v4.2 把第二个问题收束为一个参数 `expected_rhs_count = R`。小 `R` 偏向低 setup，
大 `R` 偏向低循环数，避免多阶段确认、锚点保护和问题分类路由。

## 2. 数学对象

按细点和粗点分块，令

$$
A=\begin{bmatrix}B&C\\C^T&A_{CC}\end{bmatrix},\qquad
P(W)=\begin{bmatrix}W\\I\end{bmatrix}.
$$

全局能量目标满足

$$
W_*=-B^{-1}C,\qquad
J(W)-J(W_*)=\frac12\lVert W-W_*\rVert_{B,F}^2.
$$

因此有限 PCG 路径的能量趋向精确终点。但两网格传播算子

$$
E(P)=S^T\left[I-P(P^TAP)^{-1}P^TA\right]S
$$

取决于粗空间与磨光后误差方向的对齐，而不仅取决于总体能量距离。项目的学术重点
正是这一目标错位及其有限 Krylov 谱选择性，而不是增加更多工程基线或诊断入口。

## 3. v4.2 自适应 PCG

### 3.1 统一候选与预测

默认稀疏候选为 `m = 0,12,20,28,36,44,52,60`。每个候选用同一代表性右端项运行
短两网格试验；在试验后半段，对连续对数残差比取中位数和 MAD，得到保守尾部因子

$$
\widehat\rho=\exp\{\min(-10^{-10},\operatorname{median}(\ell)
+0.25\cdot1.4826\operatorname{MAD}(\ell))\},
$$

再外推到目标容差的循环数 $\widehat N_m$。算法直接使用两网格残差，而 PCG
预条件残量只作为“离能量终点多远”的理论对应指标。

### 3.2 一个参数调节策略倾向

令

$$
d(R)=\operatorname{clip}\!\left(\frac{\log_2R}{7},0,1\right).
$$

- pilot 长度为 $64+96d(R)$；
- 预测近优容忍度从 5% 连续收紧到 2%；
- 当 $d(R)\ge0.5$ 时，只在当前优胜者的 `m±2` 增加至多两个候选；
- 在近优候选中选择最小 `m`，优先抑制构造和插值密度。

`R=1` 是激进策略：64 次 pilot、8 个候选、无细化。`R≥128` 是细致策略：160 次
pilot、最多 10 个候选和局部 step--2 细化。中间 `R` 连续插值，不增加新的拓扑、
尺度或对比度分支。每个候选同时报告

$$
\widehat T_m(R)=T_{setup,m}/R+\widehat N_m t_{cycle,m},
$$

但当前不按短时 wall-clock 直接路由，以降低机器负载噪声对算法选择的影响。

### 3.3 通用性的含义

选择器只读取矩阵、几何初值、代表性右端项和 `R`。离线最优步数在验证集中覆盖
`m=0` 到 `m=60`，仍使用同一逻辑。这说明实现没有固定经验最优区间；它不意味着
已证明可直接推广到三维、非结构网格或任意 SPD 系统。

## 4. 代码精简

公开核心只保留三个数值模块：

| 模块 | 作用 |
|---|---|
| `energy_interpolation.hpp` | 几何、局部和全局能量插值 |
| `global_pcg.hpp` | 增量有限 PCG 路径与 v4.2 统一选择器 |
| `two_grid_solver.hpp` | Galerkin 粗算子、对称平滑和两网格循环 |

已删除未进入主线的 `support_expansion.hpp`、对应测试，以及旧版广筛/确认/锚点/中点
路由和无调用的通用研究辅助函数。三个实验入口分别对应主结果、机制证据和选择质量，
不再保留独立能量/复杂度/谱诊断、消融或压力测试入口。

## 5. 实验 1：11 个两网格主问题

单因素矩阵覆盖四个尺寸组合、三个对比度和六种通道拓扑，中心问题只计算一次。
全部方法使用相同矩阵、粗点、右端项、磨光器、相对容差 $10^{-6}$ 和 6000 次上限。

| 轴 | $1/h,1/H$ | 对比度 | 拓扑 | fast `m/cycles` | reuse `m/cycles` | exact cycles |
|---|---|---:|---|---:|---:|---:|
| size | 32,8 | $10^4$ | cross | 0 / 40 | 0 / 40 | 54 |
| size | 64,8 | $10^4$ | cross | 12 / 204 | 12 / 204 | 264 |
| size | 64,16 | $10^4$ | cross | 20 / 68 | 18 / 54 | 321 |
| center | 128,16 | $10^4$ | cross | 44 / 244 | 42 / 238 | 3227 |
| contrast | 128,16 | $10^2$ | cross | 20 / 228 | 20 / 228 | 756 |
| contrast | 128,16 | $10^6$ | cross | 60 / 279 | 52 / 298 | 3471 |
| topology | 128,16 | $10^4$ | meandering | 44 / 271 | 36 / 272 | 759 |
| topology | 128,16 | $10^4$ | diagonal | 44 / 351 | 36 / 297 | 418 |
| topology | 128,16 | $10^4$ | parallel | 44 / 273 | 42 / 251 | 421 |
| topology | 128,16 | $10^4$ | branching | 36 / 267 | 36 / 267 | 615 |
| topology | 128,16 | $10^4$ | winding-ring | 44 / 233 | 44 / 233 | 768 |

汇总循环数为：fast 2458、reuse 2382、global-exact 11074。细致策略相对快速策略
再减少 3.09%，但需要更长 pilot 和局部细化；global-exact 分别是二者的 4.51 倍和
4.65 倍。几何插值只在 11 例中的 3 例于 6000 次内收敛。

| 方法 | setup 总和 | 单 RHS solve 总和 | `R=1` 总成本 | `R=256` 摊销总成本 |
|---|---:|---:|---:|---:|
| adaptive-fast | 23.135 s | 2.631 s | 25.766 s | 696.731 s |
| adaptive-reuse | 25.581 s | 2.248 s | 27.829 s | 601.144 s |
| global-exact | 18.607 s | 29.580 s | 48.187 s | 7591.139 s |

本轮中 fast 的 setup 比 reuse 低 9.6%，单右端项总成本低 7.4%；到 256 个右端项时，
reuse 因单次求解总和较低，摊销成本反而低 13.7%。这只是同一台机器上的一次完整
运行，用来验证策略方向，不作为跨平台性能定律。

优势并非每例都是十倍。中心 cross 和 $10^6$ cross 上，exact/fast 分别为 13.23
和 12.44；小网格上只有 1.35 和 1.29。正确结论是有限插值在目标错位强的问题上
可产生数量级优势，并在完整矩阵上保持约 4.5 倍累计循环优势。

`code/results/experiment1_two_grid_comparison.txt` 还逐行展示密度、粗矩阵非零元、
setup、solve 和端到端时间。计时为本机单次 wall-clock，只作辅助；多右端项是否
划算应由实际工作负载上的摊销量判断，不能由循环数单独保证。

## 6. 实验 2：有限路径机制

| 问题 | 有限点 | 有限循环 | exact 循环 | 归一化能量超额 |
|---|---:|---:|---:|---:|
| 64/16, cross, $10^4$ | $m=20$ | 68 | 321 | $3.164\times10^{-4}$ |
| 128/16, cross, $10^4$ | $m=44$ | 244 | 3227 | $2.006\times10^{-4}$ |
| 128/16, cross, $10^6$ | $m=60$ | 279 | 3471 | $6.152\times10^{-5}$ |

完整路径显示能量超额持续下降，但循环数可在有限区间达到最小后回升。例如
128/16、$10^4$ 的循环数由 `m=44` 的 244 上升到 `m=60` 的 360，最终 exact 为
3227。这是理论主张的直接数值证据，不需要额外谱扫描来替代它。

## 7. 实验 3：step--2 oracle

离线 oracle 完整求解 `m=0` 和 `m=12,14,...,60`，只用于评价，不进入在线算法。

| 问题 | fast gap | reuse gap | oracle `m/cycles` |
|---|---:|---:|---:|
| 32/8, $10^4$, cross | 0.00% | 0.00% | 0 / 40 |
| 64/8, $10^4$, cross | 0.00% | 0.00% | 12 / 204 |
| 64/16, $10^4$, cross | 25.93% | 0.00% | 18 / 54 |
| 128/16, $10^4$, cross | 5.63% | 3.03% | 38 / 231 |
| 128/16, $10^2$, cross | 0.88% | 0.88% | 26 / 226 |
| 128/16, $10^6$, cross | 0.00% | 6.81% | 60 / 279 |
| 128/16, $10^4$, ring | 2.19% | 2.19% | 52 / 228 |

fast 平均 gap 为 4.95%，最大 25.93%；reuse 平均 1.84%，最大 6.81%。这符合两种
策略的定位：快速策略接受偶发较大选择误差以降低 setup，细致策略用更多观测换取
更稳定的近 oracle 循环数。细致策略也并非逐例支配，例如 $10^6$ 问题中短残差尾
仍会误判，文档不掩盖该边界。

## 8. 理论稿与结论边界

`theory.tex` 已从 1774 行压缩到约 300 行，保留：能量极小恒等式、PCG 收缩与
预条件残量指标、两网格算子的局部连续性、有限步优势的条件性判据、稳健尾部预测、
复用参数和选择误差界。删除了与当前实现无直接关系的大段支撑扩张、复杂谱分支和
重复结论。

当前边界如下：

- 结果只针对二维规则网格和受控两网格结构；
- 代表性右端项不自动代表任意多右端项分布；
- 单次 wall-clock 易受系统负载影响，稳定证据是收敛、循环数、选择步数和 oracle gap；
- 有限步优势是结构性和实验性结论，不是对所有 SPD 问题成立的普遍定理。

## 9. 复现

在 `code` 目录执行：

```bash
./scripts/run_validation.sh quick
./scripts/run_validation.sh full
```

脚本优先使用 CMake；若环境没有 CMake，会使用等价的 C++17 严格直接构建。三个
完整逐行结果位于 `code/results`。正式研究结果使用 `full`，`quick` 只用于开发检查。
