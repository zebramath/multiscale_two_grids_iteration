# 研究方案与阶段报告 v3.1

## 高对比扩散问题中面向实际循环数的分阶段自适应 PCG

**对应代码：** `two_grids_iteration_v3.1.0`  
**理论基础：** `理论v2.tex`  
**核心实验：** experiment7、experiment9、experiment10、experiment11  
**两网格循环：** 一次前向 Gauss--Seidel、Galerkin 粗校正、一次后向
Gauss--Seidel  
**求解停止标准：** 相对残差 $10^{-6}$

---

## 摘要

v3.0.0 已证明有限步 PCG 插值存在明显的有利停止窗口，并实现了结合谱探针和
代表性右端项尾窗的自适应选择器。但进一步检查发现两个不足。

1. v3.0 在原始 $128\times128$ 通道问题上选择 $m=36$，需要268次循环，仍未
   命中离线最优 $m=38$ 的231次；
2. 对每个检查点都运行较长探针并构造粗矩阵，原始问题的自适应 Setup 约为
   3863 ms，选择成本偏高。

v3.1.0 将算法重构为“粗筛—回溯加密—少量真实确认”。粗筛不再只用尾部
收缩率估计总循环数，而是同时使用探针结束时的相对残差，预测

$$
\widehat N_m=k+
\left\lceil
\frac{\log(\varepsilon/\|r_k\|_{\rm rel})}
{\log\widehat\rho_m}
\right\rceil.
$$

由于短试运行常把最优位置向较大的 $m$ 推迟，算法从粗筛锚点向较小步数回溯，
以步长2产生精细候选；最后只把两个具有互补性的候选继续迭代到真实容差，并直接
比较实际循环数。粗筛路径中的精细快照被缓存，避免第二次运行全局 PCG。

最终结果如下。

- 原始通道自动选择 $m=38$，循环数231，与步长2离线最优完全一致；
- 原始通道自适应 Setup 从 v3.0 的3863 ms 降到 v3.1 的3195 ms，下降约17.3%；
- 18问题矩阵中，v3.1 循环数总和从 v3.0 的2407降到1813，下降约24.7%；
- 18问题自适应 Setup 总和从4556 ms降到3877 ms，下降约14.9%；
- 18例中有12例循环数严格改善，另外6例持平，没有一例比 v3.0 更差；
- 6个代表问题与步长2 oracle 比较，3例精确命中，平均循环数差距约3.49%，
  最大差距11.11%。

同时，v3.0 新增的 `frontier-gain` 支撑扩展只获得了约1.6%--3.0%的相对密度
改善，循环数没有改善，构造时间多数情况下上升。该收益不足以支撑独立算法贡献，
因此已从 v3.1 代码、实验和研究主线中删除。旧的 `adaptive-budget-v2` 仍作为
已有基线保留，但本报告不再继续研究稀疏支撑扩展。

---

## 1. 研究目标调整

### 1.1 保留的核心问题

离散系统按 F/C 点分块为

$$
A=\begin{bmatrix}B&C\\C^T&A_{CC}\end{bmatrix},
\qquad
P(W)=\begin{bmatrix}W\\I\end{bmatrix}.
$$

全局能量极小列为

$$
W_\star=-B^{-1}C.
$$

从几何插值 $W_0$ 出发运行有限步对角预条件 CG，得到路径

$$
W_0,W_1,\ldots,W_m,\ldots,W_\star.
$$

PCG 保证列能量误差下降，但不保证固定光滑器下的两网格循环数下降。因此本阶段的
唯一核心问题是：

> 在不进行完整密集扫描的情况下，如何以较低选择成本找到实际循环数接近最优的
> 有限 PCG 检查点？

### 1.2 删除稀疏支撑新策略

v3.0 的 `frontier-gain` 在四个测试场上保持了旧策略循环数，并把密度降低约
0.40--0.76个百分点。但它没有降低循环数，且弯曲通道等问题的 Setup 明显增加。
这一结果说明其节点分配规则并非错误，却不足以构成有分量的独立创新。

v3.1 因此执行以下清理：

- 删除 `frontier_gain_support.hpp`；
- 删除 experiment8 及其结果文件；
- 删除相关回归测试和 README 接口；
- 保留 v2 的 `residual_budget_support.hpp` 和 experiment4，便于复现旧基线；
- 研究方案不再宣称新的稀疏支撑贡献。

这种处理避免用很小的密度差异稀释有限 PCG 非单调性这一更强的研究主线。

---

## 2. v3.0 自适应器的问题诊断

### 2.1 只用收缩率不能准确预测总循环数

v3.0 使用

$$
\widehat N_m\approx
\frac{\log\varepsilon}{\log\widehat\rho_m}.
$$

该式隐含所有候选从相同的单位残差开始。实际上，经过 $k$ 次两网格试运行后，
不同 $m$ 的残差水平 $\|r_k\|$ 差异很大。即使两个候选具有相近尾部收缩率，已经
获得更大瞬态下降的候选也可能更早达到容差。

v3.1 改用

$$
\widehat N_m=
\begin{cases}
k, & \|r_k\|_{\rm rel}\le\varepsilon,\\[2mm]
k+\left\lceil
\dfrac{\log(\varepsilon/\|r_k\|_{\rm rel})}
{\log\widehat\rho_m}
\right\rceil,
&0<\widehat\rho_m<1,\\[3mm]
N_{\max},&\text{其他情况}.
\end{cases}
$$

这仍是外推估计，不是严格上界，但目标和实际停止条件一致。

### 2.2 短试运行的最优位置存在系统性迟滞

experiment10 在原始通道上扫描 $m=28,30,\ldots,48$。例如：

| $m$ | 64循环相对残差 | 160循环相对残差 | 实际循环数 |
|---:|---:|---:|---:|
| 36 | $2.6490\times10^{-1}$ | $5.2371\times10^{-4}$ | 268 |
| 38 | $1.6143\times10^{-1}$ | $7.3027\times10^{-5}$ | **231** |
| 40 | $1.1278\times10^{-1}$ | $6.1518\times10^{-5}$ | 234 |
| 42 | $8.0618\times10^{-2}$ | $6.5868\times10^{-5}$ | 238 |
| 44 | $5.6653\times10^{-2}$ | $7.9342\times10^{-5}$ | 244 |
| 48 | $3.9168\times10^{-2}$ | $1.6543\times10^{-4}$ | 256 |

只看64循环残差会偏向 $m=44$ 或更大候选；延长到160循环后，转折才逐渐接近
$m=38$--40。这说明短试运行适合筛选区间，不适合单独决定最终步数。

### 2.3 所有候选使用相同长探针浪费时间

v3.0 对每个检查点都运行三组随机幂探针和代表性右端项探针。它提高了安全性，
但多数明显较差的候选不需要相同精度的评估。v3.1 改用分层预算：

- 大间隔候选只做短粗筛；
- 只有粗筛锚点附近的候选做较长试运行；
- 只有两个候选继续到真实容差；
- 已经确认非常容易的候选可以提前返回。

---

## 3. v3.1 分阶段自适应算法

### 3.1 阶段A：几何候选安全检查

首先对 $m=0$ 的几何插值做短试运行。如果预测循环数很小，则继续到真实容差确认；
只有实际循环数低于快速接受阈值才返回。

对于预测不算很小但仍可能有竞争力的几何候选，算法可把它作为安全候选确认并保存，
防止后续 PCG 候选反而更差。安全确认只在预测循环数低于有限阈值时启用，避免对
明显失败的几何候选运行数千次循环。

### 3.2 阶段B：稀疏粗筛

在默认原始通道配置中，粗筛检查

$$
m=16,32,48.
$$

鲁棒性矩阵从 $m=12$ 开始，使用相应大间隔。每个候选运行短代表性右端项试验，
计算残差水平、尾窗收缩率和预计总循环数。预计总循环数最小的候选称为粗筛锚点
$m_a$。

粗筛的目标不是直接选出最优 $m$，而是定位有利区间。

### 3.3 阶段C：向较小步数回溯并加密

由于短试运行的最优位置通常晚于真实最优，算法在

$$
[m_a-10,m_a-4]
$$

内按步长2生成候选。以原始通道 $m_a=48$ 为例，精细候选为

$$
38,40,42,44.
$$

该回溯区间是实验驱动的默认值，不是普适理论常数。代码将其全部暴露为可配置参数。

### 3.4 路径快照复用

最初实现为粗筛和精细搜索分别创建 `GlobalEnergyPcgPath`，虽然能选中 $m=38$，
Setup 却上升到约5755 ms。最终实现改变路径推进方式：

1. 在从一个粗筛点推进到下一个粗筛点时，顺便保存可能用于回溯的精细插值快照；
2. 新粗筛点成为最佳锚点时，用其快照替换旧锚点快照；
3. 精细阶段直接使用缓存矩阵，不重新组装 F 系统，也不重跑 PCG 路径。

该修改不改变候选内容，只删除重复构造。

### 3.5 阶段D：多样化真实确认

精细候选先运行中等长度试验。最终确认集合包含：

- 回溯区间最早的候选，用于抵消短试运行向大 $m$ 偏移；
- 预计总循环数最小的候选。

这两个候选从已有试运行状态继续迭代到相对残差 $10^{-6}$，随后直接比较实际循环数。
最终返回真实循环数较少者。因此，最终决策不再依赖外推误差。

### 3.6 低循环快速接受和单点前瞻

若某个候选经真实确认后不超过48次循环，算法立即接受。若循环数在49--64之间，
算法不启动完整精细搜索，只额外确认一个位于半个粗筛间隔之后的前瞻候选，返回两者
中较好者。

这一规则是循环质量和 Setup 的折中：直接接受64次会错过某些45--51次候选；对它们
启动完整精细搜索又会显著增加平均 Setup。单点前瞻在完整矩阵中取得了较好的折中。

### 3.7 算法目标的严格边界

最终确认使用传入的代表性右端项。因此 v3.1 优化的是

> 给定右端项、零初值、固定容差和固定两网格循环下的实际循环数。

它不等同于最坏情形谱半径最优。如果实际任务包含多个性质明显不同的右端项，应使用
多个代表向量进行确认，或重新加入谱安全检查。

---

## 4. 实验设计

### 4.1 原始通道基准

- $h=1/128$；
- $H=1/16$；
- $H/h=8$；
- 对比度 $10^4$；
- 随机种子1；
- 交叉通道；
- 最大40000次两网格循环。

固定候选包含 $m=16,24,32,36,38,40,48$，便于直接判断是否命中已知低循环窗口。

### 4.2 18问题鲁棒性矩阵

- 网格：$(1/h,1/H)=(32,8),(64,8),(64,16)$；
- $H/h=4$ 或8；
- 对比度：$10^2,10^4,10^6$；
- 种子：1和17；
- 拓扑：交叉、弯曲、对角和平行通道平衡轮换；
- 最大12000次循环。

每个问题比较几何插值、固定 $m=40$ 和自适应方法。

### 4.3 六问题离线 oracle

experiment11 从18问题中选取六个代表问题，对

$$
m=0,12,14,\ldots,48
$$

全部求解到真实容差。循环数最少者作为步长2 oracle。oracle 只用于评价选择质量，
其扫描成本不计入自适应 Setup，也不是实用竞争算法。

---

## 5. 结果

### 5.1 原始通道：命中离线最优并降低 Setup

| 方法 | 选择步数 | 密度 | 循环数 | Setup ms |
|---|---:|---:|---:|---:|
| v3.0 自适应 | 36 | 23.5999% | 268 | 3863.259 |
| v3.1 自适应 | **38** | 25.2786% | **231** | **3195.398** |
| 固定候选 | 38 | 25.2786% | 231 | 约656 |
| 精确能量终点 | 极限 | 97.8420% | 3227 | 约2443（v2.11） |

相对 v3.0：

- 循环数下降约13.8%；
- Setup 下降约17.3%；
- 从低循环窗口附近改进为精确命中步长2离线最优。

自适应 Setup 仍明显高于先验已知 $m=38$ 的固定构造。自适应解决的是“事先不知道
步数”的模型选择问题，不能和已知 oracle 的单次构造混为一谈。

### 5.2 18问题：相对 v3.0 的整体改进

| 汇总量 | v3.0 | v3.1 | 变化 |
|---|---:|---:|---:|
| 自适应循环数总和 | 2407 | **1813** | **-24.7%** |
| 自适应 Setup 总和 | 4556 ms | **3877 ms** | **-14.9%** |
| 平均 Setup | 253 ms | **215 ms** | **-14.9%** |

逐问题比较：

- 12/18 的循环数严格减少；
- 6/18 与 v3.0 持平；
- 0/18 变差；
- 11/18 的 Setup 低于 v3.0；
- 几何插值失败的4个问题中，自适应方法仍全部收敛。

最终循环数依次为

$$
38,46,86,45,40,54,
228,121,245,158,273,160,
54,48,51,51,60,55.
$$

相对于几何插值和固定 $m=40$ 两者中较好的一个，自适应在17/18问题上不劣；唯一
例外的差距也较小。这比 v3.0 更接近“以实际循环数为主”的设计目标。

### 5.3 六问题 oracle 差距

| 问题 | 自适应 $m$ | 自适应循环 | oracle $m$ | oracle循环 | 差距 |
|---|---:|---:|---:|---:|---:|
| $32/8$, cross, $10^2$ | 0 | 38 | 0 | 38 | 0% |
| $32/8$, parallel, $10^4$ | 20 | 45 | 20 | 45 | 0% |
| $64/8$, diagonal, $10^2$ | 12 | 228 | 12 | 228 | 0% |
| $64/8$, parallel, $10^6$ | 12 | 160 | 22 | 144 | 11.11% |
| $64/16$, diagonal, $10^4$ | 28 | 51 | 30 | 50 | 2.00% |
| $64/16$, meandering, $10^6$ | 36 | 55 | 40 | 51 | 7.84% |

六例中：

- 3例精确命中；
- 平均相对差距约3.49%；
- 中位数差距约1.00%；
- 最大差距11.11%。

该结果支持“循环数已经较接近有限候选 oracle”，但样本只有六个，不能据此声称
任意问题上都在11%以内。

### 5.4 Setup 的细分判断

v3.1 的 Setup 改进不是每个问题都成立。容易问题可通过确认后快速接受，把 v3.0
几十毫秒甚至上百毫秒的选择降到约1--20 ms；部分中等困难 $H/h=8$ 问题仍需完整
粗筛和精细确认，Setup 可能高于 v3.0。

因此准确结论是：

> v3.1 同时降低了原始核心基准和18问题总体 Setup，但尚未实现逐问题一致降本。

---

## 6. 算法是否成功

### 6.1 可以认为成功的方面

1. 原始核心问题从“接近低循环窗口”提高到精确命中离线最优；
2. 18问题循环数没有任何一例相对 v3.0 退化；
3. 总循环数和总 Setup 同时下降，而不是只优化其中一个；
4. 与代表性离线 oracle 的平均差距降到约3.5%；
5. 快照复用、分层预算和单点前瞻都有明确的失败版本或消融依据；
6. 最终选择使用真实容差循环数，不再完全依赖外推指标。

### 6.2 仍不能夸大的方面

1. 代表性右端项最优不等于谱半径最优；
2. 回溯区间10、停止前移4和阈值48/64仍是经验参数；
3. 自适应 Setup 仍远高于已知最优步数的固定构造；
4. 只有六个问题完成了步长2 oracle 对比；
5. 未加入成熟 AMG 软件基线；
6. 当前仍是两网格、二维结构网格和固定 GS 光滑器。

综合判断：v3.1 的自适应 PCG 已经从“有意义但代价偏高的研究原型”推进为
“循环质量较接近离线最优、总体选择成本也得到实质降低的较成功原型”。它仍不是
生产级自动调参器，但已经足以作为论文中的主要算法贡献继续发展。

---

## 7. 软件变更与验证

### 7.1 新增和保留

- `global_pcg_path.hpp`：增量 PCG 状态和检查点快照；
- `adaptive_global_pcg.hpp`：分阶段选择、快照复用、真实确认和前瞻规则；
- experiment7：原始通道主实验；
- experiment9：18问题鲁棒性矩阵；
- experiment10：试运行长度诊断；
- experiment11：离线 oracle 差距；
- `regression_v31`：增量路径和自适应接口回归。

### 7.2 删除

- `frontier_gain_support.hpp`；
- experiment8；
- frontier-gain 的结果和测试；
- README 中对应接口和结论。

### 7.3 复现命令

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=ON
cmake --build build --parallel 4
ctest --test-dir build --output-on-failure
```

```bash
./scripts/run_validation.sh quick
./scripts/run_validation.sh full
```

也可分别运行：

```bash
./build/experiment7 --fine=128 --coarse=16 --threads=4 \
  --contrast=1e4 --max-cycles=40000

./build/experiment9 --threads=4

./build/experiment11 --threads=4
```

---

## 8. 下一阶段建议

### 8.1 多右端项确认

把当前单个代表性右端项扩展为小型右端项集合，以最大循环数、平均循环数或分位数
作为最终目标，检查所选 $m$ 是否能跨工作负载保持稳定。

### 8.2 自适应确认预算

根据两个最终候选的预测差距决定是否需要第二次完整确认。若第一候选优势足够大，
可省略另一候选；若差距很小，再追加确认或第三候选。

### 8.3 更广 oracle 验证

把步长2 oracle 从六个问题扩展到完整18问题，并增加随机种子。报告平均差距、最大
差距和选择步数偏差的置信区间。

### 8.4 多层和外部基线

将选择规则推广到多层 V-cycle，并与 BoomerAMG、PETSc GAMG 或 MueLu 做同机
对比。只有完成这些验证，才能判断方法是否具有超出两网格研究模型的实际竞争力。

---

## 9. 主要结论

1. v3.0 的主要误差来自总循环数预测不完整和均匀探针预算；
2. 把试运行结束残差纳入预测后，选择目标与实际容差一致；
3. 短试运行最优位置存在向大 $m$ 偏移，向前期回溯是必要的；
4. 缓存粗筛路径中的精细快照可删除一次完整 PCG 路径重构；
5. 两个多样化候选的真实容差确认显著提高了最终步数精度；
6. 原始通道选择由 $m=36$ 改进到 oracle $m=38$，循环数由268降到231，同时
   Setup 下降约17.3%；
7. 18问题总循环数下降约24.7%，总 Setup 下降约14.9%；
8. 六个代表问题平均 oracle 差距约3.49%，最大11.11%；
9. 收益很小的 frontier-gain 支撑策略已删除，研究主线得到收敛；
10. v3.1 的自适应 PCG 可以认为比较成功且有明确研究意义，但其结论限于代表性
    右端项和当前两网格框架。
