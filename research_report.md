# v9.1 研究报告：有限能量极小化路径的两网格机制

## 1. 研究定位

本文不是新的通用 AMG 框架，也不再主张一个复杂的 adaptive stopping algorithm。研究对象是一条完全可控的路径：固定规则粗点和粗点单位注入，从几何插值出发，用 Jacobi--PCG 逐列逼近

\[
W^*=-A_{FF}^{-1}A_{FC}.
\]

在本文无额外稀疏约束的坐标下，$W^*$ 同时是插值能量唯一极小点和 classical ideal interpolation。核心问题是：为什么沿这条路径有限迭代可能比高精度 endpoint 产生更好的固定磨光子两网格方法？

已有工作覆盖 energy-minimization interpolation、Krylov/CG 构造、有限迭代控制 setup，以及直接优化两网格率的 optimal interpolation。本文的增量不是重新发明这些对象，而是把有限 PCG 路径、真实两网格谱目标、粗空间主角、局部奇异值方向与图传播尺度放进同一套精确分析，并用一一对应的数值对象诊断。

## 2. 理论链条

令 $A\succ0$ 按 F/C 分块，$P(W)=[W;I]$，$S=A_{CC}-A_{CF}A_{FF}^{-1}A_{FC}$，并令

\[
Z=A_{FF}^{1/2}(W-W^*)S^{-1/2}.
\]

则有三条基础恒等式：

1. $J(W)-J(W^*)=\tfrac12\|W-W^*\|_{A_{FF},F}^2$；PCG 非零步使该能量严格下降。
2. 当前粗空间相对 endpoint 粗空间的 $A$-principal angles 满足 $\tan\theta_k=\sigma_k(Z)$。
3. 对前向 GS 能量坐标算子 $T$，固定对称两网格谱半径精确为
   \[
   \rho_{TG}(Z)=\|(I+ZZ^T)^{-1/2}(T_F-ZT_*)\|_2^2.
   \]

第三式包含能量目标没有的有符号磨光子耦合项，因此主角整体缩小和能量下降都不足以推出 $\rho_{TG}$ 单调。

若 $T_F$ 的最大奇异值简单，gap 为 $\delta>0$，取相应 $u_F,v$ 和 $y=T_*v$，则 endpoint 附近

\[
\sqrt{\rho_{TG}(Z)}=\sigma_1(T_F)-u_F^TZy+R(Z),\qquad R(Z)=O(\|Z\|^2),
\]

正文给出显式有效域和余项常数。若 $y\ne0$，方向 $H=u_Fy^T$ 使充分小的正 $t$ 满足 $\rho_{TG}(tH)<\rho_{TG}(0)$，所以 energy/ideal endpoint 一般不是该固定全变量对称 GS 两网格方法的局部最优点。对相邻 PCG 点，$u_F^T(Z_m-Z_{m+1})y$ 加显式余项界给出变好或变坏的充分判据；它是机制诊断，不是在线停止器。

PCG 的 Krylov 表示还给出有限传播：第 $m$ 步校正支撑包含于初始残差在 $A_{FF}$ 图上的半径 $m-1$ 邻域。这使 $m=O(h^{-1})$ 在 fixed-$H$ 情形中具有“固定物理传播距离”的解释，但不自动证明最佳步数公式。

二维规则粗化的 Jacobi--PCG 条件数满足

\[
\kappa\lesssim \chi\Lambda_{h,H}^{-1},\qquad
\Lambda_{h,H}=h^2+\frac1{q^2\log(2q)}.
\]

常系数容量试验函数给出 $\kappa=\Theta(\Lambda^{-1})$；相邻高权 F--F 边与低权外连边构造给出 $\kappa\gtrsim\chi$。因此尺寸/粗化尺度与对比度幂次分别尖锐。为保持严格，本文没有把这两种不同构造拼成对所有几何均成立的统一乘积下界。

## 3. 数值方法

所有正式实验采用二维五点扩散、规则 C/F 点、单位注入、$A_c=P^TAP$、一次前向/后向 GS 和精确粗解。线性求解相对残量为 $10^{-6}$。有限路径点固定取 $m=\operatorname{round}(c/h)$，不使用在线 selector，也不报告 oracle gap。

真实 $\rho_{TG}$ 不由 RHS 残量近似：代码对 $A$-自伴正半定两网格误差算子使用冷启动矩阵自由 Lanczos/Ritz。完整路径用 200 步并同时报告 100/200 步差；独立 5,000 步幂迭代抽查与 Lanczos 输出一致。RHS 有效因子保留为另一个应用指标。

## 4. 主比较

13 个规模、对比度与拓扑问题的汇总如下。

| 路径点 | 收敛数 | 收敛案例累计 cycles | 平均 P 密度 | setup 累计 |
|---|---:|---:|---:|---:|
| $m=(1/h)/4$ | 12/13 | 6,133 | 22.0446% | 8.225 s |
| $m=(1/h)/3$ | 13/13 | 5,975 | 31.0828% | 10.754 s |
| $m=(1/h)/2$ | 13/13 | 6,122 | 49.9512% | 15.992 s |
| endpoint | 13/13 | 28,972 | 97.6438% | 62.700 s |

唯一 slow-limit 是 $\chi=10^6$ 时的早期 $m=(1/h)/4$；同问题的 $1/3$、$1/2$ 与 endpoint 均收敛。这说明 $1/3$ 是本测试类中稳健而轻量的默认点，但不是普适最优常数。

中心 $128/16,\chi=10^4$ cross-channel 中，$m=43$ 为 242 cycles、$\rho_{eff}=0.944407$、P 密度 29.4678%；endpoint 为 3,227 cycles、0.995727、98.2099%。

## 5. 真实谱路径

| 拓扑 | 最小 \(\rho_{TG}\) / \(m\) | 最小 \(\rho_{eff}\) / \(m\) | endpoint \(\rho_{TG}\) | 最大 Ritz 阶段差 |
|---|---:|---:|---:|---:|
| cross-channel | 0.949431677 / 36 | 0.941763675 / 38 | 0.996163213 | $5.90\times10^{-5}$ |
| winding-ring | 0.949386203 / 36 | 0.941012129 / 53 | 0.984367546 | $4.41\times10^{-6}$ |

两条 $m=1,\ldots,128$ 全路径均直接呈现有限点之后的谱退化。真实谱最优位置与指定 RHS 最优位置不完全一致，这正是区分理论对象与应用观测量的必要性。图中已删除能量 J 曲线，只保留真实谱半径和 RHS 因子。

## 6. fixed-H / fixed-q 与有限传播

固定物理 coefficient rule、$\chi=10^4$，分别测试：

- fixed-$H$：$1/H=8$，$1/h=32,64,128,256$，$q$ 随加密增长；
- fixed-$q$：$q=8$，同样四个 $1/h$，粗网格同步加密。

每个网格比较 $m=q$ 与 $m=(1/h)/4$。全部 16 行的 `bound_violations=0`，实际最大传播距离均为 $m-1$。fixed-$q$ 时，$m=q$ 的 $mh$ 从 0.25 降至 0.03125，校正支撑比例从 84.45% 降至 1.255%；$m=(1/h)/4$ 始终保持 $mh=0.25$。这直接展示“迭代步数”和“物理覆盖尺度”不是同一概念。

该实验不支持把 $m=(1/h)/4$ 宣称为所有尺度上的性能最优点；它支持的是 $O(h^{-1})$ 在 fixed-$H$ 下具有自然 Krylov/传播尺度，而 fixed-$q$ 的理论尺度不同。

## 7. gap 与一阶方向诊断

在 $16/4,\chi=10^4$ cross-channel 上直接进行稠密分解：

- $\sigma_1(T_F)=0.7896795471$，$\sigma_2(T_F)=0.7829979688$；
- gap $\delta=6.6815783\times10^{-3}$，相对 gap 为 0.8461%；
- endpoint $W$ 与直接 $-B^{-1}C$ 的 Frobenius 差为 $3.44\times10^{-15}$；
- 图公式与直接两网格特征值最大差 $4.07\times10^{-14}$；
- 显式局部 gap 条件从 $m=24$ 起成立；所有有信息的局部邻步一阶符号为 28/28 命中；
- 全路径有信息步为 42/52 命中，说明局部定理不应被错误外推到远离 endpoint 的区域。

这组实验回答了三个容易混淆的问题：simple gap 在该小问题上确实成立；principal angle 通过 $\arctan\sigma_k(Z_m)$ 直接沿路径变化；一阶项在其定理有效域内可靠预测方向，但不构成廉价通用停止规则。

## 8. 稳健性、端点基线和三层试验

固定 $m=(1/h)/3$ 在五个系数 seed 上累计 327 cycles，endpoint 为 866；六 RHS 上累计 469 对 1,507。localized-left 是 endpoint 更快的例外，避免了“所有 RHS 都更优”的过度表述。

中心问题五次交替顺序计时：

| 方法 | setup mean | solve mean | total mean | cycles |
|---|---:|---:|---:|---:|
| finite-1/3 ($m=43$) | 546 ms | 325 ms | 872 ms | 242 |
| endpoint | 2,819 ms | 9,078 ms | 11,896 ms | 3,227 |

几何插值基线需要 85,524 cycles，endpoint 需要 3,227，说明 energy minimization 本身相对几何插值仍有巨大价值；本文结论只是 endpoint 并非这条路径上对固定两网格算子的最佳位置。

三层试验的两种层次均收敛。ring $128/16/8$ 中 finite-1/3 为 237 V-cycles，endpoint 为 770；finite 方法的插值复杂度显著更低。该试验是可行性证据，不扩张为成熟工业 AMG 声明。

## 9. 客观边界与投稿叙事

最强贡献是“精确谱几何 + endpoint 并非局部两网格最优 + PCG 有限传播/尺度 + 真实谱路径”的闭环，而不是算法复杂度。固定分数是经验坐标，允许针对同类实际系数调节；这在数值算法论文中合理，但必须避免以 adaptive/optimal 命名制造过强承诺。

论文应把以下内容放在主线：问题与文献边界、真实谱目标、局部错配机制、有限传播和双尺度、两条完整谱路径与 13 案例结果。容量、奇异值扰动和余项差证明留在附录；seed/RHS/三层结果可放补充材料。结论范围限定在已测试离散与两网格组合，不泛化到任意 AMG。
