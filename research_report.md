# 研究方案与阶段报告 v3.2

## 高对比扩散问题中面向总成本的低预算自适应 PCG

版本：代码 v3.2.0  
理论文件：理论v3.tex  
求解器范围：二维结构网格、Galerkin 两网格、一次前向和一次后向 Gauss--Seidel

---

## 摘要

v3.1 的分阶段自适应器能够把有限 PCG 步数选到离离线 oracle 很近的位置，但为此
需要多个候选两网格构造、较长 pilot 和两个候选的真实容差确认。它适合作为
“高质量选择器”，却使 Setup 远高于一个事先已知步数的固定构造。

v3.2 把目标改成：

> 允许实际循环数相对高质量候选增加约30%，换取显著更低的选择 Setup，并明确
> 判断额外 Setup 能否在多右端项场景中回收。

最终低预算算法只检查几何插值和至多三个正 PCG 检查点；大网格进一步减少一个
中间检查点。算法沿同一 `GlobalEnergyPcgPath` 推进，pilot 长度随 $H/h$ 调整，
不再把最终候选运行到真实容差。低预算模式中的循环数预测只用于选择，实验结果均
重新求解到真实容差，避免把预测值当成实测值。

主要结果如下。

- 原始 $128^2$ 交叉通道选择 $m=40$，234次循环；步长2 oracle 为 $m=38$、
  231次，差距1.3%。同一进程中 v3.2 Setup 约496 ms，高质量 staged 模式约
  1495 ms，下降66.8%。
- 18问题矩阵中，v3.2 总循环数1881，高质量 staged 模式1813，仅增加3.75%；
  18/18均在 staged 循环数的1.3倍以内，最大增加28.48%。
- 18问题 v3.2 Setup 总和约1052 ms，staged 模式约1926 ms，下降45.4%；
  18/18逐例更低。
- 六问题步长2 oracle 中，平均差距6.85%，最大20%，3/6精确命中。
- 策略冻结后的8个新种子/新系数场中，7例精确命中步长4 oracle；唯一困难
  交叉通道差距33.81%，平均差距4.23%。
- 五右端项实验表明：相对失败或很慢的几何插值，低预算方法通常一个右端项即可
  回收 Setup；相对事先已知合适的固定 $m=40$，可能立即占优、需要约3个右端项，
  也可能永远不能回收。这一结果明确了自适应方法的使用边界。

综合判断：v3.2 牺牲了少量选择精度，换来了数量级上更合理的 Setup，已经比 v3.1
更接近可使用的研究原型；但它仍不是任意 SPD 问题上的30% oracle 保证算法。

---

## 1. 研究目标的调整

### 1.1 为什么不继续追逐精确 oracle

v3.1 的六问题 oracle 平均差距只有3.49%，说明继续增加候选和确认预算能够进一步
逼近最小循环数。但这种目标忽略了两个事实。

1. 选出更好的 $m$ 本身需要构造和试运行多个两网格层次；
2. 如果只求解少量右端项，节省的求解循环可能无法回收选择成本。

因此 v3.2 不把“最小循环数”作为唯一目标，而同时记录：

- 选中候选的真实循环数；
- 自适应选择和最终层次构造的 Setup；
- 相对高质量 staged 模式的循环数损失；
- 相对离线 oracle 的候选选择差距；
- 多右端项下的平均求解时间和盈亏平衡点。

### 1.2 接受的精度范围

本阶段把“循环数不超过高质量模式约30%”作为工程目标，而不是理论保证。原因是
两网格循环数在有限 PCG 路径上并不单调，短 pilot 预测也可能有较大误差。30%容忍
度用于限制算法设计，而不能替代独立 oracle 实验。

### 1.3 稀疏支撑策略不再作为主线

v3.1 已删除收益很小的 frontier-gain 支撑扩展策略。v3.2 没有恢复它，也没有继续
围绕很小的密度差异调参。`residual_budget_support.hpp` 仅作为旧基线保留；本阶段
的研究主线完全收敛到有限 PCG 步数选择和总成本。

---

## 2. v3.1 Setup 的来源

v3.1 依次执行：

1. 几何候选安全检查；
2. 多个大间隔候选粗筛；
3. 锚点前方步长2加密；
4. 多个中等长度 pilot；
5. 两个候选继续到真实容差确认。

它的循环质量很好，但 Setup 包含多次候选粗算子构造、粗矩阵分解和代表右端项
迭代。进一步代码审计还发现：`run_pilot` 构造一次 `TwoGridCycle`，
`confirm_candidate` 又为同一候选构造一次。后一次构造没有增加信息。

v3.2 首先修复这一实现问题：候选插值存放在稳定的共享对象中，对应的
`TwoGridCycle` 在 pilot 和 confirmation 之间复用。之所以使用稳定存储，是因为
`TwoGridCycle` 内部保留插值矩阵引用；若直接移动候选记录，会造成悬空引用。
回归测试和 AddressSanitizer 专门覆盖了这条生命周期路径。

仅消除重复构造仍不足以达到目标，因为 v3.1 需要的候选数和确认循环仍然较多。
所以最终方案还必须减少候选和取消真实确认。

---

## 3. v3.2 低预算自适应算法

### 3.1 循环数预测

候选运行 $k$ 次 pilot 后，记相对残差为 $r_k$，尾窗估计的单循环收缩率为
$\widehat\rho$。预计总循环数为

$$
\widehat N=
\begin{cases}
k,&r_k\le\varepsilon,\\
k+\left\lceil\dfrac{\log(\varepsilon/r_k)}{\log\widehat\rho}\right\rceil,
&0<\widehat\rho<1,\\
N_{\max},&\text{其他情况}.
\end{cases}
$$

这比只用 $\widehat\rho$ 更符合实际停止条件，但仍是外推值，不是上界。

### 3.2 稀疏候选集合

一般问题检查

$$
\mathcal M=\{0,m_{\min},m_{\min}+12,40\}.
$$

其中 $m=0$ 是几何插值。对于当前 $128^2$ 大网格，为降低每个候选的粗算子构造
成本，省略中间点，使用

$$
\mathcal M_{\rm large}=\{0,16,40\}.
$$

候选集合来自诊断实验，不应解释为普适最优常数。所有位置都可以通过 options 修改。

### 3.3 与网格比相关的 pilot

默认基础 pilot 为16次。当 $H/h>4$ 时，pilot 随网格比增加；当前 $H/h=8$ 使用
32次。原因是 experiment10 和额外种子诊断都显示，较大 $H/h$ 的瞬态更长，16次
pilot 容易把真正有利的后期候选误判为较差。

这种调整只增加同一候选上的迭代，不增加新的粗算子构造。

### 3.4 低循环快速返回

如果几何候选或早期正候选的预计循环数不超过48，算法立即接受，不再构造后续
候选。该规则对容易问题尤其重要：18问题中的若干 $32^2$ 算例只需约0.6--8 ms
Setup。

### 3.5 不进行真实容差确认

v3.2 默认不在选择阶段把候选继续到 $10^{-6}$。因此报告中区分：

- `estimated_selected_cycles`：pilot 预测；
- `selected_cycles_confirmed=false`：没有在 Setup 内确认；
- 实验表 `Cycles`：选中候选从零初值重新求解到真实容差的结果。

取消确认是 Setup 下降的关键，也是选择精度略低于 v3.1 的主要原因。

### 3.6 高质量模式仍然保留

设置

```cpp
options.cost_aware_mode = false;
```

即可运行粗筛、回溯加密和真实确认模式。它用于：

- 作为消融基线；
- 对 Setup 不敏感、循环数要求更高的任务；
- 检查低预算模式是否发生明显选择退化。

---

## 4. 理论v3的新增内容

理论v3保留了局部化、PCG 正交尾、两网格谱半径公式、有限步非单调性、LMI 判据和
扰动界，并增加了成本感知选择章节。

### 4.1 预测误差到循环损失

若所有候选满足

$$
(1-\delta)N_m\le\widehat N_m\le(1+\delta)N_m,
$$

而选择器返回的候选预测值不超过最小预测值的 $1+\gamma$ 倍，则

$$
N_{\widehat m}
\le
\frac{(1+\gamma)(1+\delta)}{1-\delta}N_\star.
$$

这个结果明确说明：只设置30%容忍参数并不能自动得到30%真实保证，预测误差也必须
受控。当前代码没有对任意 SPD 问题证明统一 $\delta$，所以 oracle 验证不可省略。

### 4.2 Setup 盈亏平衡

若自适应选择成本为 $S_{\rm sel}$，返回候选的构造成本和单右端项平均求解时间为
$S_{\widehat m}$、$Q_{\widehat m}$，则 $R$ 个右端项总时间为

$$
T_{\rm ad}(R)=S_{\rm sel}+S_{\widehat m}+RQ_{\widehat m}.
$$

相对固定基准 $m_0$，当 $Q_{\widehat m}<Q_{m_0}$ 时，盈亏平衡条件为

$$
R>
\frac{S_{\rm sel}+S_{\widehat m}-S_{m_0}}
{Q_{m_0}-Q_{\widehat m}}.
$$

若自适应最终选中与已知固定基准相同的插值，而选择成本更大，则不存在盈亏平衡点。
这一结论防止把“右端项足够多总能回本”当成普遍规律。

### 4.3 多右端项目标

单个代表右端项最优不等于多个右端项平均最优或最坏最优。理论v3分别定义平均成本
和最大成本，并明确：没有额外慢模态投影假设时，不能从一个右端项推出整个工作负载
的保证。因此 experiment14 使用五种不同右端项独立检验。

---

## 5. 实验设计

### 5.1 experiment7：原始核心基准

- $1/h=128$，$1/H=16$，$H/h=8$；
- 对比度 $10^4$；
- 交叉通道，种子1；
- 固定候选 $m=16,24,32,36,38,40,48$；
- 自适应候选 $m=0,16,40$。

### 5.2 experiment9：18问题消融矩阵

- 网格：$(32,8),(64,8),(64,16)$；
- 对比度：$10^2,10^4,10^6$；
- 种子：1和17；
- 交叉、弯曲、对角和平行通道轮换；
- 比较几何、固定 $m=40$、低预算和高质量 staged 模式。

### 5.3 experiment11：步长2 oracle

六个代表问题扫描

$$
m=0,12,14,\ldots,48.
$$

所有候选求解到真实容差，最少循环数作为离线 oracle。

### 5.4 experiment12：诊断集

使用种子101、313、701和907，并加入随机连续场、棋盘场。最初只有两个正候选的
策略在其中出现80%和31.9%的 oracle 差距。该失败促使加入 $m_{\min}+12$，并把
远端移到 $m=40$。因此 experiment12 是参与算法修正的诊断集，不作为最终盲测。

### 5.5 experiment13：最终冻结验证

在 experiment12 后冻结候选和阈值，再使用新种子1301、1601、1907、2203。
八例中六例为通道场，两例为随机连续或棋盘场；oracle 为
$m=0,12,16,\ldots,48$。

### 5.6 experiment14：五右端项摊销

选取四个代表问题，对每个问题构造五种右端项：

- 常数；
- 两个低频正弦组合；
- 局部 Gaussian；
- 中等频率正弦组合；
- 更高频率的确定性组合。

同一两网格层次构造一次并复用。计时先做一次预热，收敛方法重复三轮；几何方法若
达到最大循环数则不重复。报告平均循环、最大循环、平均求解时间和盈亏平衡 RHS 数。

---

## 6. 结果

### 6.1 原始通道

| 方法 | $m$ | 循环数 | Setup ms |
|---|---:|---:|---:|
| 固定候选 oracle | 38 | 231 | 约360 |
| 固定候选 | 40 | 234 | 约385 |
| v3.2 低预算 | **40** | **234** | **约496** |
| staged 高质量 | 38 | 231 | 约1495 |

结论：循环数只增加1.3%，Setup 相对同进程 staged 模式下降约66.8%。v3.2 Setup 仍高于
事先知道 $m=40$ 的固定构造，这是自动选择无法消除的信息成本。

### 6.2 18问题低预算与高质量消融

| 指标 | v3.2 低预算 | staged 高质量 | 变化 |
|---|---:|---:|---:|
| 循环数总和 | 1881 | 1813 | +3.75% |
| Setup 总和 | 约1052 ms | 约1926 ms | **-45.4%** |
| 平均 Setup | 约58.5 ms | 约107.0 ms | **-45.4%** |
| 逐例 Setup 更低 | 18/18 | -- | -- |
| 循环数不超过 staged 的1.3倍 | 18/18 | -- | -- |
| 最大循环增加 | 28.48% | -- | -- |

低预算模式在7例中循环数高于 staged，10例相同，1例更低。出现“更低”不是低预算
具有更强保证，而是给定右端项循环数和有限候选的非单调性所致。

### 6.3 六问题步长2 oracle

| 问题 | v3.2 $m$ | v3.2循环 | oracle $m$ | oracle循环 | 差距 |
|---|---:|---:|---:|---:|---:|
| $32/8$, cross, $10^2$ | 0 | 38 | 0 | 38 | 0% |
| $32/8$, parallel, $10^4$ | 12 | 54 | 20 | 45 | 20% |
| $64/8$, diagonal, $10^2$ | 12 | 228 | 12 | 228 | 0% |
| $64/8$, parallel, $10^6$ | 12 | 160 | 22 | 144 | 11.11% |
| $64/16$, diagonal, $10^4$ | 24 | 55 | 30 | 50 | 10% |
| $64/16$, meandering, $10^6$ | 40 | 51 | 40 | 51 | 0% |

平均差距6.85%，最大20%，3/6精确命中。相比 v3.1 的3.49%，选择精度有所下降
（平均差距增大），但换来了明显更低的选择时间。

### 6.4 诊断集与最终冻结验证

修正后的 experiment12：

- 8例平均 oracle 差距约2.93%；
- 最大13.79%；
- 5例精确命中。

最终 experiment13：

- 8例中7例精确命中；
- 平均差距4.23%；
- 唯一非零差距为交叉通道33.81%；
- 包含随机连续和棋盘场，两例均精确命中。

最终集规模仍然不大，但它在参数冻结后运行，证据强于继续在原18例上调参。

### 6.5 多右端项和盈亏平衡

experiment14 的主要观察为：

1. 几何层次在三个困难问题上达到4000次上限；低预算方法相对几何方法均在第一个
   右端项内回收 Setup。
2. $64/16$、对角通道、$10^4$：低预算选择 $m=24$，平均54.4次；固定 $m=40$
   平均49.2次，但低预算层次更稀疏且一次 Setup 更低，测量中立即占优。
3. $64/16$、交叉通道、$10^6$：低预算平均62.8次，固定 $m=40$ 平均116次；
   约3个右端项后回收相对固定方法的额外 Setup。
4. $128/16$ 原始通道：低预算和固定 $m=40$ 得到相同层次。固定步数若事先已知，
   低预算方法永远不能回收选择开销。
5. staged 模式循环数略低，但相对固定 $m=40$ 的盈亏平衡点在四个问题上分别约为
   65、21、5、76个右端项，显著晚于低预算模式。

这些结果说明 v3.2 的优势不是“右端项越多一定越好”，而是取决于固定基准是否已经
合适，以及选中层次的每 RHS 成本是否确实更低。

---

## 7. 算法是否成功

### 7.1 可以认为成功的部分

1. 原始核心问题循环数几乎不变，Setup 降低约67%；
2. 18问题总循环只增加3.75%，Setup 降低约45%；
3. 所有18例都满足相对 staged 不超过30%的预设目标；
4. 新种子冻结验证没有出现灾难性失败，最大差距33.8%；
5. 明确区分预测循环和实测循环；
6. 用真实多右端项工作负载而不是只用单 RHS 推断摊销；
7. 保留高质量模式，允许调用者按任务切换精度和 Setup；
8. 生命周期错误由消毒器发现并修复，候选复用实现已进入回归测试。

### 7.2 仍不能夸大的部分

1. 候选位置12、24、40和阈值48仍是经验参数；
2. 没有证明短 pilot 在任意 SPD 问题上的统一相对误差；
3. 最终冻结集只有8例；
4. 多 RHS 只有四个系数问题和五种确定性右端项；
5. 计时仍是单机实验，不能跨平台比较绝对毫秒；
6. 没有成熟 AMG 软件外部基线；
7. 当前仍是二维、两网格、结构网格和固定 GS 光滑器；
8. 当合适的固定步数已知时，自适应选择通常没有时间优势。

综合判断：v3.2 是一个“循环数损失受控、Setup 已明显收敛、使用边界清楚”的成功
研究原型。它比 v3.1 更适合作为主算法，而 v3.1 staged 模式适合作为高精度上界和
消融基线。

---

## 8. 软件变更

### 8.1 核心代码

- `adaptive_global_pcg.hpp`
  - 新增默认低预算模式；
  - 新增预测/确认状态区分；
  - 候选集改为少量固定检查点；
  - pilot 随 $H/h$ 调整；
  - 大网格省略中间候选；
  - 保留 staged 模式；
  - pilot 与 confirmation 复用 `TwoGridCycle`；
  - 使用稳定共享插值存储，修复移动后的悬空引用。
- `global_pcg_path.hpp`
  - 继续复用同一 PCG 轨迹，不重复已完成 Krylov 工作。

### 8.2 实验与测试

- experiment9 新增低预算/staged 同机消融；
- experiment9_history 保存所有低预算预测；
- experiment12 保存参与修正的诊断集；
- experiment13 保存冻结后的最终验证；
- experiment14 保存多 RHS 与盈亏平衡结果；
- regression_v32 同时执行低预算和 staged 路径；
- 单元、回归、AddressSanitizer 和 UndefinedBehaviorSanitizer 均纳入验证。

### 8.3 终止性与长任务审计

针对完整验证曾表现为“长时间无输出”的问题，逐条检查了局部 PCG、全局 PCG 路径、
两网格求解、自适应确认和并行工作队列。数值迭代均由 `max_iterations`、`max_cycles`、
有限候选数组或 `maximum_confirmation_cycles` 限制；源码中的 `while (true)` 只用于
原子索引工作队列，任务总数在进入线程前已经确定，不是无界数值迭代。

实际造成误判的两个因素是：长实验过去只在全部案例结束后输出；experiment14 的
计时预热调用了一次完整两网格求解，几何层次不收敛时会额外运行4000次。v3.2.0
审查版已把该预热改为固定两次循环，为 experiment9、11--14 增加逐案例进度，并在
`run_validation.sh` 中为每个步骤加入默认900秒的外层超时。两网格和局部 PCG 的
计数循环也改为零起点的严格小于形式，避免上限取 `INT_MAX` 时发生有符号溢出。

新增回归检查覆盖零循环硬停止、低预算候选数不超过4、默认低预算不执行完整确认，
以及非正确认上限必须被拒绝。experiment14 修正预热后完整四问题、五右端项运行约
29秒；循环数和定性结论不变，表中计时已由修正版重新生成。计时仍是单次墙钟测量，
不应跨机器比较绝对值。

---

## 9. 复现

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=ON
cmake --build build --parallel 4
ctest --test-dir build --output-on-failure
```

快速验证：

```bash
./scripts/run_validation.sh quick
```

完整验证：

```bash
./scripts/run_validation.sh full
```

每一步默认有900秒超时并显示开始/结束状态；可用
`TGI_STEP_TIMEOUT_SECONDS=1800` 等方式调整。长实验还会逐案例输出进度，因此超时
发生时可以定位到具体实验和案例，而不会被误认为内部无限循环。

也可分别运行：

```bash
./build/experiment7 --fine=128 --coarse=16 --threads=4 \
  --contrast=1e4 --max-cycles=40000
./build/experiment9 --threads=4
./build/experiment11 --threads=4
./build/experiment13 --threads=4
./build/experiment14 --threads=4
```

---

## 10. 下一阶段建议

1. 把候选位置从固定常数改成由粗矩阵规模、插值密度和预测不确定度共同决定；
2. 使用两个低成本代表 RHS，而不是恢复两个候选的真实容差确认；
3. 对 pilot 预测建立置信区间，只有候选区间重叠时才追加循环；
4. 扩大冻结 oracle 集，报告均值、分位数、最大值和置信区间；
5. 推广到多层 V-cycle，并与 BoomerAMG、PETSc GAMG 或 MueLu 同机比较；
6. 用真实多载荷或时间序列 RHS 检验层次复用；
7. 把目标从循环数预测直接改为每 RHS 时间预测，包括插值密度和粗分解填充。

---

## 11. 主要结论

1. v3.1 的主要不足不是循环数，而是为逼近 oracle 支付过多候选和确认成本；
2. 候选 `TwoGridCycle` 可在 pilot 和 confirmation 之间安全复用，但插值必须稳定存储；
3. 少量候选、网格比相关 pilot、低循环早停和取消真实确认是 Setup 下降的主要来源；
4. 原始问题循环数只增加1.3%，Setup 下降约66.8%；
5. 18问题总循环增加3.75%，Setup 下降约45.4%，满足预设30%逐例容忍目标；
6. 六问题 oracle 平均差距6.85%，冻结新种子集平均差距4.23%；
7. 预测误差界可以推出条件性的候选 oracle 损失界，但当前没有无条件统一常数；
8. Setup 是否有意义必须由盈亏平衡公式判断，不能笼统声称大量 RHS 一定回本；
9. 相对失败或很慢的几何方法，v3.2 往往一个 RHS 即有优势；
10. 相对已知合适的固定候选，自适应可能永远不能回收额外选择成本；
11. v3.2 可以认为是有效且更实用的自适应 PCG 研究原型，但结论仍限于当前模型、
    候选集和实验范围。
