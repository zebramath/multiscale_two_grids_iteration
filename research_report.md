# 多尺度两网格能量插值研究方案 v3.6

## 1. 项目目标

本项目研究二维高对比扩散方程上粗空间构造、能量最小插值、有限步全局 PCG 插值和两网格收敛之间的定量关系。核心目标不是把插值局部方程求到机器精度，而是在可控的一次性 setup 成本下构造足够好的插值算子，使真实两网格循环数接近离线最优有限 PCG 步数对应的循环数。

当前实现同时回答四个问题：

1. 插值支撑范围、局部代数精度和两网格循环数如何耦合；
2. 全局能量方程的有限 PCG 路径为何可能优于其高精度极限；
3. 如何仅用代表性右端项的短 pilot 和 PCG 能量残差选择有限步数；
4. 该选择在网格、粗化比、对比度、随机种子和复杂系数拓扑变化下是否稳定。

研究对象覆盖连续随机场、二值棋盘场以及交叉、弯曲、对角、平行、分支和闭合曲环六类高导通道。全部公开循环数均来自独立求解到相对残差 $10^{-6}$，选择器中的预测循环数只用于 setup 决策。

## 2. 模型问题与离散化

考虑单位方形区域 $\Omega=(0,1)^2$ 上的 Dirichlet 问题

$$
-\nabla\cdot\bigl(a(x)\nabla u(x)\bigr)=f(x),\qquad
u|_{\partial\Omega}=0,
$$

其中 $a(x)>0$，对比度定义为

$$
\kappa_a=\frac{\max_{x\in\Omega}a(x)}{\min_{x\in\Omega}a(x)}.
$$

代码使用规则方形网格和五点有限差分。相邻节点之间采用调和平均

$$
a_{ij}^{\mathrm{face}}=\frac{2a_i a_j}{a_i+a_j},
$$

得到对称正定稀疏矩阵 $A\in\mathbb{R}^{n\times n}$。粗点由固定粗化比 $H/h$ 选取，默认主问题为 $h=1/128$、$H=1/16$、$H/h=8$、$\kappa_a=10^4$。

系数场采用确定性种子生成。通道类场在随机二值背景上叠加高导结构，既保持指定对比度，也能测试拓扑连通性对粗空间的影响。

## 3. 两网格方法

设插值矩阵为 $P\in\mathbb{R}^{n\times n_c}$，Galerkin 粗矩阵为

$$
A_c=P^TAP.
$$

一次两网格循环使用一次前向和一次后向 Gauss–Seidel 平滑，并在中间执行精确稀疏粗解。若 $S_f$ 和 $S_b$ 分别表示前、后平滑误差算子，则误差传播算子为

$$
E_{TG}=S_b\left(I-PA_c^{-1}P^TA\right)S_f.
$$

实际评价指标为给定右端项下达到

$$
\frac{\|r_k\|_2}{\|r_0\|_2}\le 10^{-6}
$$

所需的循环数。代码同时保留算子收敛因子估计接口，但公开结果不以估计量替代真实循环数。

## 4. 插值理论与构造

### 4.1 几何插值

几何双线性插值 $P_G$ 是最低 setup 成本的基线。其每行只连接邻近粗点，在平滑系数上有效，但不能自动沿高导通道传播粗自由度信息。

### 4.2 能量最小插值

将自由度按粗点 $C$ 和细点 $F$ 排序，并固定粗点注入 $P_C=I$。理想全局能量插值满足

$$
P=\begin{bmatrix}I\\W\end{bmatrix},\qquad
A_{FF}W=-A_{FC}.
$$

它等价于在注入约束下最小化

$$
\mathcal E(P)=\operatorname{tr}(P^TAP).
$$

全局解通常稠密。局部能量插值则只在每个粗点附近的有限 patch 上求解相同变分问题，以支撑误差换取稀疏度和较低 setup。

### 4.3 局部化、剪枝与支撑扩张

固定 patch 层数控制几何局部化误差；局部 PCG 容差控制同一支撑内的代数误差。两者必须分开测量，否则不能判断循环数变化来自支撑还是线性求解精度。

全局参考插值可按列相对阈值剪枝：若

$$
|P_{ij}|\le \delta\max_k|P_{kj}|,
$$

则删除该 F 点条目，同时严格保留 C 点注入。该实验用于观察密度—循环数前沿，不作为低成本生产构造。

支撑扩张包含几何加层、强连接一次扩张和残差预算自适应扩张。自适应扩张按残差指标能量标记列，只重求被标记的局部问题，从而避免对全部列统一放大支撑。

### 4.4 有限步全局 PCG

从 $P_G$ 出发，对所有粗列沿同一全局能量 PCG 路径推进。第 $m$ 步得到 $P_m$。对第 $j$ 列，预条件残差能量为 $r_{j,m}^TM^{-1}r_{j,m}$；全局归一化指标定义为

$$
\eta_m=
\left(
\frac{\sum_j r_{j,m}^TM^{-1}r_{j,m}}
{\sum_j r_{j,0}^TM^{-1}r_{j,0}}
\right)^{1/2}.
$$

$\eta_m$ 描述插值能量方程上的推进程度，但两网格循环数关于 $m$ 不必单调。有限步会改变粗空间的频谱选择，过度逼近全局能量极限可能增加插值密度，却不一定改善当前平滑器和代表性右端项下的两网格收敛。因此必须直接沿 PCG 路径测量或预测循环数，而不能把最小 $\eta_m$ 当作唯一目标。

## 5. 自适应 PCG 选择器

### 5.1 输入与候选预算

选择器输入 $A$、$P_G$、代表性右端项 $b$、步数区间 $[m_{\min},m_{\max}]$、步长量子和求解容差。默认 pilot 最多执行 24 个两网格循环。算法不读取系数场名称、对比度、随机种子或离线最优步数。

候选层次至多四个：

1. 几何初值 $m=0$；
2. 最小步锚点 $m=m_{\min}$；
3. 搜索区间中点；
4. 一个补充点。

若初值或锚点的短 pilot 已预示易解，则立即停止扩展候选集。否则检查中点。中点仍较难时，用 $\eta_m\le\sqrt{\tau}$ 判断能量方程是否已深入收敛区：满足时补充锚点—中点内点，不满足时补充中点—上界前向点。该规则只使用一个与目标容差直接关联的量，不需要拓扑特定参数。

### 5.2 pilot 尾部模型

设短 pilot 的残差范数为 $s_0,s_1,\ldots,s_k$。对尾部窗口和块长 $q$，计算

$$
\ell_i=\frac{1}{q}\log\frac{s_i}{s_{i-q}}.
$$

以中位数和 MAD 构造保守尾部速率，得到 $\widehat\rho<1$ 与相对不确定度。预测完整循环数为

$$
\widehat k=
k+\left\lceil
\frac{\log(\tau/(s_k/s_0))}{\log\widehat\rho}
\right\rceil.
$$

选择器先找到最小 $\widehat k$，再在其 13% 松弛范围内选择步数最小的候选。这一准则显式平衡预测循环数与插值构造成本。若所有预测完全相同，则以 $\eta_m$ 打破平局。

### 5.3 层次复用

每个 pilot 候选需要构造 $P_m$、$A_c$、粗排序和稀疏 Cholesky 因子。v3.6 将选中候选的插值矩阵与完整 `TwoGridCycle` 一同返回，正式验证和多右端项求解直接复用该层次，不再重复形成 $AP$、$P^TAP$ 或再次分解粗矩阵。

## 6. 系数场与复杂拓扑

| 类别 | 构造 | 主要测试目的 |
|---|---|---|
| continuous | 多频随机 Fourier 场经指数映射 | 平滑但强变化的连续介质 |
| checker | 粗块尺度随机二值场 | 大量孤立跳跃界面 |
| cross-channel | 横纵高导通道与随机背景 | 交叉连通结构 |
| meandering-channel | 正弦弯曲高导带 | 长距离非直线连通 |
| diagonal-channels | 两条对角高导带 | 斜向跨粗网格传播 |
| parallel-channels | 三条轻微摆动的平行带 | 多分量并行传播 |
| branching-channels | 主干加上下分支 | 分叉网络与路径竞争 |
| winding-ring | 五重起伏闭合曲环 | 闭合连通、非边界贯通结构 |

新增的分支和闭合曲环均随种子改变相位，并与随机二值背景叠加。单元测试检查它们同时包含背景值和高导值、达到指定对比度，且两个拓扑不退化为同一数组。

## 7. 软件结构与效率实现

`code/src/multigrid` 只保留四个核心模块：

| 模块 | 职责 |
|---|---|
| `energy_interpolation.hpp` | 几何、局部、全局能量插值，强连接距离插值和剪枝 |
| `support_expansion.hpp` | 强连接支撑与残差预算支撑扩张 |
| `global_pcg.hpp` | 增量全局 PCG 路径、自适应候选和层次复用 |
| `two_grid_solver.hpp` | 稀疏 Galerkin 乘积、粗排序、稀疏 Cholesky、平滑和循环 |

主要效率措施如下：

- 矩阵统一使用 CSR，Galerkin 乘积按行块并行并直接形成对称结果；
- 全局 PCG 的所有候选复用同一条路径，较大步数只增量推进，不从 $m=0$ 重算；
- 输出 $P_m$ 前先精确统计有效非零元，再一次性分配 triplet 容量，降低峰值内存和扩容成本；
- 两网格迭代的残差、细网格乘积、限制、粗解和校正向量通过 `Workspace` 循环复用；
- 选中候选的 Galerkin 层次直接进入正式求解，消除重复 setup；
- 粗矩阵使用嵌套剖分风格排序与稀疏 Cholesky，粗解工作向量复用；
- OpenMP 和列级工作线程只在问题规模达到阈值后启用，避免小问题并行开销。

## 8. 实验组织

全部测试角度统一组织为 6 个公开实验入口：

| 入口 | 内部研究 |
|---|---|
| `experiment1_localization` | 支撑半径、局部容差、全局剪枝、支撑扩张 |
| `experiment2_pcg_path` | 有限预算、步长 2 密集扫描、pilot 诊断 |
| `experiment3_adaptive_oracle` | 主问题轨迹、8 问题 step-2 oracle |
| `experiment4_robustness` | 18 问题网格/对比度/种子/六拓扑矩阵 |
| `experiment5_diagnostics` | 8 问题附加诊断与 8 问题压力测试 |
| `experiment6_workload` | 4 问题、每题 5 种右端项的摊销测试 |

每个入口只写一份同名 TXT；`results` 不包含 CSV。计时为当前执行环境中的单次 wall-clock 结果，适合比较数量级和同表候选，不应解释为跨机器微基准。

## 9. 主要数值结果

### 9.1 主通道有限步扫描

默认问题为 $h=1/128$、$H=1/16$、$\kappa_a=10^4$ 的交叉通道场。

| 方法 | $m$ | $P$ 密度 | Setup ms | Total ms | 循环数 |
|---|---:|---:|---:|---:|---:|
| PCG-global | 32 | 20.4547% | 224.322 | 429.048 | 347 |
| PCG-global | 36 | 23.5999% | 255.124 | 453.541 | 268 |
| PCG-global | 38 | 25.2786% | 268.112 | 425.427 | 231 |
| PCG-global | 40 | 26.9969% | 277.105 | 451.313 | 234 |
| PCG-global | 42 | 28.6040% | 287.657 | 476.975 | 238 |
| PCG-global | 48 | 33.8960% | 343.012 | 579.125 | 256 |
| global-exact | 收敛到 $10^{-10}$ | 97.8420% | 1768.775 | 8761.642 | 3227 |

最优扫描点为 $m=38$、231 次循环。高精度全局极限的密度和循环数都明显更高，确认“两网格最优”与“插值能量方程求得最精确”不是同一目标。

### 9.2 主问题自适应结果

| 指标 | 自适应结果 | 离线最优 |
|---|---:|---:|
| 选中步数 | 40 | 38 |
| 真实循环数 | 234 | 231 |
| 循环数 gap | 1.30% | 0 |
| 插值密度 | 26.9969% | 25.2786% |
| 自适应一次性 setup | 555.233 ms | oracle 不计成本 |

自适应 setup 包含四个短 pilot 候选；正式求解不再重复选中层次。选择器没有执行任何完整容差确认求解。

### 9.3 8 问题 step-2 oracle

| 网格 $1/h,1/H$ | 对比度 | 拓扑 | 自适应 $m$/循环 | oracle $m$/循环 | gap |
|---|---:|---|---:|---:|---:|
| 32,8 | $10^2$ | cross | 0 / 38 | 0 / 38 | 0.00% |
| 32,8 | $10^4$ | parallel | 12 / 54 | 20 / 45 | 20.00% |
| 32,8 | $10^6$ | winding-ring | 24 / 59 | 20 / 51 | 15.69% |
| 64,8 | $10^2$ | diagonal | 12 / 228 | 12 / 228 | 0.00% |
| 64,8 | $10^6$ | parallel | 12 / 160 | 22 / 144 | 11.11% |
| 64,8 | $10^4$ | branching | 12 / 167 | 12 / 167 | 0.00% |
| 64,16 | $10^4$ | diagonal | 24 / 55 | 30 / 50 | 10.00% |
| 64,16 | $10^6$ | meandering | 34 / 58 | 40 / 51 | 13.73% |

平均 gap 为 8.82%，最大 gap 为 20.00%。该 oracle 在 $m=0$ 和 $m=12,14,\ldots,48$ 上逐一完整求解，只用于评价选择质量。

### 9.4 18 问题稳健性矩阵

18 个问题平衡轮换六类通道拓扑，并覆盖 $(1/h,1/H)=(32,8),(64,8),(64,16)$、对比度 $10^2,10^4,10^6$ 和种子 1、17。

| 方法 | 全部收敛数 | 总循环数 | 平均循环数 | 相对 fixed-$m40$ |
|---|---:|---:|---:|---:|
| fixed $m=40$ | 18/18 | 2431 | 135.06 | 基线 |
| adaptive | 18/18 | 1900 | 105.56 | 总循环数降低 21.84% |

| 复杂拓扑样例 | 网格 | 对比度 | 自适应 $m$ | 自适应循环 | fixed-$m40$ 循环 |
|---|---:|---:|---:|---:|---:|
| branching | 32/8 | $10^6$ | 12 | 53 | 78 |
| winding-ring | 32/8 | $10^6$ | 24 | 59 | 78 |
| branching | 64/8 | $10^6$ | 34 | 274 | 262 |
| winding-ring | 64/8 | $10^6$ | 34 | 195 | 217 |
| branching | 64/16 | $10^6$ | 24 | 66 | 67 |
| winding-ring | 64/16 | $10^6$ | 24 | 66 | 94 |

几何基在 6 个高难问题上达到 12000 次上限；固定 PCG 与自适应在全部问题上收敛。

### 9.5 附加诊断与压力测试

| 测试组 | 问题数 | 平均 oracle gap | 最大 gap | 零 gap 数 |
|---|---:|---:|---:|---:|
| 附加诊断 | 8 | 4.96% | 13.79% | 3 |
| 压力测试 | 8 | 6.73% | 28.10% | 6 |

附加诊断中的 winding-ring 在 $10^6$ 对比度下选 $m=24$、64 次循环，oracle 为 $m=20$、63 次，gap 仅 1.59%；branching 的两个诊断点分别得到 0% gap。压力组的最大差距来自 seed 2203 的 cross-channel，说明 24 步 pilot 对强非渐近残差曲线仍可能低估后段循环数。

### 9.6 局部化与支撑结果

| 场 | 构造 | 密度 | 循环数 | 结论 |
|---|---|---:|---:|---|
| continuous | local layers=4 | 20.5466% | 90 | 达到 global 的 90 次循环 |
| channel | local layers=2 | 6.0922% | 6840 | 小几何支撑不能跟随长通道 |
| channel | adaptive support | 6.2390% | 3553 | 近似同密度下显著改善 |
| checker | local layers=2 | 6.0922% | 4144 | 支撑过小 |
| checker | local layers=3 | 12.5471% | 243 | 加一层带来数量级改善 |

局部 PCG 容差在到达支撑误差平台后继续收紧，通常只增加 setup 而不减少循环数；因此局部支撑选择比把固定 patch 上的代数系统求得更精确更重要。

### 9.7 五右端项工作负载

| 问题 | 自适应 $m$ | 一次性 setup | 平均循环 | 平均 solve ms | 对 fixed-$m40$ break-even |
|---|---:|---:|---:|---:|---:|
| 64/8 branching, $10^4$ | 12 | 58.104 ms | 211.6 | 21.038 | 2 RHS |
| 64/16 winding-ring, $10^4$ | 26 | 137.768 ms | 63.6 | 18.233 | 12 RHS |
| 64/16 cross, $10^6$ | 26 | 133.320 ms | 59.4 | 17.268 | 3 RHS |
| 128/16 cross, $10^4$ | 40 | 529.255 ms | 231.4 | 172.689 | 20 RHS |

选中层次可直接复用于不同右端项。对复杂高对比问题，自适应相对几何基通常在第一个右端项即回收 setup；相对固定 $m=40$ 的回收点依赖问题和每次求解节省。

## 10. 严格检查与验证

最终源码执行了以下检查：

- 6 个实验入口、单元测试和回归测试均以 C++17、Release 优化、`-Wall -Wextra -Wpedantic -Werror` 编译通过；
- `unit_core` 与 `regression_v36` 正常通过；
- 单元测试覆盖矩阵维数、正对角、粗解相对残差小于 $10^{-10}$、插值注入、剪枝单调性、固定步预算、局部求解失败传播、复杂拓扑和可复用自适应层次；
- UndefinedBehaviorSanitizer 版本的单元测试与回归测试通过；
- 6 个整合实验均由最终 v3.6 源码重新运行并生成 6 份 TXT；
- 源码目录不含 CSV，代码脚本和模块不含有限步 Jacobi 候选；
- `theory.tex` 保持不变。

当前执行环境未安装 CMake，因此实际验证使用与 CMake 目标等价的直接 `g++` 命令。项目仍提供标准 CMake 配置和 `scripts/run_validation.sh`，可在常规开发环境中直接运行。

## 11. 结论与边界

当前实现表明：

1. 高对比问题中，粗空间质量主要受拓扑相关支撑和有限步频谱选择控制；
2. 全局能量方程的高精度极限不是两网格循环数的可靠代理；
3. 短 pilot、稳健尾部预测和归一化 PCG 能量残差可以在不读取场类型先验的情况下选择实用步数；
4. 主问题循环数距 step-2 oracle 仅 1.30%，8 问题 oracle 平均 gap 8.82%；
5. 选中层次复用显著减少自适应结果交付给正式求解时的重复 setup；
6. 分支与闭合曲环测试证明算法不只适用于直线或单通道结构。

边界也很明确：代表性右端项改变时，最优 $m$ 可能改变；短 pilot 在少数强非渐近问题上仍有 20%–28.10% 的 oracle gap；单次 wall-clock 计时受机器负载影响。后续研究应优先分析 pilot 尾部模型与多右端项代表性，而不是继续增加拓扑特定规则。

## 12. 复现实验

在 `code` 目录执行：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=ON
cmake --build build --parallel 4
ctest --test-dir build --output-on-failure
./scripts/run_validation.sh full
```

可用 `TGI_THREADS` 调整线程数，用 `TGI_STEP_TIMEOUT_SECONDS` 调整单实验超时。输出固定写入 `code/results` 下的 6 份 TXT。
