\documentclass[11pt]{ctexart}

\usepackage[a4paper,margin=2.35cm]{geometry}
\usepackage{amsmath,amssymb,amsthm,bm,booktabs}
\usepackage[hidelinks]{hyperref}

\newtheorem{theorem}{定理}[section]
\newtheorem{proposition}[theorem]{命题}
\newtheorem{corollary}[theorem]{推论}

\DeclareMathOperator{\tr}{tr}
\DeclareMathOperator{\diag}{diag}
\DeclareMathOperator{\Ran}{Ran}
\DeclareMathOperator{\cond}{cond}
\newcommand{\R}{\mathbb R}
\newcommand{\norm}[1]{\left\lVert #1\right\rVert}
\newcommand{\abs}[1]{\left\lvert #1\right\rvert}
\newcommand{\Sc}{\bm S}
\newcommand{\rhoTG}{\rho_{\rm TG}}

\title{有限 Krylov 能量插值与尺度自适应两网格选择：理论分析 v5.4}
\author{崔正平}
\date{}

\begin{document}
\maketitle

\begin{abstract}
本文研究全局能量下降与两网格收敛目标之间的非单调关系：对固定粗点和固定磨光器，
有限步 Krylov 插值可能比能量极小终点产生更小的两网格收敛因子。
理论主线与当前实现逐项对应。首先，有限 PCG 对能量极小插值具有精确的正交误差账本。
其次，引入归一化误差
\[
 \bm Z=\bm B^{1/2}(\bm W-\bm W^\ast)\Sc^{-1/2},
\]
它同时刻画插值能量、Galerkin 粗矩阵增量和粗空间主角。由此得到对称两网格谱半径的
精确投影表达式，以及其对粗空间扰动的全局稳定界。再次，在能量终点附近，即使主导
奇异值具有重数，两网格目标的一阶变化仍可由主导奇异子空间上的对称压缩刻画；这给出
有限步优于精确能量插值的充分条件，并在显式余项控制下导出严格的反单调 PCG 尾部。
最后，在标准 Jacobi--PCG 条件数按 $h^{-2}$ 伸缩且只增加一侧路径非退化条件的模型族
上，本文给出最优路径步数的条件性 $\Theta(h^{-1})$ 窗口；据此构造与网格分辨率同步
伸缩的 fast/reuse 两档选择，并给出一般连续模下的固定候选覆盖界、候选相关且非对称
的 pilot 后悔界，以及多右端项盈亏平衡式。

这些结论构成“为何在有限 Krylov 路径上选点”的理论依据。数值验证采用二维规则网格、
高对比扩散、固定粗点和两网格结构，以受控方式检验这一机制。
\end{abstract}

\section{问题设置与研究边界}

考虑有限维对称正定系统
\begin{equation}
 \bm A\bm u=\bm b,\qquad
 \bm A=
 \begin{bmatrix}\bm B&\bm C\\ \bm C^T&\bm A_{CC}\end{bmatrix}\succ0,
 \qquad \bm B=\bm A_{FF}\succ0.                       \label{eq:blockA}
\end{equation}
固定粗点单位注入后，插值写成
\begin{equation}
 \bm P(\bm W)=\begin{bmatrix}\bm W\\\bm I\end{bmatrix},
 \qquad \bm W\in\R^{n_F\times n_C}.                  \label{eq:P}
\end{equation}
当前算法从几何插值出发，对各列方程使用 Jacobi--PCG，沿同一条增量路径生成有限步候选。
每个候选均配合同一前向/后向 Gauss--Seidel 磨光器和精确 Galerkin 粗解。

本文区分三个层次：能量极小插值是变分终点；有限 PCG 是生成候选的路径；两网格谱半径
或代表右端项上的实际循环数才是最终选择目标。把这三个量混为一谈，正是“能量越小，
两网格必然越好”这一错误直觉的来源。

\subsection{结论与假设层级}

全文按代数恒等式、全局界、局部机制和算法误差界四个层次组织假设。
\begin{center}
\begin{tabular}{p{0.20\textwidth}p{0.69\textwidth}}
\toprule
结论层级 & 明确假设 \\
\midrule
代数恒等式 & $\bm A\succ0$、固定粗点单位注入；网格可为一般离散结构 \\
PCG 能量账本 & 每列方程使用 SPD 预条件的标准 CG，结论按精确算术陈述 \\
两网格投影公式 & 精确 Galerkin 粗解，后磨光为前磨光的 $\bm A$--伴随 \\
终点一阶机制 & 能量终点处相关最大奇异值为正；允许任意有限重数 \\
严格反单调尾部 & 进入终点邻域，并满足主导奇异子空间上的逐步方向条件~\eqref{eq:tailcondition} \\
最优步数尺度 & 标准条件数伸缩、一侧路径非退化、最优点处于固定误差带 \\
候选覆盖界 & 归一化投影路径具有网格一致连续模，且磨光算子范数一致有界 \\
选择后悔界 & 仅需真实最优候选的高估界和实际所选候选的低估界 \\
\bottomrule
\end{tabular}
\end{center}

当前数值实现中的“global-reference”基线用 $10^{-10}$ 有限容差构造，它是高精度
数值参考；数学符号 $\bm W^\ast$ 才表示精确能量极小点。两者在论文表述中应始终区分。

\section{能量终点与有限 PCG 的精确账本}

定义插值总能量
\begin{equation}
 J(\bm W)=\frac12\tr\!\left(\bm P(\bm W)^T
                    \bm A\bm P(\bm W)\right),
 \qquad \norm{\bm X}_{\bm B,F}^2=\tr(\bm X^T\bm B\bm X).
\end{equation}

\begin{theorem}[能量极小点与精确能量差]\label{thm:energy}
唯一极小点和对应 Schur 补为
\begin{equation}
 \bm W^\ast=-\bm B^{-1}\bm C,\qquad
 \Sc=\bm A_{CC}-\bm C^T\bm B^{-1}\bm C\succ0.        \label{eq:Wstar}
\end{equation}
令 $\bm\Delta=\bm W-\bm W^\ast$，则
\begin{align}
 J(\bm W)-J(\bm W^\ast)
 &=\frac12\norm{\bm\Delta}_{\bm B,F}^2,              \label{eq:energygap}\\
 \bm P(\bm W)^T\bm A\bm P(\bm W)
 &=\Sc+\bm\Delta^T\bm B\bm\Delta.                   \label{eq:acgap}
\end{align}
\end{theorem}

\begin{proof}
展开 $J$ 的一阶条件得 $\bm B\bm W+\bm C=0$。把
$\bm W=\bm W^\ast+\bm\Delta$ 代回，交叉项因
$\bm B\bm W^\ast+\bm C=0$ 消失，即得两式。
\end{proof}

能量极小插值及局部优化插值的经典算法背景可参见
\cite{WanChanSmith2000,Dohrmann2007,OlsonSchroderTuminaro2011,JannaEtAl2023}。
这些工作以能量、近核空间再现、局部支撑或并行实现为主要设计准则；本文固定粗点与
初始插值，由式~\eqref{eq:Wstar} 出发，研究同一全局能量方程的有限 Krylov 路径与
两网格目标之间的非单调关系。

第 $j$ 列满足 $\bm B\bm w_j^\ast=-\bm C\bm e_j$。
设 $\bm w_{j,m}$ 是从给定初值运行 $m$ 步 PCG 的结果，
$\bm e_{j,m}=\bm w_j^\ast-\bm w_{j,m}$，
$\bm s_{j,m}=\bm w_{j,m+1}-\bm w_{j,m}$。

\begin{theorem}[PCG 的正交能量尾]\label{thm:pcgtail}
在精确算术下，对每个尚未收敛的列，
\begin{equation}
 \norm{\bm e_{j,m}}_{\bm B}^2
 =\norm{\bm e_{j,m+1}}_{\bm B}^2
  +\norm{\bm s_{j,m}}_{\bm B}^2.                      \label{eq:pcgpyth}
\end{equation}
因而对矩阵误差 $\bm\Delta_m=\bm W_m-\bm W^\ast$，
\begin{equation}
 \norm{\bm\Delta_m}_{\bm B,F}^2
 =\norm{\bm\Delta_{m+1}}_{\bm B,F}^2
  +\norm{\bm W_{m+1}-\bm W_m}_{\bm B,F}^2.           \label{eq:matrixpyth}
\end{equation}
特别地，插值能量沿 PCG 路径单调不增；只要至少一列产生非零 PCG 更新，该步便严格
下降。
\end{theorem}

\begin{proof}
CG 的 Galerkin 条件给出
$\bm e_{j,m+1}\perp_{\bm B}(\bm w_{j,m+1}-\bm w_{j,m})$。
对 $\bm e_{j,m}=\bm e_{j,m+1}+\bm s_{j,m}$ 使用勾股恒等式并对列求和。
\end{proof}

该定理刻画路径朝能量终点前进；两网格目标还取决于粗空间方向。两者的差异是后文的
逻辑起点。PCG 的经典最佳逼近和多项式界可参见~\cite{Greenbaum1997,Saad2003}。

\section{归一化误差统一能量、粗矩阵与粗空间}

令
\begin{equation}
 \bm Z=\bm B^{1/2}\bm\Delta\Sc^{-1/2}.                 \label{eq:Z}
\end{equation}
再定义两个 $\bm A$--正交归一基
\begin{equation}
 \bm Q_F=\begin{bmatrix}\bm B^{-1/2}\\0\end{bmatrix},
 \qquad
 \bm Q_\ast=\bm P(\bm W^\ast)\Sc^{-1/2}.             \label{eq:bases}
\end{equation}
它们满足
$\bm Q_F^T\bm A\bm Q_F=\bm I$、
$\bm Q_\ast^T\bm A\bm Q_\ast=\bm I$ 和
$\bm Q_F^T\bm A\bm Q_\ast=0$。
记对应的欧氏正交基为
$\widetilde{\bm Q}_F=\bm A^{1/2}\bm Q_F$ 和
$\widetilde{\bm Q}_\ast=\bm A^{1/2}\bm Q_\ast$。

\begin{theorem}[归一化误差的三个精确含义]\label{thm:Z}
对任意 $\bm W$：
\begin{align}
 2\{J(\bm W)-J(\bm W^\ast)\}
 &=\norm{\bm Z\Sc^{1/2}}_F^2,                         \label{eq:Zenergy}\\
 \bm P(\bm W)^T\bm A\bm P(\bm W)
 &=\Sc^{1/2}(\bm I+\bm Z^T\bm Z)\Sc^{1/2}.          \label{eq:Zcoarse}
\end{align}
此外，$\Ran(\bm P(\bm W))$ 在
$\Ran(\bm Q_F)\oplus\Ran(\bm Q_\ast)$ 坐标中是图空间
\begin{equation}
 \Ran\begin{bmatrix}\bm Z\\\bm I\end{bmatrix}.
\end{equation}
若 $\theta_i$ 是该空间与能量极小粗空间的主角，则
\begin{equation}
 \tan\theta_i=\sigma_i(\bm Z),\qquad
 \sin\theta_{\max}=
 \frac{\norm{\bm Z}_2}{\sqrt{1+\norm{\bm Z}_2^2}}.   \label{eq:angles}
\end{equation}
\end{theorem}

\begin{proof}
前两式由定理~\ref{thm:energy} 和定义~\eqref{eq:Z} 直接得到。
又有
$\bm P(\bm W)\Sc^{-1/2}=\bm Q_\ast+\bm Q_F\bm Z$。
在 $\bm A$--正交坐标中应用图空间主角公式即可；参见~\cite{BjorckGolub1973}。
\end{proof}

式~\eqref{eq:Zenergy} 与~\eqref{eq:angles} 说明能量距离和粗空间距离相关但不等价：
前者是带 $\Sc$ 权的 Frobenius 量，后者由 $\bm Z$ 的最大奇异值决定。
因此即使总能量严格下降，影响最坏两网格模态的方向仍可能变坏。

\section{两网格性能的精确表达与稳定界}

令 $\bm G$ 表示前磨光误差传播算子，后磨光取其 $\bm A$--伴随。
在欧氏能量坐标中定义
\begin{equation}
 \bm T=\bm A^{1/2}\bm G\bm A^{-1/2}.
\end{equation}
对粗空间 $\mathcal U=\Ran(\bm A^{1/2}\bm P)$，令 $\bm\Pi_{\mathcal U}$ 为
欧氏正交投影，并定义对应的 $\bm A$--正交粗校正投影
\begin{equation}
 \bm Q_{\mathcal U}^{\bm A}
 =\bm P(\bm P^T\bm A\bm P)^{-1}\bm P^T\bm A.
\end{equation}
再记
$\bm G^{\dagger_{\bm A}}=\bm A^{-1}\bm G^T\bm A$ 及
$\bm E_{TG}=\bm G^{\dagger_{\bm A}}
(\bm I-\bm Q_{\mathcal U}^{\bm A})\bm G$。该对称两网格算子与
$\bm T^T(\bm I-\bm\Pi_{\mathcal U})\bm T$ 相似，故
\begin{equation}
 \rhoTG(\mathcal U)=
 \norm{(\bm I-\bm\Pi_{\mathcal U})\bm T}_2^2.         \label{eq:rho}
\end{equation}
这一表达是后续分析的固定框架；精确两网格收敛估计、两网格扰动与非精确变体的背景
可参见~\cite{FalgoutVassilevskiZikatanov2005,Notay2007,XuZhang2022}。

\begin{proposition}[最坏谱因子与给定 RHS 循环数的层级]\label{prop:rhs}
令
$\bm K_{\mathcal U}=\bm T^T(\bm I-\bm\Pi_{\mathcal U})\bm T$，并令
$\bm e_{k+1}=\bm E_{TG}\bm e_k$、$\bm r_k=\bm A\bm e_k$。
若 $\rhoTG(\mathcal U)<1$，则对任意非零初始误差
\begin{equation}
 \norm{\bm e_k}_{\bm A}
 \le \rhoTG(\mathcal U)^k\norm{\bm e_0}_{\bm A}.      \label{eq:energycontract}
\end{equation}
对应欧氏残量满足
\begin{equation}
 \frac{\norm{\bm r_k}_2}{\norm{\bm r_0}_2}
 \le \sqrt{\cond_2(\bm A)}\,
       \rhoTG(\mathcal U)^k.                           \label{eq:residualcontract}
\end{equation}
给定 RHS 的实际循环数还取决于初始误差在
$\bm K_{\mathcal U}$ 各特征方向上的分量；$\rhoTG$ 提供最坏情形控制。
\end{proposition}

\begin{proof}
$\bm A^{1/2}\bm E_{TG}\bm A^{-1/2}=\bm K_{\mathcal U}$，而
$\bm K_{\mathcal U}$ 对称半正定且
$\norm{\bm K_{\mathcal U}}_2=\rhoTG(\mathcal U)$，故第一式成立。
再用
$\sqrt{\lambda_{\min}(\bm A)}\norm{\bm e}_{\bm A}
\le\norm{\bm A\bm e}_2
\le\sqrt{\lambda_{\max}(\bm A)}\norm{\bm e}_{\bm A}$
作用于分子和分母即可。
\end{proof}

命题~\ref{prop:rhs} 给出理论与实现的接口：谱半径描述最坏情形，代码则用代表 RHS 的
短残量历史选择候选，分别对应谱目标与给定载荷目标。

\begin{proposition}[由 $\bm Z$ 给出的精确粗投影]\label{prop:projector}
在基~\eqref{eq:bases} 中，图空间的正交投影为
\begin{equation}
 \bm\Pi(\bm Z)=
 \begin{bmatrix}\bm Z\\\bm I\end{bmatrix}
 (\bm I+\bm Z^T\bm Z)^{-1}
 \begin{bmatrix}\bm Z^T&\bm I\end{bmatrix}.           \label{eq:projector}
\end{equation}
因此
\begin{equation}
 \rhoTG(\bm W)=
 \norm{(\bm I-\bm\Pi(\bm Z))\bm T}_2^2,              \label{eq:rhoZ}
\end{equation}
其中 $\bm T$ 也写在同一能量正交坐标中。
\end{proposition}

\begin{proof}
令 $\bm Y=[\bm Z^T,\bm I]^T$。图空间的欧氏正交投影为
$\bm Y(\bm Y^T\bm Y)^{-1}\bm Y^T$，且
$\bm Y^T\bm Y=\bm I+\bm Z^T\bm Z$，故得式~\eqref{eq:projector}；再代入
式~\eqref{eq:rho} 即得式~\eqref{eq:rhoZ}。
\end{proof}

\begin{theorem}[固定磨光器下的最佳同维粗空间]\label{thm:globalopt}
在所有维数为 $n_C$ 的粗空间中，
\begin{equation}
 \min_{\dim\mathcal U=n_C}\rhoTG(\mathcal U)
 =\sigma_{n_C+1}(\bm T)^2.                             \label{eq:bestspace}
\end{equation}
一个最优空间由 $\bm T$ 的前 $n_C$ 个左奇异向量张成。
\end{theorem}

\begin{proof}
这是矩阵最佳秩 $n_C$ 逼近的投影形式：
$\min_{\operatorname{rank}\bm\Pi=n_C}\norm{(\bm I-\bm\Pi)\bm T}_2
=\sigma_{n_C+1}(\bm T)$。
\end{proof}

定理~\ref{thm:globalopt} 给出固定磨光器下的理论最优空间。直接以两网格收敛率定义
最优插值的相关研究可参见~\cite{BrannickEtAl2018}。能量极小粗空间一般与该最优空间
不同，因而有限步候选优于能量终点在结构上完全可能。

\begin{theorem}[谱半径平方根的全局稳定性]\label{thm:lipschitz}
对任意两个同维粗空间 $\mathcal U,\mathcal V$，
\begin{align}
 \abs{\sqrt{\rhoTG(\mathcal U)}-
       \sqrt{\rhoTG(\mathcal V)}}
 &\le \norm{\bm T}_2\,
       \norm{\bm\Pi_{\mathcal U}-\bm\Pi_{\mathcal V}}_2
 =\norm{\bm T}_2\sin\theta_{\max}(\mathcal U,\mathcal V),       \label{eq:lipschitz}\\
 \abs{\rhoTG(\mathcal U)-\rhoTG(\mathcal V)}
 &\le \bigl(\sqrt{\rhoTG(\mathcal U)}+
              \sqrt{\rhoTG(\mathcal V)}\bigr)
       \norm{\bm T}_2\sin\theta_{\max}(\mathcal U,\mathcal V). \label{eq:rholipschitz}
\end{align}
特别地，对 $\bm W$ 与 $\bm W^\ast$，右端最后一项可由
式~\eqref{eq:angles} 精确写成 $\bm Z$ 的函数。
\end{theorem}

\begin{proof}
记 $f_{\mathcal U}=\norm{(\bm I-\bm\Pi_{\mathcal U})\bm T}_2$。
谱范数的逆三角不等式给出
\[
 \abs{f_{\mathcal U}-f_{\mathcal V}}
 \le\norm{(\bm\Pi_{\mathcal U}-\bm\Pi_{\mathcal V})\bm T}_2
 \le\norm{\bm\Pi_{\mathcal U}-\bm\Pi_{\mathcal V}}_2\norm{\bm T}_2.
\]
等维子空间满足
$\norm{\bm\Pi_{\mathcal U}-\bm\Pi_{\mathcal V}}_2
=\sin\theta_{\max}(\mathcal U,\mathcal V)$；参见~\cite{Bhatia1997}，故得
式~\eqref{eq:lipschitz}。再由
$\abs{f_{\mathcal U}^2-f_{\mathcal V}^2}
=(f_{\mathcal U}+f_{\mathcal V})\abs{f_{\mathcal U}-f_{\mathcal V}}$
得到式~\eqref{eq:rholipschitz}。与常用的
$2\norm{\bm T}_2^2\sin\theta_{\max}$ 上界相比，式~\eqref{eq:rholipschitz}
保留了两个粗空间自身的实际收敛因子，因而在二者都较好时更紧。
\end{proof}

\begin{corollary}[固定性能增益需要非零能量距离]\label{cor:barrier}
若某候选相对能量终点满足
$\sqrt{\rhoTG(\bm W^\ast)}-\sqrt{\rhoTG(\bm W)}\ge\gamma>0$，则
\begin{equation}
 \norm{\bm Z}_2\ge
 \frac{\gamma}{\sqrt{\norm{\bm T}_2^2-\gamma^2}},
 \qquad 0<\gamma<\norm{\bm T}_2,                       \label{eq:barrier}
\end{equation}
并且相应插值能量超额满足
\begin{equation}
 J(\bm W)-J(\bm W^\ast)
 \ge \frac{\lambda_{\min}(\Sc)}{2}
 \frac{\gamma^2}{\norm{\bm T}_2^2-\gamma^2}.          \label{eq:energybarrier}
\end{equation}
\end{corollary}

\begin{proof}
由定理~\ref{thm:lipschitz} 和式~\eqref{eq:angles}，
$\gamma\le\norm{\bm T}_2\norm{\bm Z}_2/\sqrt{1+\norm{\bm Z}_2^2}$。
平方并整理即得式~\eqref{eq:barrier}。再由式~\eqref{eq:Zenergy}，
\[
 J(\bm W)-J(\bm W^\ast)
 =\tfrac12\norm{\bm Z\Sc^{1/2}}_F^2
 \ge\tfrac12\lambda_{\min}(\Sc)\norm{\bm Z}_F^2
 \ge\tfrac12\lambda_{\min}(\Sc)\norm{\bm Z}_2^2,
\]
代入式~\eqref{eq:barrier} 即得式~\eqref{eq:energybarrier}。
\end{proof}

这两个下界把固定幅度的两网格优势同时转化为非零粗空间距离和非零插值能量超额，说明
有限步优势不能无限逼近能量终点。

\section{有限步优于能量终点的局部机制}

记
\begin{equation}
 f(\bm Z)=\sqrt{\rhoTG(\bm Z)}
 =\norm{(\bm I-\bm\Pi(\bm Z))\bm T}_2.
\end{equation}
设 $\bm Z=0$ 时最大奇异值 $\sigma=f(0)>0$ 的重数为 $r$。取对应的正交左右
奇异子空间基 $\bm U,\bm V$，使
$ (\bm I-\bm\Pi(0))\bm T\bm V=\sigma\bm U$ 与
$\bm T^T(\bm I-\bm\Pi(0))\bm U=\sigma\bm V$。在式~\eqref{eq:bases} 的
欧氏能量坐标中定义
\begin{equation}
 \bm U_F=\widetilde{\bm Q}_F^T\bm U,
 \qquad
 \bm Y=\widetilde{\bm Q}_\ast^T\bm T\bm V,
 \qquad \operatorname{sym}(\bm X)=\tfrac12(\bm X+\bm X^T).       \label{eq:ua}
\end{equation}
由于 $\bm U$ 属于 $\Ran(\bm I-\bm\Pi(0))$，其粗空间坐标为零。

\begin{theorem}[能量终点处的一阶有利方向]\label{thm:firstorder}
对任意方向 $\bm H$，
\begin{equation}
 f'(0;\bm H)=
 -\lambda_{\min}\!\left(
   \operatorname{sym}(\bm U_F^T\bm H\bm Y)\right).   \label{eq:derivative}
\end{equation}
这里无需最大奇异值为单。若
$\lambda_{\min}(\operatorname{sym}(\bm U_F^T\bm H\bm Y))>0$，则存在
$\varepsilon>0$，使
$0<t<\varepsilon$ 时
\begin{equation}
 \rhoTG(t\bm H)<\rhoTG(0),
 \qquad
 J(\bm W^\ast+\bm B^{-1/2}t\bm H\Sc^{1/2})>J(\bm W^\ast).
                                                                    \label{eq:finitebetter}
\end{equation}
即一个能量上更差的有限误差可产生更好的两网格因子。
\end{theorem}

\begin{proof}
由~\eqref{eq:projector}，
\[
 \bm\Pi'(0)[\bm H]=
 \begin{bmatrix}0&\bm H\\\bm H^T&0\end{bmatrix}.
\]
令 $\bm F(\bm Z)=(\bm I-\bm\Pi(\bm Z))\bm T$，则
$\bm F'(0)[\bm H]=-\bm\Pi'(0)[\bm H]\bm T$。重最大奇异值的方向导数公式
（参见~\cite{Bhatia1997}）给出
\[
 f'(0;\bm H)=\lambda_{\max}\!\left(
 \operatorname{sym}(\bm U^T\bm F'(0)[\bm H]\bm V)\right).
\]
又因 $\widetilde{\bm Q}_\ast^T\bm U=0$，分块相乘得到
$\bm U^T\bm F'(0)[\bm H]\bm V=-\bm U_F^T\bm H\bm Y$，从而得到
式~\eqref{eq:derivative}。所给矩阵正定时方向导数严格为负，故第一结论成立；第二结论
由~\eqref{eq:Zenergy} 得到，因为其能量超额为
$t^2\norm{\bm H\Sc^{1/2}}_F^2/2>0$。
\end{proof}

当 $r=1$ 时，式~\eqref{eq:derivative} 退化为
$f'(0;\bm H)=-\bm u_F^T\bm H\bm y$，即原来的“问题可见性与路径对齐”标量。
当 $r>1$ 时，要求对整个主导奇异子空间正定，而不是任意选取一对不唯一的奇异向量；
因此结论既消除了单性假设，也避免了基选择造成的歧义。尺度、对比度和拓扑进入
$\bm U_F,\bm Y$，实际 Krylov 误差方向进入 $\bm H$。

\subsection{严格反单调 PCG 尾部}

令 $\bm Z_m=\bm B^{1/2}\bm\Delta_m\Sc^{-1/2}$，并记
\begin{equation}
 g(\bm Z)=\lambda_{\min}\!\left(
 \operatorname{sym}(\bm U_F^T\bm Z\bm Y)\right).     \label{eq:gZ}
\end{equation}
最大奇异值的完整重数为 $r$，故该主导簇与其余奇异值分离。结合谱范数的重奇异值
扰动展开~\cite{Bhatia1997} 和投影~\eqref{eq:projector} 的解析性，存在邻域
$\norm{\bm Z}_F\le r_0$ 与常数 $K>0$，使
\begin{equation}
 f(\bm Z)=f(0)-g(\bm Z)+R(\bm Z),
 \qquad \abs{R(\bm Z)}\le K\norm{\bm Z}_F^2.          \label{eq:taylor}
\end{equation}

\begin{theorem}[有限 PCG 的严格反单调尾部]\label{thm:antimonotone}
设连续一段 PCG 候选均落在上述邻域，并且每一步满足
\begin{equation}
 g(\bm Z_m)-g(\bm Z_{m+1})
 >K\bigl(\norm{\bm Z_m}_F^2+\norm{\bm Z_{m+1}}_F^2\bigr).          \label{eq:tailcondition}
\end{equation}
则在该段上
\begin{equation}
 J(\bm W_{m+1})<J(\bm W_m),
 \qquad
 \rhoTG(\bm W_{m+1})>\rhoTG(\bm W_m).                 \label{eq:antimonotone}
\end{equation}
\end{theorem}

\begin{proof}
条件~\eqref{eq:tailcondition} 的右端非负且左端严格为正，故
$\bm Z_m\ne\bm Z_{m+1}$，相应 PCG 步非零。第一式于是由
定理~\ref{thm:pcgtail} 严格成立。
另一方面，由~\eqref{eq:taylor}，
\begin{align*}
 f(\bm Z_{m+1})-f(\bm Z_m)
 &=g(\bm Z_m)-g(\bm Z_{m+1})+R(\bm Z_{m+1})-R(\bm Z_m)\\
 &\ge g(\bm Z_m)-g(\bm Z_{m+1})
 -K(\norm{\bm Z_m}_F^2+\norm{\bm Z_{m+1}}_F^2)>0.
\end{align*}
平方不改变严格不等号。
\end{proof}

条件~\eqref{eq:tailcondition} 是可检验的充分条件：主导子空间上的一阶有利量压过二阶
余项时，“能量下降、两网格因子上升”形成严格尾部。它允许主导奇异值具有重数；当
$r=1$ 时，$g(\bm Z_m)-g(\bm Z_{m+1})$ 正好退化为原标量步进方向。

\subsection{高对比通道的可见性来源}

对加权图拉普拉斯型离散，设节点集 $S$ 的加权边界与体积为
\begin{equation}
 \partial_w S=\sum_{i\in S,j\notin S}w_{ij},\qquad
 \operatorname{vol}_w(S)=\sum_{i\in S}d_i,
 \quad d_i=\sum_jw_{ij}.
\end{equation}

\begin{proposition}[低割比通道产生慢 Jacobi 模态]\label{prop:channel}
令 $\bm L$ 为加权图拉普拉斯，$\bm D=\diag(d_i)$，其中 $d_i>0$，并取
$\bm x=\bm D^{1/2}\bm 1_S$。则
\begin{equation}
 \frac{\bm x^T\bm D^{-1/2}\bm L\bm D^{-1/2}\bm x}
      {\bm x^T\bm x}
 =\frac{\partial_w S}{\operatorname{vol}_w(S)}.        \label{eq:cutmode}
\end{equation}
因此内部强连接而外部弱连接的通道产生低 Jacobi 缩放特征值，难由局部磨光快速消除。
\end{proposition}

\begin{proof}
代入 $\bm x=\bm D^{1/2}\bm 1_S$ 后，分子为
$\bm 1_S^T\bm L\bm 1_S=\partial_w S$，分母为
$\bm 1_S^T\bm D\bm 1_S=\operatorname{vol}_w(S)$。
\end{proof}

该命题解释高对比拓扑提高主导慢模态可见性的来源；最佳 PCG 步数还由粗点覆盖和
路径对齐共同确定。

\section{最优步数的尺度窗口与自适应选择}

令 $n=1/h$ 为每个坐标方向的细网格区间数，并定义可由矩阵直接取得的对角尺度比
\begin{equation}
 \chi_A=\frac{\max_i A_{ii}}{\min_i A_{ii}}.           \label{eq:diagratio}
\end{equation}

\subsection{固定能量衰减所需的步数}

令 $\bm D_h=\diag(\bm B_h)$ 为当前实现使用的 Jacobi 预条件器，假定
$\bm W_{0,h}\ne\bm W_h^\ast$，并记
\begin{equation}
 \kappa_h=\cond_2(\bm D_h^{-1/2}\bm B_h\bm D_h^{-1/2}),\qquad
 d_{m,h}=\frac{\norm{\bm W_{m,h}-\bm W_h^\ast}_{\bm B_h,F}}
 {\norm{\bm W_{0,h}-\bm W_h^\ast}_{\bm B_h,F}}.       \label{eq:dh}
\end{equation}
标准 PCG 能量范数估计~\cite{Greenbaum1997,Saad2003} 给出
\begin{equation}
 d_{m,h}\le 2q_h^m,\qquad
 q_h=\frac{\sqrt{\kappa_h}-1}{\sqrt{\kappa_h}+1}.      \label{eq:cgscale}
\end{equation}

\begin{proposition}[固定能量衰减的线性分辨率上界]\label{prop:fixedreduction}
若某个网格族满足 $\kappa_h\le\kappa_+h^{-2}$，则对任意固定
$\eta\in(0,1)$，只要
\begin{equation}
 m\ge \frac12\left(\frac{\sqrt{\kappa_+}}{h}+1\right)
             \log\frac{2}{\eta},                     \label{eq:fixedreduction}
\end{equation}
便有 $d_{m,h}\le\eta$。因此达到固定相对能量误差只需 $O(h^{-1})$ 步。
\end{proposition}

\begin{proof}
对 $x\ge1$ 有 $(x-1)/(x+1)\le\exp\{-2/(x+1)\}$。把
$x=\sqrt{\kappa_h}\le\sqrt{\kappa_+}/h$ 代入式~\eqref{eq:cgscale} 即得。
\end{proof}

命题~\ref{prop:fixedreduction} 给出可由条件数控制的一侧界。实际步数下界还取决于初始
误差是否含有端点附近的谱分量；若这些分量随网格细化消失，PCG 可以明显早于最坏情形
收敛。下面把这一必要的非退化性质显式列入假设。

\begin{theorem}[最优有限步的条件性 $\Theta(h^{-1})$ 窗口]
\label{thm:optimalwindow}
考虑一族固定对比度范围、拓扑类别、粗化比和磨光器的问题。设存在与 $h$ 无关的正常数
$\kappa_+,a_-,\gamma_+$，使
\begin{equation}
 \kappa_h\le\kappa_+h^{-2},
 \qquad
 d_{m,h}\ge a_-e^{-\gamma_+mh}
 \quad(0\le m\le m_{\rm opt}(h)).                    \label{eq:onesideddecay}
\end{equation}
再设两网格目标的一个最优有限步 $m_{\rm opt}(h)$ 落在固定可见误差带
\begin{equation}
 0<\eta_-\le d_{m_{\rm opt}(h),h}\le\eta_+<1,       \label{eq:visibleband}
\end{equation}
其中 $a_->\eta_+$。则
\begin{equation}
 \frac{1}{\gamma_+}\log\frac{a_-}{\eta_+}\,h^{-1}
 \le m_{\rm opt}(h)\le
 \frac12\left(\frac{\sqrt{\kappa_+}}{h}+1\right)
 \log\frac{2}{\eta_-}.                              \label{eq:optimalwindow}
\end{equation}
特别地，$m_{\rm opt}(h)=\Theta(h^{-1})$。
\end{theorem}

\begin{proof}
把式~\eqref{eq:visibleband} 的上界与式~\eqref{eq:onesideddecay} 的路径下界组合，得到
$a_-e^{-\gamma_+m_{\rm opt}h}\le\eta_+$，从而得到式~\eqref{eq:optimalwindow} 左侧。
对于另一侧，不再假设路径具有人为给定的指数上包络。由标准 PCG 界
式~\eqref{eq:cgscale}、不等式
$q_h\le\exp\{-2/(\sqrt{\kappa_h}+1)\}$ 和条件数假设直接得到
\[
 d_{m,h}\le
 2\exp\!\left\{-\frac{2m}{\sqrt{\kappa_h}+1}\right\}
 \le
 2\exp\!\left\{-\frac{2m}{\sqrt{\kappa_+}/h+1}\right\}.
\]
在 $m=m_{\rm opt}(h)$ 处结合 $d_{m_{\rm opt},h}\ge\eta_-$，取对数即得
式~\eqref{eq:optimalwindow} 右侧。左端常数为正；右端在 $h\to0$ 时为
$O(h^{-1})$，故双侧合起来给出 $\Theta(h^{-1})$。
\end{proof}

式~\eqref{eq:onesideddecay} 的第二部分只排除相关谱质量随细化消失而导致的超快路径；
另一侧完全由标准条件数界推出，因而只需一侧路径非退化假设。式~\eqref{eq:visibleband}
把全局距离屏障和局部反单调尾部概括为跨网格稳定的
有利区间。常数可随固定的对比度范围、拓扑类别和粗化比变化。PCG 的代数终止上界由
细点系统维数决定，而式~\eqref{eq:optimalwindow} 描述两网格性能最优点的模型尺度。

当前七个设计组 step--2 sampled oracle 满足
$m_{\rm or}/(1/h)\in[0.125,0.484]$，为固定归一化搜索窗提供当前测试族上的证据。
它们用于检验尺度假设，不等同于定理~\ref{thm:optimalwindow} 中连续路径上的
$m_{\rm opt}(h)$，也不参与在线选择。

下面把“候选数固定而步数随 $h^{-1}$ 伸缩”写成网格一致结论。令
$\bm\Pi_h(s)$ 表示归一化位置 $s=mh$ 上粗空间投影的连续插值，并记
\begin{equation}
 \phi_h(s)=\norm{(\bm I-\bm\Pi_h(s))\bm T_h}_2,
 \qquad \rho_h(s)=\phi_h(s)^2.                       \label{eq:normalizedprofile}
\end{equation}

\begin{theorem}[固定归一化候选的网格一致覆盖界]
\label{thm:normalizedcover}
令固定有限集合 $\mathcal S\subset[\alpha,\beta]$，并对每个 $\xi\in\mathcal S$
取实际整数候选 $m_\xi=\operatorname{round}(\xi/h)$。记
\begin{equation}
 I_h=[\alpha,\beta]\cup\{hm_\xi:\xi\in\mathcal S\}.
\end{equation}
假设存在与 $h$ 无关的 $M_T>0$ 和非减连续模
$\omega_\Pi:[0,\infty)\to[0,\infty)$，其中 $\omega_\Pi(0)=0$ 且
$\omega_\Pi(t)\to0$ 当 $t\downarrow0$，使对任意 $s,t\in I_h$，
\begin{equation}
 \norm{\bm\Pi_h(s)-\bm\Pi_h(t)}_2
 \le\omega_\Pi(\abs{s-t}),
 \qquad \norm{\bm T_h}_2\le M_T.                   \label{eq:pathmodulus}
\end{equation}
令 $s_h^*$ 最小化 $\phi_h$，连续候选集和舍入后候选集的覆盖半径分别为
\begin{equation}
 \delta_{\mathcal S}=
 \max_{s\in[\alpha,\beta]}\min_{\xi\in\mathcal S}\abs{s-\xi},
 \qquad
 \delta_{\mathcal S,h}=
 \max_{s\in[\alpha,\beta]}\min_{\xi\in\mathcal S}\abs{s-hm_\xi}.
\end{equation}
则 $\delta_{\mathcal S,h}\le\delta_{\mathcal S}+h/2$。令
$\widehat m_h$ 最小化舍入候选上的 $\phi_h(hm)$，并记
$\varepsilon_h=M_T\omega_\Pi(\delta_{\mathcal S,h})$。那么
\begin{align}
 0&\le \phi_h(h\widehat m_h)-\phi_h(s_h^*)
 \le \varepsilon_h,                                  \label{eq:coverphi}\\
 0&\le \rho_h(h\widehat m_h)-\rho_h(s_h^*)
 \le \varepsilon_h
 \min\{2M_T,\,2\phi_h(s_h^*)+\varepsilon_h\}.      \label{eq:coverrho}
\end{align}
\end{theorem}

\begin{proof}
对任意 $s\in[\alpha,\beta]$，取 $\xi_s\in\mathcal S$ 使
$\abs{s-\xi_s}\le\delta_{\mathcal S}$。整数舍入满足
$\abs{hm_{\xi_s}-\xi_s}\le h/2$，故三角不等式给出
\[
 \min_{\xi\in\mathcal S}\abs{s-hm_\xi}
 \le\abs{s-hm_{\xi_s}}
 \le\delta_{\mathcal S}+h/2.
\]
对 $s$ 取最大即得 $\delta_{\mathcal S,h}\le\delta_{\mathcal S}+h/2$。

再取舍入候选 $m_\xi$ 使
$\abs{s_h^*-hm_\xi}\le\delta_{\mathcal S,h}$。由定理~\ref{thm:lipschitz} 与
式~\eqref{eq:pathmodulus}，
\[
 \abs{\phi_h(hm_\xi)-\phi_h(s_h^*)}
 \le \norm{\bm T_h}_2
      \norm{\bm\Pi_h(hm_\xi)-\bm\Pi_h(s_h^*)}_2
 \le\varepsilon_h.
\]
候选最优性给出式~\eqref{eq:coverphi}。令
$\Delta_h=\phi_h(h\widehat m_h)-\phi_h(s_h^*)$，则
$0\le\Delta_h\le\varepsilon_h$，并且 $0\le\phi_h\le M_T$。于是
\begin{align*}
 \rho_h(h\widehat m_h)-\rho_h(s_h^*)
 &=\Delta_h\{\phi_h(h\widehat m_h)+\phi_h(s_h^*)\}\\
 &\le\varepsilon_h
 \min\{2M_T,\,2\phi_h(s_h^*)+\varepsilon_h\},
\end{align*}
即得式~\eqref{eq:coverrho}。
\end{proof}

该定理只要求网格一致连续性，不再要求 Lipschitz 正则性，也不再假定舍入候选仍落在
原区间内；平方目标的结论还保留真实最优因子。固定候选预算的离散损失由归一化覆盖
半径而非网格规模控制，并且不要求实际循环数关于 $m$ 单调。当前 reuse 集合的区间为
$[1/8,1/2]$，连续覆盖半径为 $1/12$。

\subsection{两档实现}

令 $n_H=1/H=n/r$ 为每方向粗网格区间数。算法保留 fast 与 reuse 两档。fast 面向
单个或少量右端项，不运行 pilot，只构造
\begin{equation}
 m_{\rm fast}=
 \begin{cases}
 \operatorname{round}(n/8),&n_H\le8,\\
 \operatorname{round}(n/4),&n_H>8,\ \chi_A<10^3,\\
 \operatorname{round}(n/3),&n_H>8,\ 10^3\le\chi_A<10^5,\\
 \operatorname{round}(n/2),&n_H>8,\ \chi_A\ge10^5.
 \end{cases}                                           \label{eq:fast}
\end{equation}
该规则只使用粗空间分辨率和矩阵对角尺度比：小粗空间采用短路径，其余问题按尺度比的
三个对数区间选取归一化位置。阈值 $10^3,10^5$ 是测试对比度 $10^2,10^4,10^6$ 的
对数中点。fast 始终只有一次路径推进、一次插值组装和一次粗算子构造。

reuse 面向插值被多个右端项复用的场景，在归一化区间 $[n/8,n/2]$ 上取固定五点
\begin{equation}
\mathcal M_{\rm reuse}
 =\operatorname{round}\left\{n/8,3n/16,n/4,n/3,n/2\right\},
 \qquad L=\operatorname{round}(n/2).                  \label{eq:reuse}
\end{equation}
重复候选在小网格上合并。候选数不随 $n$ 增长，而搜索区间和 pilot 长度随 $n$ 同步
伸缩；几何端点被移出在线选择，新增 $3n/16$ 以细化 oracle 较集中的左半区间。实现按
$m$ 递增地延续同一条 PCG 路径，并流式保留当前优胜者。

\subsection{收敛预测与选择规则}

对 reuse 候选 $m$，令 $\ell_m\le L$ 为实际执行的 pilot 循环数，达到正式容差时提前
停止。记第 $k$ 个两网格循环后的相对残量为 $r_{m,k}$，取历史中点
$k_0=\lfloor\ell_m/2\rfloor$，并用后半段几何平均收缩率
\begin{equation}
 \widehat\rho_m=
 \left(\frac{r_{m,\ell_m}}{r_{m,k_0}}\right)^{1/(\ell_m-k_0)}.
                                                               \label{eq:pilotfactor}
\end{equation}
若 pilot 尚未收敛，由此预测达到容差 $\tau$ 的总循环数
\begin{equation}
 \widehat N_m=\ell_m+
 \max\left\{0,
 \left\lceil\frac{\log(\tau/r_{m,\ell_m})}
 {\log\widehat\rho_m}\right\rceil
 \right\},                                             \label{eq:forecast}
\end{equation}
并截断到正式循环上限；若已在 pilot 内收敛，则取 $\widehat N_m=\ell_m$。reuse 选择
$\widehat N_m$ 最小者，预测相同时取较小的 $m$。式~\eqref{eq:pilotfactor} 直接估计
实际使用的两网格残量收缩，与投影目标 $\rhoTG$ 具有一致的“越小越好”方向。

\begin{theorem}[预测误差到选择后悔值]\label{thm:regret}
令 $e_m=\widehat N_m-N_m$，$\widehat m$ 为上述规则的输出，
$m_{\mathcal M}$ 为候选集内真实循环数最少者。无需控制其余候选的预测误差，恒有
\begin{equation}
 0\le N_{\widehat m}-N_{m_{\mathcal M}}
 \le e_{m_{\mathcal M}}-e_{\widehat m}.              \label{eq:exactregret}
\end{equation}
特别地，只要真实最优候选的高估和实际所选候选的低估分别满足
$e_{m_{\mathcal M}}\le\varepsilon_+^{\rm opt}$ 与
$-e_{\widehat m}\le\varepsilon_-^{\rm sel}$，就有
\begin{equation}
 N_{\widehat m}
 \le N_{m_{\mathcal M}}+
 \varepsilon_+^{\rm opt}+\varepsilon_-^{\rm sel}.   \label{eq:regret}
\end{equation}
若 $N_m>0$，且只对这两个候选有单侧相对界
\begin{equation}
 \widehat N_{m_{\mathcal M}}
 \le(1+\delta_+^{\rm opt})N_{m_{\mathcal M}},
 \qquad
 \widehat N_{\widehat m}
 \ge(1-\delta_-^{\rm sel})N_{\widehat m},
 \quad 0\le\delta_-^{\rm sel}<1,
\end{equation}
则
\begin{equation}
 \frac{N_{\widehat m}}{N_{m_{\mathcal M}}}
 \le\frac{1+\delta_+^{\rm opt}}
          {1-\delta_-^{\rm sel}}.                   \label{eq:relregret}
\end{equation}
统一双侧界 $\abs{e_m}\le\varepsilon$ 和统一相对界 $\delta$ 分别只是
式~\eqref{eq:regret} 中 $2\varepsilon$ 与式~\eqref{eq:relregret} 中
$(1+\delta)/(1-\delta)$ 的特例。
\end{theorem}

\begin{proof}
由真实最优性，$N_{\widehat m}-N_{m_{\mathcal M}}\ge0$。由预测最优性，
$\widehat N_{\widehat m}\le\widehat N_{m_{\mathcal M}}$，所以
\begin{align*}
 N_{\widehat m}-N_{m_{\mathcal M}}
 &=\widehat N_{\widehat m}-\widehat N_{m_{\mathcal M}}
   +e_{m_{\mathcal M}}-e_{\widehat m}\\
 &\le e_{m_{\mathcal M}}-e_{\widehat m},
\end{align*}
得到式~\eqref{eq:exactregret}。插入两个单侧加法界即得式~\eqref{eq:regret}。
对于相对界，同一条预测最优链给出
\[
 (1-\delta_-^{\rm sel})N_{\widehat m}
 \le\widehat N_{\widehat m}
 \le\widehat N_{m_{\mathcal M}}
 \le(1+\delta_+^{\rm opt})N_{m_{\mathcal M}},
\]
除以正数便得式~\eqref{eq:relregret}。
\end{proof}

该结果表明后悔值只由“真实最优候选被高估多少”和“实际所选候选被低估多少”决定，
无需对所有候选施加统一双侧准确性。它把 reuse 的 pilot 精度与候选集内选择损失
同时按绝对量和比例定量联系起来，
对应参数选择中“廉价代理--真实目标--额外开销”的标准结构~\cite{FrascaCacciaSingh2023}。
fast 以单点规则消除选择开销；reuse 支付五个候选的预测成本，以提高多右端项下的
摊销收益。

\subsection{多右端项的盈亏平衡}

设策略 $a,b$ 的一次 setup 为 $S_a,S_b$，单右端项平均求解时间为 $T_a,T_b$。
对 $R$ 个右端项，总成本为
\begin{equation}
 C_a(R)=S_a+RT_a.                                       \label{eq:cost}
\end{equation}
若 $S_b>S_a$ 且 $T_b<T_a$，细致策略胜出的充分必要条件是
\begin{equation}
 R>R_{\rm break}=\frac{S_b-S_a}{T_a-T_b}.              \label{eq:breakeven}
\end{equation}
式~\eqref{eq:breakeven} 给出两档策略的可解释选择阈值，代表右端项的迁移能力由多
右端项实验检验。相应的 RHS 迁移实验由常数 RHS 选择候选，再在六种平滑、局部与
振荡 RHS 上独立求解，其结果直接衡量当前测试族上的代表性。

\section{结论与适用范围}

本文以同一个 $\bm Z$ 坐标把能量极小插值、粗空间几何和固定磨光器下的两网格目标
闭合到有限 PCG 路径：
\[
 \boxed{
 \text{PCG 正交能量尾}
 \longrightarrow \bm Z\text{ 的粗空间几何}
 \longrightarrow \rhoTG\text{ 的投影目标}
 \longrightarrow \text{有限步非单调性与尺度自适应选择}.}
\]
代数恒等式和全局稳定界适用于有限维 SPD 系统、固定粗点及对称两网格结构；终点
一阶机制和反单调尾部允许主导奇异值具有任意有限重数，后者另需局部邻域与方向条件。
最优步数的双侧尺度窗口由标准条件数伸缩、一侧路径非退化和固定可见误差带推出。
归一化覆盖界只要求一致连续模，并保留舍入误差与真实最优因子；预测后悔界只控制两个
决定性候选的单侧误差。数值部分在二维规则网格与高对比通道族上隔离并检验这一主线，
fast/reuse 两档体现少右端项与多右端项之间的 setup--solve 权衡。

\begin{thebibliography}{99}

\bibitem{WanChanSmith2000}
W.-L. Wan, T. F. Chan, and B. Smith,
An energy-minimizing interpolation for robust multigrid methods,
\emph{SIAM Journal on Scientific Computing}, 21(4):1632--1649, 2000,
\href{https://doi.org/10.1137/S1064827598334277}{doi:10.1137/S1064827598334277}.

\bibitem{Dohrmann2007}
C. R. Dohrmann,
Interpolation operators for algebraic multigrid by local optimization,
\emph{SIAM Journal on Scientific Computing}, 29(5):2045--2058, 2007,
\href{https://doi.org/10.1137/06066103X}{doi:10.1137/06066103X}.

\bibitem{OlsonSchroderTuminaro2011}
L. N. Olson, J. B. Schroder, and R. S. Tuminaro,
A general interpolation strategy for algebraic multigrid using energy minimization,
\emph{SIAM Journal on Scientific Computing}, 33(2):966--991, 2011,
\href{https://doi.org/10.1137/100803031}{doi:10.1137/100803031}.

\bibitem{JannaEtAl2023}
C. Janna, A. Franceschini, J. B. Schroder, and L. Olson,
Parallel energy-minimization prolongation for algebraic multigrid,
\emph{SIAM Journal on Scientific Computing}, 45(5):A2561--A2584, 2023,
\href{https://doi.org/10.1137/22M1513794}{doi:10.1137/22M1513794}.

\bibitem{BrannickEtAl2018}
J. Brannick, F. Cao, K. Kahl, R. D. Falgout, and X. Hu,
Optimal interpolation and compatible relaxation in classical algebraic multigrid,
\emph{SIAM Journal on Scientific Computing}, 40(3):A1473--A1493, 2018,
\href{https://doi.org/10.1137/17M1123456}{doi:10.1137/17M1123456}.

\bibitem{FrascaCacciaSingh2023}
G. Frasca-Caccia and P. Singh,
Optimal parameters for numerical solvers of PDEs,
\emph{Journal of Scientific Computing}, 97:11, 2023,
\href{https://doi.org/10.1007/s10915-023-02324-0}
{doi:10.1007/s10915-023-02324-0}.

\bibitem{Greenbaum1997}
A. Greenbaum,
\emph{Iterative Methods for Solving Linear Systems},
SIAM, 1997,
\href{https://doi.org/10.1137/1.9781611970937}{doi:10.1137/1.9781611970937}.

\bibitem{Saad2003}
Y. Saad,
\emph{Iterative Methods for Sparse Linear Systems}, 2nd ed.,
SIAM, 2003,
\href{https://doi.org/10.1137/1.9780898718003}{doi:10.1137/1.9780898718003}.

\bibitem{BjorckGolub1973}
{\AA}ke Bj\"orck and G. H. Golub,
Numerical methods for computing angles between linear subspaces,
\emph{Mathematics of Computation}, 27(123):579--594, 1973,
\href{https://doi.org/10.1090/S0025-5718-1973-0348991-3}
{doi:10.1090/S0025-5718-1973-0348991-3}.

\bibitem{Bhatia1997}
R. Bhatia,
\emph{Matrix Analysis},
Springer, 1997,
\href{https://doi.org/10.1007/978-1-4612-0653-8}
{doi:10.1007/978-1-4612-0653-8}.

\bibitem{FalgoutVassilevskiZikatanov2005}
R. D. Falgout, P. S. Vassilevski, and L. T. Zikatanov,
On two-grid convergence estimates,
\emph{Numerical Linear Algebra with Applications}, 12(5--6):471--494, 2005,
\href{https://doi.org/10.1002/nla.437}{doi:10.1002/nla.437}.

\bibitem{Notay2007}
Y. Notay,
Convergence analysis of perturbed two-grid and multigrid methods,
\emph{SIAM Journal on Numerical Analysis}, 45(3):1035--1044, 2007,
\href{https://doi.org/10.1137/060652312}{doi:10.1137/060652312}.

\bibitem{XuZhang2022}
X. Xu and C.-S. Zhang,
Convergence analysis of inexact two-grid methods: A theoretical framework,
\emph{SIAM Journal on Numerical Analysis}, 60(1):133--156, 2022,
\href{https://doi.org/10.1137/20M1356075}{doi:10.1137/20M1356075}.

\end{thebibliography}

\end{document}
