# 研究方案与完整阶段报告 v3.0

## 高对比扩散问题中的有限步能量插值、自适应停止与稀疏支撑扩展

**对应代码：** `two_grids_iteration_v3.0.0`  
**理论依据：** `理论v2.tex`  
**核心新增实验：** experiment7--experiment9  
**两网格框架：** 一次前向 Gauss--Seidel、Galerkin 粗校正、一次后向
Gauss--Seidel  
**停止标准：** 相对残差 $10^{-6}$

---

## 摘要

本研究考察高对比扩散离散系统中，插值矩阵的能量精度、支撑稀疏性和两网格
收敛之间并不一致的现象。v2.11 已发现：在 $h=1/128$、$H=1/16$、对比度
$10^4$ 的通道问题中，从几何插值出发构造全局能量列时，有限步 PCG 在
$m=38$ 只需 231 次两网格循环，而把同一列方程求解到 $10^{-10}$ 后反而需要
3227 次。`理论v2.tex` 进一步说明，能量极小化控制的是列能量，固定光滑器下
的两网格质量则由另一个子空间目标控制；因此不能从 PCG 能量误差单调下降推出
两网格收敛单调改善。

v3.0.0 围绕这一结论完成三项算法升级。

1. 实现可在检查点间继续推进的全局 PCG 路径，避免每个候选从几何初值重新计算；
2. 设计双判据自适应停止器，以多随机谱探针和代表性右端项尾窗收缩率的较保守者
   估计候选质量，并结合构造、粗网格和预期多右端项求解成本选择历史最优候选；
3. 在 v2 预算式支撑扩展上设计 `frontier-gain-v3`，按全局收益分配连接前沿节点，
   同时加入列公平性下限、全局预算、暖启动和实测停滞冻结。

新增验证不再局限于一个随机场。代码加入交叉、弯曲、对角和平行通道四种拓扑，
并完成由三组网格、三种对比度、两个随机种子构成的 18 问题扫描。完整扫描中，
几何插值有 4 例在 12000 次循环内失败，固定 $m=40$ 和自适应方法均 18 例收敛；
自适应方法在所有 18 例中都不劣于几何插值，在 11 例中不劣于固定 $m=40$，
正步候选的平均密度约为 28.58%，而固定 $m=40$ 的平均密度约为 76.15%。

这些结果支持一个更准确的结论：自适应方法的主要价值是避免灾难性错误、识别
低循环窗口并控制密度，而不是保证一次求解的墙钟时间必然最短。当前谱与右端项
探针仍有可见开销，若只求解一个右端项，直接使用已知的固定候选可能更快；若参数
未知、问题族变化或有多个右端项，自动选择的价值更明显。

---

## 1. 研究问题与 v3.0 目标

在 $Omega=(0,1)^2$ 上考虑

$$
-\nabla\cdot(a(x)\nabla u)=f,
\qquad u|_{\partial\Omega}=0,
$$

其中 $a(x)>0$，对比度为

$$
\kappa_a=\frac{a_{\max}}{a_{\min}}.
$$

五点格式得到对称正定系统 $Au=b$。按 F 点和 C 点分块，记

$$
A=
\begin{bmatrix}
B&C\\ C^T&A_{CC}
\end{bmatrix},
\qquad
P(W)=\begin{bmatrix}W\\I\end{bmatrix}.
$$

v3.0 重点回答以下问题。

1. 能否沿有限 PCG 路径自动选出有利检查点，而不是预先指定 $m$？
2. 如何防止有限谱探针漏掉通道问题中的极慢模态？
3. 如何让支撑节点跨列竞争有限预算，并避免少数大残差列垄断预算？
4. 单个种子、单个通道和单个 $H/h$ 上的结论能否在小型但有说服力的矩阵中复现？
5. 新算法究竟改善了循环数、密度、构造时间还是鲁棒性，哪些方面仍未改善？

---

## 2. 理论依据及其对算法的约束

### 2.1 能量终点是唯一的，但不是两网格目标的同义词

能量泛函

$$
J(W)=\frac12\operatorname{tr}(P(W)^TAP(W))
$$

的唯一极小点为

$$
W_\star=-B^{-1}C,
$$

且

$$
J(W)-J(W_\star)
=\frac12\|W-W_\star\|_{B,F}^2.
$$

这给出了清晰、稳定的列方程终点，却没有把 Gauss--Seidel 光滑器写进目标函数。
固定插值 $P$ 的对称两网格误差传播算子记为 $E(P)$，真正控制最坏模态的是
$\rho(E(P))$。一般不存在

$$
J(W_1)<J(W_2)
\quad\Longrightarrow\quad
\rho(E(P(W_1)))<\rho(E(P(W_2))).
$$

因此，自适应停止不能只观察 PCG 方程残差或能量下降。

### 2.2 支撑扩大和 PCG 迭代是两个不同预算

对第 $j$ 个局部支撑 $S_j$，局部能量列是全局列在支撑空间中的 $B$-正交
投影。若 $S_j\subseteq T_j$，则支撑扩大带来的能量下降等于新旧局部解之差的
能量平方。另一方面，局部系统只做有限步 PCG 时，还存在支撑内代数误差。

`理论v2.tex` 给出的支撑外残差恒等式说明，设局部列延拓后的支撑外不平衡为
$g_j$，则空间截断误差可由相应 Schur 补逆加权量精确表示。代码使用的
对角缩放残差并非该精确量，但可作为成本较低的列排序指标。因此 v3 支撑算法遵循：

- 用支撑外指标决定“扩哪里”；
- 用局部方程残差决定“支撑内是否求够”；
- 用扩展后的实测下降决定“是否继续给该列预算”。

三者不能由一个普通相对残差替代。

### 2.3 有限 PCG 的正交尾解释非单调性，但不提供无条件最优步数

从同一几何初值出发，PCG 检查点可写为 $W_m$。PCG 的共轭方向给出精确的
有限正交尾结构，因而 $\|W_m-W_\star\|_{B,F}$ 随 $m$ 下降；但 $P(W_m)$
经过的粗空间可能先更接近固定光滑器需要的低维空间，随后又离开该区域。

理论中的相邻步矩阵判据、局部扰动判据和通道 Ritz 充分条件说明“有限步优于
精确终点”是可以严格发生的；它们同时指出，这不是所有 SPD 问题上的普遍定理。
所以 v3 算法必须：

1. 保存历史最优，而不是默认返回最后一步；
2. 允许 $m=0$，避免在几何插值已经很好时强行全局化；
3. 对每个实际问题测量两网格相关指标；
4. 明确探针是有限代价估计，不把它表述为严格上界。

### 2.4 为什么需要两种质量探针

对称两网格循环在能量内积下自伴。随机初值的能量范数幂迭代可估计
$\rho(E(P_m))$。但是，如果极慢通道模态在随机初值中的投影很小，短幂迭代会
严重低估谱半径。

v3 采用两个互补估计：

$$
\widehat\rho_m^{\rm power}
=\max_{s=1,\ldots,n_p}
\widehat\rho_m(s),
$$

以及代表性右端项短程求解尾窗的几何平均收缩率

$$
\widehat\rho_m^{\rm rhs}
=\max_{k\in\mathcal W}
\left(
\frac{\|r_k\|_2}{\|r_{k-w}\|_2}
\right)^{1/w}.
$$

最终使用

$$
\widehat\rho_m
=\max\{\widehat\rho_m^{\rm power},
        \widehat\rho_m^{\rm rhs}\}.
$$

第一项面向未指定右端项的慢模态，第二项面向当前工作负载并加快通道慢模态暴露。
二者仍不是数学上的严格谱半径上界，但比单个短随机探针稳健得多。

---

## 3. v3.0.0 算法设计

### 3.1 增量全局 PCG 路径

旧 experiment6 对每个 $m$ 都从几何初值重新运行 PCG。v3 的
`GlobalEnergyPcgPath` 对每列保存

$$
(x_m,r_m,p_m,r_m^TD^{-1}r_m),
$$

并在请求下一个检查点时从当前状态继续。其性质如下。

- 检查点序列必须递增；
- 只增加尚未完成的列迭代；
- 任意检查点可重新组装插值矩阵；
- 回归测试验证连续推进到 $m=5$ 与从同一初值直接运行固定 5 步的矩阵作用相对差
  小于 $10^{-12}$；
- 记录总列迭代数、最大相对方程残差、组装和求解时间。

这项改动只消除重复 Krylov 工作，不改变 PCG 数学轨迹。

### 3.2 双判据自适应 PCG

候选集合包括 $m=0$ 和递增检查点。每个检查点进行以下操作。

1. 增量推进 PCG，并组装 $P_m$；
2. 构造 Galerkin 粗矩阵和对称两网格循环；
3. 计算多个随机能量谱探针；
4. 若提供代表性右端项，再运行短程尾窗探针；
5. 由 $\widehat\rho_m$ 估计达到容差 $\varepsilon$ 的循环数

$$
\widehat N_m=
\min\left\{N_{\max},
\left\lceil\frac{\log\varepsilon}
{\log\widehat\rho_m}\right\rceil\right\};
$$

6. 对预期 $q$ 个右端项计算成本分数

$$
\widehat C_m=
T_{\rm path}(m)+T_{\rm coarse}(m)
+q\,\widehat N_m\,\widehat t_{\rm cycle}(m);
$$

7. 仅在分数达到最小相对改善时更新历史最优；连续若干检查点不改善后停止；
8. 返回历史最优 $P_m$，而不是最后组装的候选。

当前实现把探针耗时单独报告。上式比较的是“事先选用该候选”的预计成本；真正
在线搜索还会支付多个候选的探针和粗矩阵构造费用。因此报告同时保留
`selection_wall_ms`，不能把模型分数误当成实际选择耗时。

### 3.3 从失败版本到最终版本的修改

算法不是一次设计后直接保留。实际消融过程如下。

1. 第一版只用 3 个、每个 12 步的随机谱探针。在小网格上能选出 $m=16$，但这
   不足以证明鲁棒性；
2. 在 $64/16$、对比度 $10^6$ 的交叉通道上，10 步探针错误选择 $m=0$，而几何
   插值在 12000 次内失败；
3. 将谱探针增加到 30 步后，该例改选 $m=24$，实际 63 次收敛；
4. 在原始 $128/16$ 通道上，仅用 24 步谱探针仍错误选择 $m=0$，而几何插值在
   40000 次内失败；
5. 加入 40 步代表性右端项尾窗后，$m=0$ 的估计从约 $0.95$ 修正到 $0.9981$，
   最终选择 $m=36$，实际 268 次收敛。

这组反例说明：探针长度不是无关紧要的实现参数，过短探针会构成方法漏洞。

### 3.4 `frontier-gain-v3` 稀疏支撑扩展

新策略以两层局部能量基为初值。每一轮包括：

1. 计算每列支撑外缩放残差，并按平方和做列级 Dörfler 标记；
2. 只在当前支撑的强连接前沿产生候选，保持扩展与原支撑连通；
3. 对候选节点 $i$ 使用残差与连接强度的收益分数

$$
s_{ij}=\theta_{ij}
\left(
\frac{|(AP_j)_i|}{\sqrt{A_{ii}}}
+0.2\frac{|(AP_j)_{\rm source}|}{\sqrt{A_{\rm source,source}}}
+\epsilon
\right);
$$

4. 所有标记列的候选进入同一个全局优先序列；
5. 先给每个标记列少量公平性预算，再按收益继续分配，受每轮全局上限、每列每轮
   上限和每列总上限共同约束；
6. 只暖启动重求发生变化的列；
7. 比较扩展前后实际列指标。连续三轮收益低于阈值才冻结该列。

与旧策略相比，新策略的独立性体现在“跨列全局竞争 + 公平性下限 + 事后收益
停滞”三部分。它不是把 patch 从两层统一改成三层，也不是只调旧策略的节点个数。

第一轮默认值曾使用两次低收益就冻结。弯曲通道因此过早停止，循环数为 328，差于
旧策略的 287。最终把低收益阈值降至 0.002，并要求连续三轮后才冻结；弯曲通道
恢复为 287 次，同时密度低于旧策略。该失败版本未作为最终结果保留。

---

## 4. 系数场和实验矩阵

### 4.1 系数拓扑

v3 新增四类二值通道。

- `cross-channel`：一条近水平和一条近竖直通道；
- `meandering-channel`：贯穿区域的正弦弯曲通道；
- `diagonal-channels`：两条相交对角通道；
- `parallel-channels`：三条带轻微摆动的平行通道。

通道背景继续叠加由随机种子控制的块状高导夹杂。这样既能控制主要拓扑，又保留
随机背景扰动。

### 4.2 三层验证

验证分为三个层次。

1. **单元与回归测试**：矩阵合法性、插值注入、粗解精度、固定步预算、增量路径
   等价性、自适应检查点合法性、支撑预算和新拓扑对比度；
2. **核心消融**：原始 128×128 通道上的固定检查点和自适应选择；四种场上的旧、
   新支撑策略对比；
3. **鲁棒性矩阵**：
   - $(1/h,1/H)=(32,8),(64,8),(64,16)$；
   - $H/h=4$ 或 8；
   - 对比度 $10^2,10^4,10^6$；
   - 随机种子 1 和 17；
   - 四种通道按案例平衡轮换。

三组网格、三种对比度和两个种子形成 18 个问题，每个问题比较几何插值、固定
$m=40$ 和同一套自适应参数。不是对每个问题单独调参。

### 4.3 公平性和指标

- 所有方法使用同一离散矩阵、右端项和光滑器；
- Galerkin 粗矩阵均由生产代码构造；
- `Cycles` 达到相对残差 $10^{-6}$ 时停止；
- 鲁棒性扫描最多 12000 次，原始基准最多 40000 次；
- `P density %` 为非零数占完整 $n\times n_C$ 矩阵的百分比；
- `Setup ms` 包括相应插值和粗网格构造；自适应行还包括在线选择开销；
- 墙钟时间只做同机横向比较，且当前仅单次计时，不给出统计置信区间。

---

## 5. 数值结果

### 5.1 原始通道：自适应器识别低循环窗口

参数为 $h=1/128$、$H=1/16$、$H/h=8$、对比度 $10^4$、种子 1。

| 方法 | $m$ | P 密度 | 循环数 | Setup ms | Total ms |
|---|---:|---:|---:|---:|---:|
| 固定 PCG | 16 | 9.1300% | 4043 | 434.632 | 2727.897 |
| 固定 PCG | 24 | 14.4398% | 926 | 526.303 | 1187.878 |
| 固定 PCG | 32 | 20.4547% | 347 | 588.972 | 890.523 |
| 固定 PCG | 40 | 26.9969% | 234 | 663.749 | 920.797 |
| 固定 PCG | 48 | 33.8960% | 256 | 795.164 | 1254.479 |
| 固定 PCG | 64 | 48.1166% | 366 | 1022.566 | 1740.647 |
| 自适应 PCG | **36** | **23.5999%** | **268** | 3863.259 | 4151.908 |

旧密集扫描的循环数 oracle 是 $m=38$ 的 231 次。v3 自适应器的候选间隔为 4，
且优化预计多右端项成本而非单独循环数，因此选择 $m=36$，没有声称找到精确
oracle；但它成功进入 $m=36$--40 的低循环窗口，并避开两个灾难性选择：
$m=0$ 在 40000 次内失败，精确能量终点需要 3227 次。

自适应在线搜索的 Setup 明显高于一个已知固定候选。这是当前版本的主要成本：它
为多个候选构造粗矩阵并运行质量探针。与“先验知道 $m=40$”相比，自适应不会在
单右端项时间上获胜；与未知参数下做完整密集扫描相比，它避免了重启 PCG，并用
历史最优和耐心窗口提前终止。

### 5.2 双判据确实修复了慢模态漏检

原始通道的部分决策轨迹为：

| $m$ | 谱探针 | RHS 尾窗 | 最终 $\widehat\rho$ | 预计循环数 |
|---:|---:|---:|---:|---:|
| 0 | 0.961737 | 0.998076 | 0.998076 | 7173 |
| 16 | 0.972002 | 0.991221 | 0.991221 | 1567 |
| 24 | 0.957437 | 0.966438 | 0.966438 | 405 |
| 28 | 0.920445 | 0.927620 | 0.927620 | 184 |
| 36 | 0.926178 | 0.924854 | 0.926178 | 181 |

如果只保留谱探针，$m=0$ 的质量会被严重高估。代表性右端项尾窗把慢模态暴露
出来，是本轮算法迭代中最重要的修正。

### 5.3 新支撑策略：相同循环数下更低密度

以下为 $h=1/32$、$H=1/8$、对比度 $10^4$、种子 17 的最终消融。

| 场 | 方法 | P 密度 | 循环数 | Setup ms |
|---|---|---:|---:|---:|
| checker | v2 adaptive-budget | 24.4325% | 52 | 22.050 |
| checker | v3 frontier-gain | **24.0311%** | 52 | 22.831 |
| cross | v2 adaptive-budget | 25.5113% | 82 | 26.539 |
| cross | v3 frontier-gain | **24.7807%** | 82 | **25.153** |
| meandering | v2 adaptive-budget | 25.1078% | 287 | **19.862** |
| meandering | v3 frontier-gain | **24.3518%** | 287 | 27.561 |
| diagonal | v2 adaptive-budget | 25.8617% | 52 | 26.551 |
| diagonal | v3 frontier-gain | **25.3860%** | 52 | 29.442 |

四个场中，新策略均保持旧策略循环数并降低密度，密度下降约 0.40--0.76 个百分点。
构造时间并非一致改善：交叉通道略快，另外三例相近或更慢，弯曲通道的多轮暖启动
开销尤其明显。因此准确表述是“稀疏性 Pareto 改进”，不是“所有指标全面领先”。

### 5.4 完整 18 问题鲁棒性结果

汇总如下。

- 几何插值：14/18 收敛，4/18 在 12000 次内失败；
- 固定 $m=40$：18/18 收敛；
- 自适应：18/18 收敛；
- 自适应循环数在 18/18 中不劣于几何插值；
- 自适应循环数在 11/18 中不劣于固定 $m=40$；
- 自适应选择 $m=0$ 共 7 例，说明它没有把“必须做 PCG”写成先验；
- 其余 11 例选择 $m=12,16,20$ 或 24，正步候选平均密度约 28.58%；
- 固定 $m=40$ 平均密度约 76.15%。

代表性困难案例：

| $1/h$ | $1/H$ | 对比度 | 拓扑 | 几何 | 固定 $m=40$ | 自适应 |
|---:|---:|---:|---|---:|---:|---:|
| 64 | 16 | $10^4$ | diagonal | failed@12000 | 51 | $m=16$, 54 |
| 64 | 16 | $10^4$ | parallel | failed@12000 | 51 | $m=12$, 144 |
| 64 | 16 | $10^6$ | cross | failed@12000 | 123 | $m=24$, **63** |
| 64 | 16 | $10^6$ | meandering | failed@12000 | 51 | $m=24$, 68 |

自适应器在所有几何失败例中都恢复收敛，但没有逐例达到固定 $m=40$ 的循环数。
它的优势是较低密度和避免失败；在 parallel $10^4$ 例中，$m=12$ 的 144 次明显
差于固定候选的 51 次，说明当前成本模型会用更多循环换取低密度和低单循环成本。

### 5.5 软件验证

本轮完成以下检查。

- `unit_core` 全部通过；
- `regression_v3` 全部通过；
- 9 个实验程序均以 `-std=c++17 -O2 -Wall -Wextra -Wpedantic` 编译通过；
- 增量 PCG 与固定步参考路径通过 $10^{-12}$ 级矩阵作用比较；
- 新支撑不包含 C 点，不超过每列预算；
- 新通道场保持请求的系数对比度；
- v2 experiment6 的候选继续保留独立长双精度稠密 Cholesky 粗解交叉检查结果。

本地环境缺少 CMake，因此本轮使用 `g++` 直接完成编译和运行；项目仍提供标准
CMake 配置和一键验证脚本，交付前已检查所有目标的源码可独立编译。

---

## 6. 研究价值和创新性判断

### 6.1 已经成立的贡献

1. **现象层面：** 在统一、可复现的离散和粗解交叉检查下，确认有限 PCG 插值可
   严格优于精确能量终点，且存在明显有利窗口和后续退化；
2. **理论层面：** 把能量投影、PCG 正交尾、两网格子空间目标和通道 Ritz 条件
   放在同一框架中，明确无条件恒等式、结构性充分条件和反例边界；
3. **算法层面：** 自适应停止直接使用两网格质量探针，不以 PCG 构造残差冒充求解器
   质量；双判据由实际失败案例驱动，而非事后添加装饰性指标；
4. **稀疏化层面：** 支撑扩展由逐列固定预算改成跨列全局收益分配，并用事后收益
   停滞控制无效扩展；
5. **验证层面：** 从单网格、单种子扩展到 18 问题矩阵，并保留失败案例和负面结果。

### 6.2 不应夸大的部分

- 有限步优于精确终点不是所有 SPD 系统上的普遍规律；
- 当前自适应分数不是谱半径的严格上界，也没有全局最优步数保证；
- 18 问题矩阵仍是小型验证，不等同于大规模统计研究；
- `frontier-gain` 主要改善密度，不保证构造时间更低；
- 与 BoomerAMG、PETSc GAMG、ML/MueLu 等成熟基线尚未做同机比较；
- 当前仍是两网格而非完整多层 AMG。

以本科生科研成果衡量，v3.0 已超过一般“实现已知算法并做几个算例”的层次：它有
反常数值现象、对应理论解释、失败驱动的自适应算法修改和多因素验证。若作为期刊论文，
仍建议补充外部基线、多次计时统计和至少一种非结构或更大规模问题。

---

## 7. 当前局限与风险

### 7.1 理论局限

理论中的通道 Ritz 条件是充分条件，需要验证主模态可见性、方向对齐和谱分离，不能
从“系数场看起来像通道”直接推出。空间局部化速率依赖缩放系统条件数，当前没有得到
对比度无关的统一指数常数。

### 7.2 算法局限

自适应选择需要为多个候选构造粗矩阵并运行探针。原始通道中，自适应 Setup 为
3863 ms，明显高于固定 $m=40$ 的 664 ms。预期右端项数越少，这一开销越难摊薄。
未来应研究分级筛选、候选粗矩阵增量更新和置信区间早停。

代表性右端项探针提高了当前工作负载的安全性，却可能漏掉与该右端项近乎正交的慢
模态；随机谱探针用于缓解这一问题，但有限步下仍非严格保证。

### 7.3 实验局限

当前计时只运行一次，受缓存、线程调度和系统负载影响。18 问题的拓扑采用平衡轮换，
不是所有网格、对比度、种子和拓扑的完整笛卡尔积。结论仍限于固定 GS 光滑器、固定
几何粗点和二维结构网格。

---

## 8. 下一阶段研究计划

### 8.1 自适应器降本

1. 使用短探针筛选全部候选，只对历史最优和竞争候选运行长探针；
2. 用置信下界和上界控制是否需要追加探针；
3. 缓存或增量更新 $P^TAP$，减少相邻检查点的重复粗网格构造；
4. 将预计右端项数 $q$ 做成命令行参数，并报告盈亏平衡右端项数；
5. 对无代表性右端项和有代表性右端项分别给出默认策略。

### 8.2 支撑策略深化

1. 比较“单位新增节点的残差下降”“单位新增非零的谱改善”和“单位时间改善”三种
   收益定义；
2. 在同一密度预算下比较旧、新支撑，而不只比较默认参数；
3. 研究一次选择多跳连通前沿，减少反复暖启动轮数；
4. 记录列级扩展轨迹，检验扩展节点是否沿高导通道传播；
5. 将扩支撑与有限局部 PCG 步数放在一个联合预算器中。

### 8.3 论文级验证

建议增加：

- 每组至少 5 次独立计时，报告中位数和四分位距；
- 更多随机种子，并对循环数、密度和总时间做配对统计；
- BoomerAMG、PETSc GAMG 或 MueLu 的同机基线；
- 更大二维网格、至少一个三维问题；
- 非结构网格或随机粗点；
- Jacobi、Chebyshev、ILU 等不同光滑器，以验证“最优窗口随光滑器变化”的理论预言；
- 从两网格推广到多层 V-cycle，并检查层间自适应是否稳定。

---

## 9. 复现实验

### 9.1 构建与测试

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=ON
cmake --build build --parallel 4
ctest --test-dir build --output-on-failure
```

### 9.2 一键验证

```bash
./scripts/run_validation.sh quick
./scripts/run_validation.sh full
```

### 9.3 单独运行

```bash
./build/experiment7 --fine=128 --coarse=16 --threads=4 \
  --contrast=1e4 --max-cycles=40000

./build/experiment8 --fine=32 --coarse=8 --threads=4 \
  --contrast=1e4 --max-cycles=12000

./build/experiment9 --threads=4
```

结果位于：

- `results/experiment7.csv` 与 `experiment7_history.csv`；
- `results/experiment8.csv` 与 `experiment8_support.csv`；
- `results/experiment9.csv`；
- 对应 `.txt` 为可直接阅读的报告。

---

## 10. 主要结论

1. 全局能量误差单调下降不推出固定光滑器两网格质量单调改善；原始通道的有限步
   最优窗口在 v3 中再次复现；
2. 自适应停止必须测量两网格相关量。短随机探针会漏掉通道极慢模态，代表性右端项
   尾窗是必要的互补判据；
3. 最终自适应器在原始通道选中 $m=36$ 并达到 268 次循环，成功避开几何失败和
   精确能量终点的 3227 次退化；
4. 18 问题扫描中，自适应方法全部收敛，并在全部案例中不劣于几何插值；
5. `frontier-gain-v3` 在四类场上以更低密度保持旧支撑策略的循环数，但构造时间
   仍有场依赖性；
6. 当前最可信的创新点是“理论解释 + 反例驱动的双判据选择 + 跨列收益预算”，而
   不是无条件声称某个固定参数优于成熟 AMG；
7. v3.0 已形成完整、可复现的本科科研成果。要达到更有竞争力的期刊完成度，下一步
   应优先降低选择开销、加入成熟外部基线并扩展到多层和更大问题。
