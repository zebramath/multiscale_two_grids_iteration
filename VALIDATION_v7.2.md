# multiscale_two_grids_iteration v7.2 验证记录

验证日期：2026-09-01  
数值源码版本：7.2.0  
理论与研究方案版本：7.2

## 1. 变更边界

v7.2 保持 v7.1 的算法、理论结论和实验参数不变，完成两项交付要求：

1. 删除 `code/tests` 目录中的 `unit_core.cpp`、`regression_core.cpp`，同步删除 CMake 与
   `run_validation.sh` 中的测试构建入口；最终源码只保留七个正式实验及其共享实现；
2. 先移走全部旧 TXT，再由 7.2.0 最终源码以 full 模式、4 线程完整重跑实验 1--7，
   因而交付结果不继承或混用任何早期版本数据。

核心数值实现相对 v7.1 未改动；版本号、复现说明、研究方案计时表和结果索引已同步。

## 2. 验证环境

- Linux 6.18.35，x86_64；
- GCC 13.3.0，C++17；
- CMake 当前环境不可用，使用脚本提供的严格直接构建路径；
- XeTeX 3.141592653-2.6-0.999995（TeX Live 2023）；当前环境缺少 `ctexart.cls`。

## 3. 构建与动态检查

| 检查 | 配置 | 结果 |
|---|---|---|
| 七个正式实验严格构建 | `-O3 -DNDEBUG -Wall -Wextra -Wpedantic -Werror`，并启用 GCC 扩展告警 | 通过，无告警 |
| 完整实验流程 | `TGI_THREADS=4 ./scripts/run_validation.sh full` | 实验 1--7 全部完成 |
| 脚本语法 | `sh -n scripts/run_validation.sh` | 通过 |
| ASan + UBSan 实验级检查 | 实验 5 quick，两案例、三种停止策略 | 通过 |
| 随机理论恒等式 | 固定 seed 的 50 组随机 SPD 小系统 | 通过，最大绝对误差 $7.38\times10^{-15}$ |

Sanitizer 检查直接运行正式实验入口，不向交付包恢复独立测试源码。LeakSanitizer 因当前
容器路径不可靠而关闭；AddressSanitizer 与 UndefinedBehaviorSanitizer 均保持开启。

## 4. 七组完整结果

| 实验 | 完整运行内容 | 核心结果 |
|---|---|---|
| 1 | 13 个尺寸、对比度、拓扑问题，含两组 256/16 | adaptive/reference 均 13/13 收敛，循环和 4459/28972；geometric 为 4 converged、9 slow-limit |
| 2 | 三条有限 PCG 路径，每条含起点、8 个检查点和 reference | 能量超额单调下降，循环数在有限步达到较小值后回升 |
| 3 | 7 个设计问题与 3 个留出验证问题的 step--2 oracle | 设计组平均/最大 gap 8.93%/29.65%，验证组 27.34%/54.20% |
| 4 | 5 seed、6 RHS、中心问题预热后 5 次计时 | seed 循环和 327/866，RHS 循环和 469/1507；total 中位数 594.021/8727.991 ms |
| 5 | 6 个问题、3 种停止策略 | adaptive/fixed-step/fixed-residual 收敛 6/6、5/6、5/6；循环和 1147/21098/25658 |
| 6 | 32/4、64/8、128/16 固定物理几何加密 | 共享节点失配 0；adaptive 循环数 122/119/145 |
| 7 | 64/16/8、128/16/8 三层 V-cycle | adaptive 为 101/237，reference 为 321/770；二者均 2/2 收敛 |

实验 4 的计时依赖当前机器负载，研究方案已使用本轮 Q1、中位数和 Q3 全量替换旧值；
循环数、收敛状态、路径能量、oracle、消融、共享节点和复杂度等确定性指标均与理论叙述
及结果索引一致。

## 5. 理论稿与文本检查

- `theory.tex` 有 87 个唯一标签、92 次交叉引用，无缺失或重复标签；
- 17 次引用对应 13 个唯一 `bibitem`，无缺失、重复或未引用条目；
- 因环境缺少 `ctexart`，在临时副本中仅替换文档类为标准 `article`，XeLaTeX 连续两遍
  编译生成 11 页 PDF；无未定义控制序列、公式环境错误、悬空引用或悬空文献；
- 50 组随机核验的最大误差分别为：能量差 $3.55\times10^{-15}$、粗矩阵
  $5.20\times10^{-15}$、图投影 $5.01\times10^{-15}$、两网格相似变换
  $7.38\times10^{-15}$、多层分解 $2.13\times10^{-15}$；
- README、研究方案、理论标题、代码常量、七份结果和复现脚本的版本均为 v7.2/7.2.0。

## 6. 已知边界

- 按交付要求，最终包不含独立单元/回归测试文件；安全检查由 v7.1 已完成的测试记录、
  v7.2 的严格全实验运行及实验级 sanitizer 共同覆盖；
- 当前环境不能对 `ctexart` 中文字体与分页作最终视觉验收，投稿前仍应在完整 TeX Live
  中文环境中编译一次；
- 三层实验只支持机制可递归性的有限案例定位，不外推任意层数的网格无关或线性复杂度。

## 7. 压缩包复验

最终 ZIP 已在独立临时目录解包；目录清洁、七份 7.2.0 full 结果、版本一致性和脚本
语法均通过，并从解包副本重新严格构建七个正式实验及完成 quick 端到端运行。压缩包
包含 32 个必要文件，不包含 build、测试目录、缓存、临时日志或 Git 元数据。
