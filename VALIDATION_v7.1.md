# multiscale_two_grids_iteration v7.1 验证记录

验证日期：2026-09-01  
数值源码版本：7.1.0  
理论与研究方案版本：7.1

## 1. 审校范围与历史取舍

本次以已验证的 v6.4 为主线，逐项检查代码、七组实验、正式结果、研究方案和理论稿；
同时比较工作区内 v5.10、v6.1--v6.4 交付及本地 Git 镜像中的 v3.5--v5.9 历史。

历史版本中重新保留的唯一实质内容是多层层次的插值复杂度

$$
C_P=\frac{\sum_{\ell=0}^{L-1}\operatorname{nnz}(P_\ell)}
          {\operatorname{nnz}(A_0)},
$$

它与现有算子复杂度 $C_A$ 一起解释存储代价。v4.0 曾出现的多策略多层框架、逐层 pilot
选择和额外预条件器接口没有恢复：当前紧凑 V-cycle 已覆盖次要递归实验，恢复旧框架只会
增加未经当前主线使用的分支。v6.1 中已被更强 Pareto 定理取代的行列式、单方向和锥条件
也没有回填。

## 2. v7.1 实质修改

### 代码

- 为向量运算、稀疏矩阵乘法、残量和 Cholesky 求解增加维数检查，避免错误输入越界；
- CSR 构造现在验证维数、行指针、列下标、逐行严格排序、唯一性和数值有限性；
- Cholesky 置换必须是双射，未分解求解和非方阵分解会明确失败；
- 两网格、全局 PCG 路径和全局能量参考增加层次维数及停止参数校验；PCG 失去正曲率时
  不再静默返回部分路径；
- 报告表格验证表头、列宽和每行列数，消除错误 schema 的越界入口；
- 将两网格和 V-cycle 共用的结果类型统一命名为 `StationaryIterationResult` 和
  `StationaryIterationStatus`；
- `MultilevelVCycle` 增加 $C_P$，实验 7 同步报告 $C_A$ 与 $C_P$；
- 单元与回归测试加入版本一致性、非法 CSR、非法置换、未分解求解和错误报告 schema。

### 理论与研究方案

- 修正多层稳定界及逐层展开中三处误写的 `^{,2}`，统一为范数平方；
- 明确“所有同维粗空间中的最优”是固定 C 点图空间可行族的绝对下界；只有与 F 子空间
  横截的最优奇异子空间才可由当前单位注入表示，因而不再暗示该下界必可达到；
- 随机 SPD 小系统独立核验能量差、Galerkin 粗矩阵、图空间投影、两网格相似变换和
  递归 V-cycle 恒等式；
- 研究方案加入 $C_P$ 定义与结果，README 和正文删除版本演化叙述及重复说明，只保留
  当前研究主线、适用范围和复现入口。

## 3. 验证环境

- Linux 6.18.35，x86_64；
- GCC 13.3.0，C++17；
- CMake 当前不可用，验证脚本使用等价的严格直接构建路径；
- XeTeX 3.141592653-2.6-0.999995（TeX Live 2023）；当前环境缺少 `ctexart.cls`。

## 4. 构建、测试与动态检查

| 检查 | 配置 | 结果 |
|---|---|---|
| 全部七个实验及两项测试 | `-O3 -DNDEBUG -Wall -Wextra -Wpedantic -Werror`，并启用 GCC 扩展告警 | 通过，无告警 |
| `unit_core` | 版本、插值、粗解、两层退化和三层收敛 | 通过 |
| `regression_core` | 路径延续、残量停止、固定物理场和非法输入 | 通过 |
| quick 端到端 | 实验 1 的 32/8 cross、128/16 cross/ring | 通过，循环数与既有正式结果一致 |
| `run_validation.sh` | `sh -n` | 通过 |
| ASan + UBSan | 两项测试，`-O1 -g`，遇错立即停止 | 通过 |
| 随机理论恒等式 | 固定 seed 的 50 组随机 SPD 小系统 | 通过，最大绝对误差 $7.38\times10^{-15}$ |

动态检查关闭 LeakSanitizer，因为当前容器不提供可靠的 leak 路径；AddressSanitizer 与
UndefinedBehaviorSanitizer 均保持开启。

## 5. 数值结果管理与实验 7 复验

实验 1--4 的正式 TXT 来自 v6.2.0 完整运行，实验 5--6 来自 v6.3.0 完整运行；算法与
输入未变，因此不作无必要的全量重复计算。v7.1 对实验 1 quick 子集重新运行，三种方法
的循环数和状态与正式结果逐项一致。实验 7 因新增 $C_P$ 由 v7.1.0 源码以完整模式、
4 线程重新运行：

| 层次 | 方法 | $C_A$ | $C_P$ | 精确两网格 | 三层 V-cycle | 状态 |
|---|---|---:|---:|---:|---:|---|
| 64/16/8 | adaptive | 3.0786 | 12.5962 | 65 | 101 | converged |
| 64/16/8 | global-reference | 3.7064 | 42.4913 | 321 | 321 | converged |
| 64/16/8 | geometric | 1.1128 | 0.5852 | 24950 | 30000 | slow-limit |
| 128/16/8 | adaptive | 1.5379 | 13.4528 | 237 | 237 | converged |
| 128/16/8 | global-reference | 1.6617 | 44.1727 | 768 | 770 | converged |
| 128/16/8 | geometric | 1.0276 | 0.6372 | 30000 | 30000 | slow-limit |

$C_P$ 显示有限步 adaptive 明显低于全局参考，同时其 V-cycle 循环数保持优势。该实验
仍只作为三层递归可行性证据，不外推任意层数下的网格无关收敛或最优线性复杂度。

## 6. 理论稿与文本一致性

- `theory.tex` 有 87 个唯一标签、92 次交叉引用，无缺失或重复标签；
- 17 次引用全部对应 13 个唯一 `bibitem`，无缺失、重复或未引用条目；
- 因环境缺少 `ctexart`，在临时副本中仅替换为标准 `article` 后用 XeLaTeX 连续两遍
  编译，生成 11 页 PDF；无未定义控制序列、公式环境错误、悬空引用或悬空文献；
- 50 组随机代数核验的最大误差分别为：能量差 $3.55\times10^{-15}$、粗矩阵
  $5.20\times10^{-15}$、图投影 $5.01\times10^{-15}$、两网格相似变换
  $7.38\times10^{-15}$、多层分解 $2.13\times10^{-15}$；
- 版本号、实验参数、容差、循环上限、结果表和复现命令在 README、研究方案、代码与
  结果索引之间一致。

## 7. 已知边界

- 本次没有重新运行耗时较大的实验 1--6 全集；保留的 TXT 是可追溯的既有完整运行，
  quick 回归覆盖代码路径与代表问题，实验 7 则由最终版本重新完整运行；
- 当前环境不能对 `ctexart` 中文排版作最终视觉验收，但公式、标签、引用和环境已通过
  两遍替代类编译；正式投稿前仍应在完整 TeX Live 中文环境中编译一次；
- 三层实验报告确定性循环数和稀疏复杂度，不使用单次墙钟时间，也不声称一般多层最优性。

## 8. 压缩包复验

最终 ZIP 已在独立临时目录解包；目录清洁、7 份结果、7.1.0 版本常量和脚本语法检查
均通过，并从解包副本再次完成严格构建、两项测试及 quick 端到端实验。压缩包含 34 个
必要文件，不包含构建目录、临时日志、缓存或 Git 元数据。
