# multiscale_two_grids_iteration v7.4 验证记录

验证日期：2026-09-01  
数值源码版本：7.4.0  
理论与研究报告版本：v7.4

## 1. 本轮范围

v7.4 是一次以学术表述为重点的完整审校，不重新运行全部正式实验，也不改变数值算法、
矩阵、参数或既有数据。主要修改如下：

1. 将 `research_report.md` 从过程性研究方案整理为可直接用于汇报或论文初稿的研究报告，
   补齐摘要、贡献、指标定义、结果解释、限制与结论；
2. 统一 `adaptive`、`global-reference`、sampled oracle、插值算子、主角和收敛状态等术语；
3. 明确 `global-reference` 是逐列相对欧氏残量 $10^{-10}$ 定义的数值参考，不与精确
   能量极小点混同；
4. 明确定义全程有效收敛因子、末端因子、插值密度、计时分项、gap 与累计循环数口径；
5. 将停止消融、固定物理场加密和三层 V-cycle 的结论限制在实际测试集合，不外推为
   参数最优性、near-oracle 性质、网格无关收敛或渐近复杂度；
6. 修正理论中粗化比与奇异值重数共用符号的问题，并收紧摘要、结论和证明衔接；
7. 统一 README、代码说明和七个实验未来运行时生成的英文报告文案，删除面向开发过程的
   投稿措辞和主观评价。

## 2. 方法定位与理论边界

adaptive 始终定位为

$$
\boxed{\text{theory-informed lightweight empirical selector}}.
$$

标准 $\kappa_h=O(h^{-2})$ 条件数上界只给出达到固定能量误差比例所需的
$m=O(h^{-1})$ 充分尺度。$1/8,1/4,1/3,1/2$ 和 $10^3,10^5$ 是经验参数；项目不宣称
它们最优，也不把 adaptive 描述为 near-oracle。固定有限维 PCG 的无限步渐近相干推论
不在理论稿、研究报告或项目说明中。

## 3. 正式结果继承

`code/results` 中七份 TXT 均由 v7.2.0 数值源码以 full 模式、4 线程生成。v7.3 和 v7.4
未改变数值算法或实验参数；本轮未运行 full，七份 TXT 与 v7.3 逐文件字节一致。结果内
保留的 `Version 7.2.0` 是运行来源记录，不是遗漏。

## 4. 本轮验证

- `./scripts/run_validation.sh quick` 通过：在当前环境无 CMake 时使用严格直接构建路径，
  七个实验入口均以 C++17、`-Wall -Wextra -Wpedantic -Werror` 及可用的扩展告警成功构建；
- quick 模式只运行实验 1 的 32/8 cross、128/16 cross 和 128/16 ring 三个代表案例，
  输出版本为 7.4.0，循环数和状态与正式结果对应行一致；
- `bash -n code/scripts/run_validation.sh` 通过；源码与 CMake 项目版本均为 7.4.0；
- `theory.tex` 在临时标准 `article` 文档类替身下由 XeLaTeX 连续编译两遍，通过公式、
  环境、标签、交叉引用和文献引用检查，无未定义引用或重复标签。当前容器缺少
  `ctexart` 及中文字体，因此该替身只用于结构验证；正式中文排版仍需完整 TeX Live
  中文组件；
- 正文检索确认不存在固定有限维 PCG 的无限步渐近推论或旧版 near-oracle 定位；
- 七份正式 TXT 的 SHA-256 与 v7.3 对应文件完全一致；quick 输出写入 `code/build`，不覆盖
  `code/results`。

## 5. 结论解释限制

- 理论中的谱半径结论依赖固定粗点、精确 Galerkin 粗解和指定的对称磨光结构；
- $m=O(h^{-1})$ 是固定能量衰减的充分尺度，不是两网格最优步数定理；
- sampled oracle 只在预设 step-2 有限候选窗内定义，且只用于离线描述性评价；
- 固定物理场实验只有三层嵌套网格，三层 V-cycle 实验只有两个层次结构；二者均不承担
  无限细化或任意层数下的渐近结论；
- 墙钟时间仅用于当前记录环境中的实现级比较，循环数和收敛因子是主要的可移植指标。
