# 有限能量极小化路径的两网格机制

## 1. 研究对象与贡献

固定规则粗点和粗点单位注入，从双线性几何插值出发，用 Jacobi--PCG 逐列逼近

\[
W^*=-A_{FF}^{-1}A_{FC}.
\]

在当前无额外稀疏约束的坐标下，$W^*$ 同时是插值能量唯一极小点和 classical ideal interpolation。研究重点是有限 PCG 路径、真实两网格谱目标、粗空间主角、局部奇异值方向与图传播尺度之间的精确关系。

能量极小插值、Krylov/CG 构造、有限迭代 setup 和 optimal interpolation 构成已有基础。本文建立有限路径的机制分析闭环，并以对应的谱与局部方向实验进行验证。

## 2. 理论链条

令 $A\succ0$ 按 F/C 分块，$P(W)=[W;I]$，$S=A_{CC}-A_{CF}A_{FF}^{-1}A_{FC}$，并令

\[
Z=A_{FF}^{1/2}(W-W^*)S^{-1/2}.
\]

基本恒等式为：

1. $J(W)-J(W^*)=\tfrac12\|W-W^*\|_{A_{FF},F}^2$，PCG 非零步使能量严格下降。
2. 当前粗空间相对 endpoint 粗空间的 $A$-principal angles 满足 $\tan\theta_k=\sigma_k(Z)$。
3. 对前向 GS 能量坐标算子 $T$，固定对称两网格谱半径精确为
   \[
   \rho_{TG}(Z)=\|(I+ZZ^T)^{-1/2}(T_F-ZT_*)\|_2^2.
   \]

第三式包含有符号磨光子耦合项，刻画能量目标与两网格谱目标的不同几何。

若 $T_F$ 的最大奇异值简单，gap 为 $\delta>0$，取相应 $u_F,v$ 和 $y=T_*v$，则 endpoint 附近

\[
\sqrt{\rho_{TG}(Z)}=\sigma_1(T_F)-u_F^TZy+R(Z),\qquad R(Z)=O(\|Z\|^2).
\]

正文给出显式有效域和余项常数。$y\ne0$ 时，方向 $H=u_Fy^T$ 在充分小的正 $t$ 上满足 $\rho_{TG}(tH)<\rho_{TG}(0)$，由此得到 energy/ideal endpoint 的局部可改进性。相邻 PCG 点的 $u_F^T(Z_m-Z_{m+1})y$ 与显式余项界给出双向变化判据。

PCG 的 Krylov 表示给出有限传播：第 $m$ 步校正支撑包含于初始残差在 $A_{FF}$ 图上的半径 $m-1$ 邻域。因此 $m=O(h^{-1})$ 在 fixed-$H$ 情形中对应固定物理传播距离。

二维规则粗化的 Jacobi--PCG 条件数满足

\[
\kappa\lesssim \chi\Lambda_{h,H}^{-1},\qquad
\Lambda_{h,H}=h^2+\frac1{q^2\log(2q)}.
\]

常系数容量试验函数达到 $\kappa=\Theta(\left(\Lambda_{h,H}\right)^{-1})$；相邻高权 F--F 边与低权外连边构造达到 $\kappa\gtrsim\chi$。两个构造分别刻画网格容量尺度和对比度幂次的尖锐性。

## 3. 数值设置

正式实验采用二维五点扩散、规则 C/F 点、单位注入、$A_c=P^TAP$、一次前向/后向 GS 和精确粗解。线性求解相对残量为 $10^{-6}$。有限路径点取 $m=\operatorname{round}(c/h)$。

真实 $\rho_{TG}$ 由冷启动矩阵自由 Lanczos/Ritz 计算。完整路径使用 200 步并报告 100/200 步差；选定点的 5,000 步幂迭代与 Lanczos 输出一致。RHS 有效因子作为应用指标单独记录。

## 4. 主比较

| 路径点 | 收敛数 | 收敛案例累计 cycles | 平均 P 密度 | setup 累计 |
|---|---:|---:|---:|---:|
| $m=(1/h)/4$ | 12/13 | 6,133 | 22.0446% | 8.225 s |
| $m=(1/h)/3$ | 13/13 | 5,975 | 31.0828% | 10.754 s |
| $m=(1/h)/2$ | 13/13 | 6,122 | 49.9512% | 15.992 s |
| endpoint | 13/13 | 28,972 | 97.6438% | 62.700 s |

$\chi=10^6$ 案例中的 $m=(1/h)/4$ 达到 slow-limit；$1/3$、$1/2$ 与 endpoint 均收敛。本测试类中 $1/3$ 给出稳健且轻量的默认路径点。

中心 $128/16,\chi=10^4$ cross-channel 中，$m=43$ 为 242 cycles、$\rho_{eff}=0.944407$、P 密度 29.4678%；endpoint 为 3,227 cycles、0.995727、98.2099%。

## 5. 真实谱路径

| 拓扑 | 最小 $\rho_{TG}$ / $m$ | 最小 $\rho_{eff}$ / $m$ | endpoint $\rho_{TG}$ | 最大 Ritz 阶段差 |
|---|---:|---:|---:|---:|
| cross-channel | 0.949431677 / 36 | 0.941763675 / 38 | 0.996163213 | $5.90\times10^{-5}$ |
| winding-ring | 0.949386203 / 36 | 0.941012129 / 53 | 0.984367546 | $4.41\times10^{-6}$ |

两条 $m=1,\ldots,128$ 路径均呈现有限点之后的谱退化。真实谱最优位置与指定 RHS 最优位置分别反映算子目标和应用观测量。

## 6. 双尺度与有限传播

fixed-$H$ 使用 $1/H=8$，fixed-$q$ 使用 $q=8$；两组均测试 $1/h=32,64,128,256$。每个网格比较 $m=q$ 与 $m=(1/h)/4$。

全部 16 行的 `bound_violations=0`，实际最大传播距离均为 $m-1$。fixed-$q$ 时，$m=q$ 的 $mh$ 从 0.25 降至 0.03125，校正支撑比例从 84.45% 降至 1.255%；$m=(1/h)/4$ 始终保持 $mh=0.25$。该结果直接连接 Krylov 步数与物理覆盖尺度。

## 7. gap 与一阶方向诊断

在 $16/4,\chi=10^4$ cross-channel 上：

- $\sigma_1(T_F)=0.7896795471$，$\sigma_2(T_F)=0.7829979688$；
- gap $\delta=6.6815783\times10^{-3}$，相对 gap 为 0.8461%；
- endpoint $W$ 与直接 $-B^{-1}C$ 的 Frobenius 差为 $3.44\times10^{-15}$；
- 图公式与直接两网格特征值最大差为 $4.07\times10^{-14}$；
- 显式局部 gap 条件从 $m=24$ 起成立；局部有信息步一阶符号为 28/28 命中；
- 全路径有信息步一阶符号为 42/52 命中。

这些量直接对应 simple gap、$\arctan\sigma_k(Z_m)$ 主角路径和一阶方向项。

## 8. 稳健性、基线与三层试验

固定 $m=(1/h)/3$ 在五个系数 seed 上累计 327 cycles，endpoint 为 866；六 RHS 上累计 469 对 1,507。localized-left 案例中 endpoint 更快，展示 RHS 相关差异。

中心问题五次交替计时：

| 方法 | setup mean | solve mean | total mean | cycles |
|---|---:|---:|---:|---:|
| finite-1/3 ($m=43$) | 546 ms | 325 ms | 872 ms | 242 |
| endpoint | 2,819 ms | 9,078 ms | 11,896 ms | 3,227 |

几何插值基线需要 85,524 cycles，endpoint 需要 3,227，展示能量极小化相对几何插值的显著收益以及有限路径点的进一步改进。

三层实验中，cross $64/16/8$ 的 finite-1/3 为 70 V-cycles、endpoint 为 321；ring $128/16/8$ 的 finite-1/3 为 237、endpoint 为 770。有限路径点同时降低插值复杂度。

全文主线由真实谱目标、局部错配机制、有限传播与双尺度、完整谱路径和主比较构成；容量、奇异值扰动与余项差证明位于附录。
