# 研究方案与阶段报告 v2.2

## 面向高对比度多尺度扩散问题的自适应稀疏局部化能量粗空间

**版本：** v2.2  
**对应代码：** `two_grids_iteration 2.2.0`  
**研究定位：** 从两网格机制实验出发，研究能量极小多尺度基的局部化、稀疏非局部支撑及其多层迭代实现。

---

## 摘要

本文研究高对比度多尺度扩散问题的算子自适应粗空间。固定粗自由度后，全局能量极小基给出理想粗空间，但其支撑通常稠密；规则局部补丁可以降低构造成本，却可能切断跨粗区域的高导通路径。本文的核心问题是：能否从廉价局部基出发，只对少量困难基函数沿强连接拓扑进行稀疏非局部扩展，在保持粗空间质量的同时控制插值、Galerkin 粗算子和多层迭代的复杂度。

全局能量极小基与局部正交分解（LOD）具有相同的变分结构：它属于细尺度核空间的能量正交补；限制支撑后的局部能量基则是全局基的局部化近似。已有理论表明，适当条件下全局校正随粗网格层数指数衰减，但高对比度通道会使几何局部化常数显著退化。因此，本研究不把固定几何半径视为普适方案，而以局部能量极小化为默认构造，以拓扑感知的稀疏扩展处理长距离连接。

v2.2.0 的实验增加了固定预算、扩展后剪枝、逐轮残差预算、多右端项和种子—对比度扫描。结果表明：随机连续场中几何插值已经足够；与粗网格尺度匹配的棋盘结构适合有限层局部基；在当前 $128/16$ 通道样本上，固定强路径扩展明显优于两层局部基。但这一优势随网格尺度和场结构变化，逐轮残差预算也未优于固定预算。更重要的是，即使固定粗约束下的全局能量基，在困难通道场上仍可能收敛较慢。这说明支撑局部化是核心问题，但不能替代粗空间模式本身的充分性。

后续工作收束为三步：首先建立全局基尾部、局部残差与支撑误差之间的可计算关系；其次构造拓扑自适应的稀疏支撑并完成两网格验证；最后在固定几何层次上递归形成多重网格，研究多层收敛与复杂度。局部谱增广只作为全局参考粗空间仍不足时的后备机制，不在当前阶段展开为独立研究方向。

**关键词：** 多尺度扩散；高对比度；能量极小基；局部化；稀疏非局部支撑；多重网格

---

## 1. 研究目标与范围

### 1.1 模型问题

考虑

$$
-\nabla\cdot\bigl(a(x)\nabla u(x)\bigr)=f(x),
\qquad u|_{\partial\Omega}=0,
$$

其中

$$
0<a_{\min}\le a(x)\le a_{\max},
\qquad
\kappa=\frac{a_{\max}}{a_{\min}}\gg1.
$$

系数可以包含随机振荡、块状夹杂、细长通道或屏障。细网格离散得到对称正定系统

$$
A_0u_0=f_0.
$$

研究目标不是提出新的离散格式，而是构造能够识别低能量多尺度误差的粗空间，并将其用于多层迭代求解。

### 1.2 核心科学问题

本研究只保留一个中心问题：

> 如何利用局部可计算信息，为每个能量极小基选择最小但足够的稀疏支撑，使局部粗空间接近全局理想粗空间，并在多层递归中保持稳定收敛和可控复杂度？

它包含三个紧密相连的子问题：

1. 全局能量基在什么度量下局部化，衰减速度如何受系数拓扑影响？
2. 如何判断某个局部基是否需要扩展、向何处扩展以及何时停止？
3. 两网格局部化误差如何传递为多层收敛和算子复杂度？

### 1.3 当前边界

当前主线限定为：

- 标量、对称正定扩散问题；
- 规则网格上的确定性算法；
- 固定粗网格下先研究支撑，再推广到固定几何层次；
- 使用同一类光滑器，避免把支撑效果与光滑器调参混合；
- 不重新引入神经网络；
- 不在当前阶段同时研究完全代数粗化、非对称方程和复杂多物理系统。

若固定粗约束下的全局能量基仍不能达到目标收敛率，则将其判定为“粗空间模式不足”，而不是继续无限扩大支撑。局部谱增广仅在这一情况下作为后备处理。

---

## 2. 能量极小粗空间与局部化

### 2.1 全局理想基

令 $R$ 表示粗自由度约束。第 $j$ 个全局能量极小基定义为

$$
\psi_j^\infty
=
\arg\min_{v}\frac12v^TAv,
\qquad
Rv=e_j.
$$

令细尺度空间

$$
W=\ker R.
$$

最优性条件给出

$$
w^TA\psi_j^\infty=0,
\qquad \forall w\in W.
$$

因此，全局粗空间是 $W$ 的 $A$-正交补。这与 LOD 中“粗函数减去细尺度正交校正”的结构一致。全局基是固定粗观测下的理想多尺度基，同时也是本研究评价局部化误差的参考。

### 2.2 支撑受限的局部基

给定允许支撑 $S_j$，定义

$$
\psi_j^{S_j}
=
\arg\min_{
Rv=e_j,\;\operatorname{supp}v\subseteq S_j
}
\frac12v^TAv.
$$

若 $S_j\subseteq T_j$，则可行集嵌套，从而

$$
\|\psi_j^{T_j}\|_A^2
\le
\|\psi_j^{S_j}\|_A^2.
$$

支撑扩大使构造能量不增，但不能保证实际多重网格循环数单调下降，因为收敛还取决于粗空间与光滑器的谱互补性。

### 2.3 局部化假设

设 $U_k(j)$ 为 $k$ 层粗网格补丁。在适当的局部稳定性和 Poincaré 条件下，期望有

$$
\|\psi_j^\infty-\psi_j^k\|_A
\le
Ck^{d/2}\theta^k\|\psi_j^\infty\|_A,
\qquad 0<\theta<1.
$$

指数衰减针对补丁层数 $k$。若支撑节点数 $K\sim k^d$，则对节点数的衰减通常写成

$$
\exp\bigl(-cK^{1/d}\bigr),
$$

而不是一般意义上的 $\exp(-cK)$。

高导通通道可能使几何距离下的 $\theta$ 接近 1。研究的主要假设是：使用系数加权强连接距离，并沿少量关键路径形成非凸稀疏支撑，可以比扩大完整几何补丁更经济地逼近 $\psi_j^\infty$。

### 2.4 多尺度逼近与迭代求解的统一

令 $P$ 的列为局部化多尺度基，则粗问题为

$$
A_c=P^TAP,
\qquad
A_cu_c=P^Tf,
\qquad
u_{\mathrm{ms}}=Pu_c.
$$

若只使用 $u_{\mathrm{ms}}$，这是多尺度降阶或上尺度方法；若把该粗空间放入 V-cycle 或作为 PCG 预条件器，则最终仍可收敛到细网格离散解。因此，同一个 $P$ 同时承担多尺度基函数矩阵和多重网格插值矩阵的作用。

---

## 3. 核心算法框架

### 3.1 局部优先、困难列扩展

每个粗基从两层或三层局部支撑开始：

1. 在初始支撑上求能量极小基；
2. 计算支撑边界或支撑外的投影残差；
3. 只标记残差显著的困难列；
4. 沿强连接图增加少量候选节点；
5. 对变化列温启动重新能量极小化；
6. 删除低贡献节点并再次优化；
7. 达到误差阈值或复杂度预算后停止。

候选指标可从

$$
r_j=A\psi_j^{S_j}
$$

出发，但需要去除粗约束方向的正常残差。拟研究的支撑指标写为

$$
\eta_j(S_j)
=
\left\|
D^{-1/2}\Pi_W r_j
\right\|_{\partial S_j},
$$

其中 $\Pi_W$ 是细尺度空间投影，$\partial S_j$ 表示支撑边界。扩展节点的优先级由残差贡献、强连接路径和新增非零元成本共同决定。

### 3.2 支撑扩展的停止与分流

扩展在以下任一条件满足时停止：

$$
\eta_j(S_j)\le\tau_{\mathrm{loc}},
$$

$$
\frac{\Delta E_j}{\Delta\operatorname{nnz}}
\le\tau_{\mathrm{gain}},
$$

或达到每列与全局复杂度预算。

若局部支撑持续扩大，但固定粗约束下的全局参考基仍表现较差，则问题不是局部化不足，而是粗空间模式不足。此时只允许一种后备处理：在对应粗区域增加由局部小特征值识别的模式。该机制用于保证方法边界清楚，不在当前阶段扩展为完整 CEM-GMsFEM 研究。

### 3.3 多层递归

在第 $\ell$ 层构造 $P_\ell$ 后，定义

$$
A_{\ell+1}=P_\ell^TA_\ell P_\ell,
\qquad \ell=0,\ldots,L-1.
$$

并保持近零空间约束

$$
P_\ell B_{\ell+1}=B_\ell.
$$

对于标量扩散，首先只保持常数模式。多层设计遵循“层内局部、层次全局”：细层不构造接近全局的巨大支撑，长距离传播由较粗层逐级承担。

多层阶段固定使用对称 Gauss–Seidel 或一种预先选定的简单光滑器，以 V-cycle 作为 PCG 预条件器。初步研究不同时优化多种循环和光滑器。

### 3.4 复杂度约束

除收敛率外，必须监控

$$
C_g=\frac{\sum_{\ell=0}^{L}n_\ell}{n_0},
\qquad
C_A=\frac{\sum_{\ell=0}^{L}\operatorname{nnz}(A_\ell)}
{\operatorname{nnz}(A_0)},
$$

以及

$$
C_P=\frac{\sum_{\ell=0}^{L-1}\operatorname{nnz}(P_\ell)}{n_0}.
$$

支撑算法的目标不是单独最小化基能量，而是在目标收敛率下控制 $C_P$、$C_A$、setup 时间和循环成本。

---

## 4. v2.2.0 实现与实验设计

### 4.1 已实现的方法

当前代码包含：

- 几何插值；
- 两层、三层及更大局部能量基；
- 高精度全局能量参考基；
- 固定预算强连接扩展；
- `K128-prune64` 过采样—剪枝—再优化；
- 三种强连接归一化下的逐轮残差预算扩展；
- 多右端项测试和能量范数幂迭代谱代理；
- 种子、对比度和网格尺度扫描。

v2.2.0 只对发生支撑变化的列重新求解，并使用旧基作为温启动；PCG 停止判断减少了循环中的平方根计算。实验结果同时输出构造、粗算子准备、求解和总时间。

### 4.2 统一问题设置

主实验使用：

| 项目 | 设置 |
|---|---:|
| 细网格 | $h=1/128$ |
| 粗网格 | $H=1/16$ |
| $H/h$ | 8 |
| 细自由度 | $127^2=16129$ |
| 粗自由度 | $15^2=225$ |
| 主对比度 | $10^4$ |
| 构造线程 | 4 |

系数场包括随机连续场、二值连通通道和粗尺度棋盘场。实验 6 使用一个制造解、三个随机频谱解和两个点源，共六个右端项；实验 7 使用三个随机种子以及 $10^2,10^4,10^6$ 三个对比度。

### 4.3 评价指标

统一记录：

- 插值和粗算子非零元数；
- 相对全局基的能量误差；
- 多右端项平均、最坏循环数和失败数；
- 能量范数收缩率代理；
- 构造、粗算子准备、求解与总时间；
- 多层阶段的网格、插值和算子复杂度。

---

## 5. v2.2 阶段性结果

### 5.1 支撑算法消融

实验 5 的代表性结果如下。

| 系数场 | 方法 | $\operatorname{nnz}(P)$ | 循环数 | 总时间/ms |
|---|---|---:|---:|---:|
| 随机连续 | local-2 | 221,089 | 102 | 112.8 |
| 随机连续 | fixed-$K64$ | 222,469 | 102 | 176.6 |
| 通道 | local-2 | 220,848 | 3,121 | 1,843.1 |
| 通道 | fixed-$K64$ | 235,462 | 1,033 | 720.6 |
| 通道 | $K128$-prune64 | 235,473 | 1,026 | 769.2 |
| 棋盘 | local-2 | 220,680 | 3,020 | 1,848.6 |
| 棋盘 | fixed-$K128$ | 249,727 | 431 | 393.7 |
| 棋盘 | $K128$-prune64 | 235,473 | 691 | 659.2 |

固定强路径扩展在困难样本上明显降低循环数；过采样后剪枝可以在接近 $K=64$ 的最终规模下改善基质量，但额外 setup 成本并不总能转化为更低总时间。

逐轮残差预算在当前实现中未达到固定预算基线：通道场的 `budget-row` 为 2,707 次，棋盘场为 2,420 次。其主要问题是局部残差下降与两网格关键模态改善并不等价，且预算可能集中到少数对整体收敛不关键的列。因此该算法保留为负向消融，不作为推荐方法。

### 5.2 多右端项结果

实验 6 使用 4,000 次循环上限。下表报告六个右端项的平均循环数。

| 系数场 | 几何 | local-2 | local-3 | fixed-$K64$ | prune64 | 全局参考 |
|---|---:|---:|---:|---:|---:|---:|
| 随机连续 | 83.5 | 90.8 | 88.8 | 91.0 | 91.0 | 88.5 |
| 通道 | 4,000.0 | 3,195.7 | 1,920.3 | 1,116.5 | 1,052.0 | 657.2 |
| 棋盘 | 4,000.0 | 3,075.0 | 198.5 | 987.5 | 823.0 | 144.2 |

结果支持三个判断：

1. 随机连续场不需要复杂能量插值，几何基线更便宜；
2. 棋盘场的关键结构位于有限几何半径内，local-3 明显优于固定非局部扩展；
3. 通道场需要长距离信息，但固定粗约束下的全局参考仍需平均 657.2 次循环，说明仅优化支撑不能完全解决粗空间模式不足。

### 5.3 尺度依赖

实验 7 的 $64/8$ 网格、三个种子扫描显示，固定 $K64$ 在多数样本上没有带来与 $128/16$ 主实验相同的循环收益。以对比度 $10^4$ 为例：

| 场类型 | local-2 | local-3 | fixed-$K64$ |
|---|---:|---:|---:|
| 连续随机 | 132.3 | 133.0 | 132.3 |
| 块随机 | 178.0 | 177.3 | 177.7 |
| 通道 | 421.0 | 381.0 | 390.0 |

这说明固定节点预算不具有尺度一致性。支撑规则应以粗层数、代数距离或后验误差为尺度，而不能把固定 $K$ 直接推广为普适参数。

### 5.4 当前证据的统一解释

目前最稳妥的结论是：

- 局部化良好的随机连续场使用几何或很小局部基即可；
- 与粗网格尺度匹配的块结构存在有限临界半径；
- 跨越多个粗区域的通道使几何局部化变慢，适合稀疏方向性扩展；
- 扩展是否有益取决于尺度和粗空间约束，不能由“随机、棋盘、通道”名称直接决定；
- 能量下降、残差下降和两网格收敛改善不是同一指标。

---

## 6. 研究贡献与创新边界

能量极小插值、LOD 指数局部化、oversampling 和局部谱粗空间均已有成熟研究。因此，本项目不把这些概念本身作为创新。

在高完成度下，本研究的潜在贡献限定为：

1. **拓扑自适应稀疏局部化：** 用非凸、方向性的强连接支撑替代完整几何补丁；
2. **可计算支撑误差指标：** 将局部投影残差与全局能量基尾部建立可靠联系；
3. **两网格到多层的传递：** 说明局部化误差如何影响粗空间近似、多层收敛和算子复杂度；
4. **清楚的方法边界：** 区分支撑不足与粗空间模式不足，避免用无限扩展掩盖粗空间缺陷。

如果只完成固定支撑扩展和 V-cycle，创新性有限；如果能够完成后验指标、拓扑支撑、多层收敛和复杂度验证，则研究价值高、创新性中高。

---

## 7. 后续研究计划

### 阶段一：局部化规律与误差指标

目标是回答“何时需要扩展”。

1. 在中等规模上构造全局参考基；
2. 测量不同几何层和代数图层下的尾部能量；
3. 拟合衰减因子并研究其对对比度、通道长度和粗网格位置的依赖；
4. 验证投影边界残差 $\eta_j(S_j)$ 与真实基误差的相关性；
5. 尝试建立可靠性估计

$$
\|\psi_j^\infty-\psi_j^{S_j}\|_A
\le C\eta_j(S_j).
$$

阶段一不引入多层和谱增广，以保持问题单一。

### 阶段二：自适应稀疏支撑

目标是形成一个稳定的两网格算法：

1. local-2 或 local-3 初始化；
2. 困难列检测；
3. 沿强连接路径的少量扩展；
4. 过采样—剪枝—再优化；
5. 以误差阈值和复杂度预算停止；
6. 在多种种子、尺度、对比度和通道拓扑上验证。

只有当该方法相对最佳局部基在困难场上形成稳定 Pareto 优势，并在简单场上基本不触发扩展，才进入多层阶段。

### 阶段三：固定层次多重网格

目标是将通过验证的两网格构造递归到

$$
A_{\ell+1}=P_\ell^TA_\ell P_\ell.
$$

重点检查：

- V-cycle/PCG 收敛是否随网格加密和层数保持稳定；
- 扩展主要发生在哪些层；
- $C_P$ 和 $C_A$ 是否受控；
- setup 能否通过局部并行和多右端项复用摊销。

若某层的全局参考粗空间本身不能达到目标收敛率，再在该层局部加入少量谱模式；除此之外不扩展新的研究分支。

---

## 8. 成功判据与停止条件

### 8.1 成功判据

研究取得实质进展应同时满足：

1. 支撑指标能跨种子和尺度预测局部化误差；
2. 困难通道场上，自适应支撑优于达到相近收敛率的规则大补丁；
3. 随机连续和简单块场上，算法能够自然停留在小支撑；
4. 多层收敛不随细网格规模明显恶化；
5. 插值与算子复杂度保持可控；
6. setup 加求解总成本在单右端项或多右端项场景中具有明确优势。

### 8.2 停止或收缩条件

若出现以下情况，应停止对应路线：

- 支撑指标与真实尾部误差和两网格性能长期缺乏稳定关系；
- 自适应扩展普遍不如简单 local-3；
- 为保持收敛必须接近全局基密度；
- 多层 Galerkin 填充抵消全部迭代收益；
- 性能问题实际主要来自粗空间模式不足，而非局部化不足。

最后一种情况意味着研究应转向一个受控的局部谱增广，而不是继续增加支撑算法。

---

## 9. 代码与结果对应

| 位置 | 内容 |
|---|---|
| `src/multigrid/energy_interpolation.hpp` | 局部与全局能量极小插值 |
| `src/multigrid/adaptive_support.hpp` | 固定强连接支撑扩展 |
| `src/multigrid/residual_budget_support.hpp` | 逐轮残差预算消融 |
| `src/multigrid/support_pruning.hpp` | 过采样、剪枝和再优化 |
| `src/multigrid/two_grid_solver.hpp` | Galerkin 粗算子与两网格循环 |
| `test/experiment1.cpp`–`experiment4.cpp` | 原始局部化、容差、截断和支撑实验 |
| `test/experiment5.cpp` | 支撑算法消融 |
| `test/experiment6.cpp` | 多右端项和谱代理 |
| `test/experiment7.cpp` | 种子、对比度和尺度扫描 |
| `results/experiment5*`–`experiment7*` | v2.2 新增完整结果与 CSV |

当前代码是机制研究平台，不是生产级 AMG 库。计时结论只用于同一实现内的相对比较，正式性能研究仍需固定硬件、Release 构建、预热和重复统计。

---

## 10. 结论

本研究的核心不是为不同系数场手工指定不同算法，也不是追求稠密全局能量基，而是理解全局理想多尺度基为何能够局部化、何时局部化变慢，以及如何用最少的拓扑相关支撑恢复必要的长距离信息。

v2.2 的数值结果支持“局部优先、困难列稀疏扩展”的总体方向，但同时否定了两个过强结论：固定 $K$ 不具有尺度一致性，简单残差预算也不能自动代表对两网格关键模态的改善。下一步最重要的工作不是继续增加算法模块，而是建立可信的局部化误差指标，并在此基础上形成单一、可解释的自适应支撑方法。

在两网格机制得到验证后，该方法可递归形成多层 Galerkin 层次。其最终目标是得到一个面向高对比度多尺度扩散问题的求解器：在普通区域保持局部和低成本，在长距离通道附近进行少量稀疏扩展，并在固定粗空间确实不足时才增加必要的局部模式。

---

## 参考文献

1. W. L. Wan, T. F. Chan, B. Smith, “[An Energy-Minimizing Interpolation for Robust Multigrid Methods](https://doi.org/10.1137/S1064827598334277),” *SIAM Journal on Scientific Computing*, 21, 1632–1649, 2000.
2. J. Xu, L. Zikatanov, “[On an Energy Minimizing Basis for Algebraic Multigrid Methods](https://doi.org/10.1007/s00791-004-0147-y),” *Computing and Visualization in Science*, 7, 121–127, 2004.
3. P. S. Vassilevski, “[General Constrained Energy Minimization Interpolation Mappings for AMG](https://doi.org/10.1137/080726252),” *SIAM Journal on Scientific Computing*, 32, 1–13, 2010.
4. L. N. Olson, J. B. Schroder, R. S. Tuminaro, “[A General Interpolation Strategy for Algebraic Multigrid Using Energy Minimization](https://doi.org/10.1137/100782505),” *SIAM Journal on Scientific Computing*, 33, 966–991, 2011.
5. A. Målqvist, D. Peterseim, “[Localization of Elliptic Multiscale Problems](https://doi.org/10.1090/S0025-5718-2014-02868-8),” *Mathematics of Computation*, 83, 2583–2603, 2014.
6. E. T. Chung, Y. Efendiev, W. T. Leung, “[Constraint Energy Minimizing Generalized Multiscale Finite Element Method](https://doi.org/10.1016/j.cma.2018.04.010),” *Computer Methods in Applied Mechanics and Engineering*, 339, 298–319, 2018.
7. J. Brannick, S. P. MacLachlan, J. B. Schroder, B. S. Southworth, “[The Role of Energy Minimization in Algebraic Multigrid Interpolation](https://arxiv.org/abs/1902.05157),” 2019.
8. C. Janna, A. Franceschini, J. B. Schroder, L. Olson, “[Parallel Energy-Minimization Prolongation for Algebraic Multigrid](https://doi.org/10.1137/22M1513794),” *SIAM Journal on Scientific Computing*, 45, A2561–A2584, 2023.
