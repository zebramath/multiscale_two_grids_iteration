# 多尺度有限 Krylov 能量插值研究方案 v4.3

## 1. 研究定位

本项目研究高对比扩散问题中，全局能量极小插值的有限 PCG 路径与实际两网格收敛
目标之间的非单调错位。研究对象明确限定为二维规则网格、固定粗点、固定对称
Gauss--Seidel、Galerkin 粗校正和两网格结构；它不是通用工业 AMG，也不以多层软件
框架或工程基线竞速作为核心贡献。

论文应围绕三个问题组织：

1. 为什么插值能量严格趋近全局极小值时，两网格循环数可能先下降后显著回升；
2. 哪些粗空间几何量连接有限 PCG 能量误差和固定磨光器下的两网格性能；
3. 能否不读取尺寸、对比度和拓扑标签，用一个简单参数在低 setup 与低循环数之间取舍。

其中前两项是学术主线，第三项是把理论现象变成可复现实验方法的必要算法接口。
`expected_rhs_count = R` 仍是唯一策略倾向参数：小 `R` 为少右端项压缩 setup，
大 `R` 为复用场景支付更多选择成本。

## 2. 理论稿 v1--v16 的完整综合

本轮以仓库历史中的每个 `theory.tex` 版本为对象，按正文结构、定理层级和相邻版本
变化重新梳理。各版本的有效贡献及问题如下。

| 版本 | 主要推进 | 对 v4.3 的处理 |
|---|---|---|
| v1 | 建立能量极小、PCG 正交尾、Ritz 滤波和有限步机制 | 保留能量与有限步核心 |
| v2 | 加入局部残差、循环数关系、剪枝扰动与结论层级 | 仅保留与主线直接相关的边界 |
| v3 | 加入成本感知候选、预测误差和多 RHS | 保留预测后悔界与 break-even |
| v3.3 | 对 v3 做代码接口校准 | 吸收接口一致性要求 |
| v4 | 增加可输出诊断量 | 删除不再进入主实验的诊断支线 |
| v5 | 扩展到递归多层 | 因项目定位回到两网格而删除 |
| v6 | 去除多层，转向渐进竞速选择 | 保留简单预算思想 |
| v7 | 大幅压缩为约 300 行 | 结构清楚，但理论支撑过薄，不作为终稿 |
| v8 | 引入 Schur 图坐标，连接能量、夹角与两网格 | 成为 v4.3 的核心坐标 `Z` |
| v9 | 加入最佳同维粗空间、全局稳定性和 Pareto 障碍 | 保留最佳空间与 Lipschitz/能量障碍 |
| v10 | 扩展 Grassmann 方向、二阶分类和预测误差传播 | 保留终点一阶方向，删除高阶分类 |
| v11 | 扩展一般 pre/post 磨光与近似粗解 | 只在适用边界中说明，不冒充已实现功能 |
| v12 | 达到 3659 行、56 个定理型环境，覆盖最完整 | 判断为明显过度展开，删去旁支和重复变体 |
| v13 | 开始收紧记号和自适应证书结构 | 保留“谱几何与循环预测是两类误差”的判断 |
| v14 | 进一步删减推广章节和选择接口 | 继续收束，但仍有较多未实现证书 |
| v15 | 压缩至 1102 行，形成能量--图坐标--选择主链 | 作为 v4.3 的直接理论骨架 |
| v16 | 加入严格反单调 PCG 尾部 | 保留并改写为显式一阶量压过二阶余项的充分条件 |

综合后的 `theory.tex` 为 535 行、10 个定理/命题/推论型环境。它删除了局部支撑扩张、
剪枝、近似粗解、多层推广、复杂 Pareto 分支和未在代码中计算的路径谱认证；保留并强化：

- PCG 的精确正交能量尾；
- 归一化误差 $Z=B^{1/2}(W-W_*)S^{-1/2}$ 对能量、粗矩阵和主角的统一解释；
- 对称两网格谱半径的精确投影表达、最佳同维粗空间和全局 Lipschitz 界；
- 有限步优势的“问题可见性 × 路径对齐”一阶分解；
- 带显式余项条件的严格反单调 PCG 尾部；
- 与 v4.3 代码逐项一致的候选预算、pilot 预测、选择后悔界和多 RHS 盈亏平衡。

## 3. v4.3 自适应 PCG

### 3.1 单参数预算族

令

$$
d(R)=\operatorname{clip}\!\left(\frac{\log_2R}{8},0,1\right).
$$

默认 `minimum_steps=12`、`maximum_steps=60`、量化步长 2，策略使用

$$
\begin{aligned}
\text{stride}(R)&=Q_2(20-12d),\\
\overline m(R)&=Q_2(52+8d),\\
L(R)&=\operatorname{round}(16+144d),\\
\delta(R)&=0.10-0.08d.
\end{aligned}
$$

- `R=1`：只评估 `m=0,12,32,52`，16 次 pilot，10% 近优松弛，无细化；
- `R=256`：评估 `m=0,12,20,...,60`，160 次 pilot，2% 松弛，至多两个 `m±2` 细化；
- 中间 `R` 连续改变候选间距、路径上限、pilot 和松弛，不增加拓扑路由。

相对 v4.2，激进档的固定候选数从 8 降为 4，pilot 从 64 降为 16，最长路径从 60
降为 52。这是确定性的算法预算缩减，不依赖 wall-clock 测量。

### 3.2 预测与选择

每个候选用同一代表性右端项运行短两网格试验。在 pilot 后半段对连续对数残差比取
中位数和 MAD，构造保守尾部因子并预测完整循环数 $\widehat N_m$。从满足

$$
\widehat N_m\le(1+\delta(R))\min_j\widehat N_j
$$

的候选中选最小 `m`。该规则先保持预测循环数近优，再偏向较早路径点；不使用尺寸、
对比度、拓扑标签、oracle 或短时 wall-clock 路由。

激进档刻意不再追求 near-oracle。若所有候选预测误差不超过 $\varepsilon$，理论稿证明

$$
N_{\widehat m}\le(1+\delta)N_{m_*}+(2+\delta)\varepsilon.
$$

短 pilot 会增大 $\varepsilon$，这是用少量 setup 换取更多循环数的明确风险，而不是
需要隐藏的缺陷。

## 4. 代码结构与本轮改动

公开核心保持三个数值模块：

| 模块 | 作用 |
|---|---|
| `energy_interpolation.hpp` | 几何、局部和全局能量插值 |
| `global_pcg.hpp` | 增量有限 PCG 路径与 v4.3 单参数预算选择器 |
| `two_grid_solver.hpp` | Galerkin 粗算子、对称平滑和两网格循环 |

本轮没有重新引入多层、复杂锚点、拓扑分类或多阶段确认。新增报告字段
`checkpoint_stride` 和 `maximum_sampled_steps`，使实际预算可在 oracle 输出中核对；
回归测试固定检查 `R=1` 的 16 次 pilot、四候选上限和 `R=256` 的 160 次 pilot。

## 5. 主实验：11 个两网格问题

单因素矩阵覆盖四个尺寸组合、三个对比度和六种通道拓扑。全部方法使用相同矩阵、粗点、
右端项、磨光器、相对容差 $10^{-6}$ 和 6000 次上限。

| 轴 | $1/h,1/H$ | 对比度 | 拓扑 | fast `m/cycles` | reuse `m/cycles` | exact cycles |
|---|---|---:|---|---:|---:|---:|
| size | 32,8 | $10^4$ | cross | 0 / 40 | 0 / 40 | 54 |
| size | 64,8 | $10^4$ | cross | 12 / 204 | 12 / 204 | 264 |
| size | 64,16 | $10^4$ | cross | 32 / 114 | 18 / 54 | 321 |
| center | 128,16 | $10^4$ | cross | 52 / 276 | 42 / 238 | 3227 |
| contrast | 128,16 | $10^2$ | cross | 32 / 293 | 20 / 228 | 756 |
| contrast | 128,16 | $10^6$ | cross | 52 / 298 | 52 / 298 | 3471 |
| topology | 128,16 | $10^4$ | meandering | 52 / 336 | 36 / 272 | 759 |
| topology | 128,16 | $10^4$ | diagonal | 52 / 351 | 36 / 297 | 418 |
| topology | 128,16 | $10^4$ | parallel | 52 / 363 | 42 / 251 | 421 |
| topology | 128,16 | $10^4$ | branching | 32 / 347 | 36 / 267 | 615 |
| topology | 128,16 | $10^4$ | winding-ring | 52 / 228 | 44 / 233 | 768 |

循环数汇总为 fast 2850、reuse 2382、global-exact 11074。global-exact 分别是 fast
和 reuse 的 3.89 倍与 4.65 倍；几何插值仅 3/11 在 6000 次内收敛。有限插值优势
具有明显问题依赖性：中心 cross 和 $10^6$ cross 上 exact/fast 为 11.69 和 11.65，
而 32/8 小问题只有 1.35。正确结论是完整矩阵上保持约四倍累计优势，并在目标错位强的
问题上出现数量级优势，而不是逐例十倍。

| 方法 | setup 总和 | 单 RHS solve 总和 | `R=1` 总成本 | `R=256` 总成本 |
|---|---:|---:|---:|---:|
| adaptive-fast | 9.354 s | 5.176 s | 14.530 s | 1334.349 s |
| adaptive-reuse | 46.080 s | 3.613 s | 49.693 s | 971.044 s |
| global-exact | 26.623 s | 47.217 s | 73.840 s | 12114.264 s |
| geometric | 0.018 s | 23.851 s | 23.869 s | 6105.948 s |

同一完整运行内，fast 相对 reuse 把 setup 降低 79.7%，代价是循环总和增加 19.6%；
单 RHS 总成本降低 70.8%。到 256 个右端项时，reuse 因单次求解更快而使总成本降低
27.2%。这些时间为一次本机 wall-clock，只用于验证策略方向；稳定证据仍是收敛、候选
预算、循环数和 oracle gap。

## 6. 有限路径机制实验

| 问题 | 有限最优点 | 有限循环 | exact 循环 | 该点归一化能量超额 |
|---|---:|---:|---:|---:|
| 64/16, cross, $10^4$ | $m=20$ | 68 | 321 | $3.164\times10^{-4}$ |
| 128/16, cross, $10^4$ | $m=44$ | 244 | 3227 | $2.006\times10^{-4}$ |
| 128/16, cross, $10^6$ | $m=60$ | 279 | 3471 | $6.152\times10^{-5}$ |

完整路径显示能量超额持续下降，但循环数可先达到有限最小值再回升。例如 128/16、
$10^4$ 的循环数从 `m=44` 的 244 上升到 `m=60` 的 360，最终 exact 为 3227。
这直接支撑理论稿的“能量目标与固定磨光两网格目标错位”，无需另设大规模谱诊断入口。

## 7. 离线 step--2 oracle

oracle 完整求解 `m=0` 和 `m=12,14,...,60`，只用于评价，不进入在线算法。

| 问题 | fast `m/cycles/gap` | reuse `m/cycles/gap` | oracle `m/cycles` |
|---|---:|---:|---:|
| 32/8, $10^4$, cross | 0 / 40 / 0.00% | 0 / 40 / 0.00% | 0 / 40 |
| 64/8, $10^4$, cross | 12 / 204 / 0.00% | 12 / 204 / 0.00% | 12 / 204 |
| 64/16, $10^4$, cross | 32 / 114 / 111.11% | 18 / 54 / 0.00% | 18 / 54 |
| 128/16, $10^4$, cross | 52 / 276 / 19.48% | 42 / 238 / 3.03% | 38 / 231 |
| 128/16, $10^2$, cross | 32 / 293 / 29.65% | 20 / 228 / 0.88% | 26 / 226 |
| 128/16, $10^6$, cross | 52 / 298 / 6.81% | 52 / 298 / 6.81% | 60 / 279 |
| 128/16, $10^4$, ring | 52 / 228 / 0.00% | 44 / 233 / 2.19% | 52 / 228 |

fast 的逐例 gap 平均为 23.86%、最大 111.11%；reuse 为 1.84% 和 6.81%。fast 的
极端 gap 出现在 64/16 问题，但其 114 次仍显著少于 exact 的 321 次，且几何插值在
6000 次内失败。该结果准确体现两种档位：fast 是低 setup 的粗选择器，不是 near-oracle；
reuse 才是多 RHS 场景下的细致选择器。七问题选择耗时总和分别为 5.648 s 和 26.886 s，
fast 降低 79.0%。

## 8. JSC 投稿定位与仍需补强之处

v4.3 已形成比“参数调得好”更稳定的论文骨架：理论上给出目标错位、全局稳定性和局部
反单调充分条件；算法上给出无标签、单参数预算选择；实验上同时展示全局精确插值、
几何插值、完整有限路径和离线 oracle。

按 Journal of Scientific Computing 的定位，正式投稿前最有价值的补强不是继续增加
工程模块，而是：

1. 将理论稿改写为英文论文体，并对一阶导数和反单调尾部做独立逐式审校；
2. 增加若干随机右端项或右端项分布迁移实验，验证 `R` 的复用含义；
3. 对核心表格做至少 5 次独立计时，报告中位数与 IQR，但继续把循环数作为主证据；
4. 增加一组非规则或三维的最小外推实验会明显提高说服力，但不应扩展成通用 AMG 工程；
5. 在论文中完整披露 fast 的 111% oracle gap，并强调它换取约 79% 选择 setup 降幅。

当前不能声称：任意 SPD 问题都存在有限步优势；单 RHS pilot 能代表任意 RHS 分布；
fast 是近 oracle；或二维两网格结果已经构成通用 AMG 方法。

## 9. 复现

在 `code` 目录执行：

```bash
./scripts/run_validation.sh quick
./scripts/run_validation.sh full
```

脚本优先使用 CMake；若环境没有 CMake，会使用等价的 C++17 严格直接构建。三份完整
逐行结果位于 `code/results`。正式研究结果使用 `full`，`quick` 只用于开发检查。
