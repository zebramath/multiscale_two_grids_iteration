# 多尺度有限 Krylov 能量插值研究方案 v4.1

## 1. 研究定位

本项目研究高对比扩散问题中全局能量插值的有限 Krylov 路径，以及插值能量目标与实际两网格收敛目标之间的非单调错位。v4.1 只保留两网格这一最直接的受控对象，不再实现或测试递归多层 V-cycle 与多层预条件 PCG。

中心问题是：全局能量方程求得更精确时，为什么两网格循环数反而可能增加；以及能否在不知道系数拓扑名称和对比度标签的前提下，自动找到有限 PCG 路径上的优势窗口。

本项目不是通用工业 AMG 基准。规则二维网格、固定粗点、固定对称 Gauss--Seidel 平滑器和固定 Galerkin 粗校正用于隔离插值本身的作用。

## 2. 模型与三种插值

考虑

$$
-\nabla\cdot(a(x)\nabla u)=f\quad\text{in }(0,1)^2,
\qquad u=0\quad\text{on }\partial\Omega,
$$

离散后得到对称正定系统 $A_hu_h=b_h$。系数场覆盖交叉、曲折、对角、平行、分支和闭合曲环六类通道，对比度为 $10^2$、$10^4$ 或 $10^6$。

固定粗点单位注入后，将插值写成 $P(W)$，并定义

$$
J(W)=\frac12\operatorname{tr}(P(W)^TAP(W)).
$$

三种比较对象为：

1. `geometric`：几何双线性初值 $P_0$；
2. `global-exact`：把全局能量系统求到相对预条件残差 $10^{-10}$，得到 $P_\ast$；
3. `adaptive-PCG`：从 $P_0$ 出发，在有限 PCG 路径 $P_0,P_1,\ldots,P_m$ 上自适应选取中间点。

对有限路径有

$$
J(P_m)-J(P_\ast)
=\frac12\lVert W_m-W_\ast\rVert_{A_{FF},F}^2,
$$

所以能量沿 PCG 路径单调下降。但两网格误差传播

$$
E_{TG}(P_m)=S^\ast
\left(I-P_m(P_m^TAP_m)^{-1}P_m^TA\right)S
$$

取决于有限插值误差的方向，而不只取决于其能量大小。因此 $J(P_m)$ 单调下降不蕴含两网格循环数单调下降。

## 3. v4.1 渐进式候选竞速

v4.0 只检查至多四个候选，并用 24 次 pilot 尾部外推。该方案能发现宽优势区间，但在明显非渐近的残差路径上会漏掉较窄的有限步窗口。

v4.1 使用统一的三阶段选择器：

1. **广筛**：若几何候选不够好，沿同一增量 PCG 路径生成 $m=12,20,28,36,44,52,60$，每个候选运行至多 64 次两网格 pilot；
2. **确认**：把预测最好的三个候选、最小步锚点以及 3% 松弛下的稀疏优先候选推进到至多 384 次；
3. **局部细化**：检查当前优胜者相邻的 step--2 候选，并在优胜者与次优者之间补充中点。

最小步锚点被强制保留，是因为有限步正则化常在路径前段最强；只按尾部预测排名可能重新滑向接近精确终点的稠密候选。局部细化从几何初值重启一条短 PCG 路径，广筛阶段则完整复用增量路径。

选择器不读取网格案例名称、拓扑、对比度、种子或 oracle 最优步数。代价是 setup 比 v4.0 更高，因此 v4.1 同时报告循环数、密度、setup、solve 和 total，而不只强调迭代改善。

## 4. 软件与实验结构

核心代码只保留四个两网格相关模块：

| 模块 | 功能 |
|---|---|
| `energy_interpolation.hpp` | 几何、局部和全局能量插值 |
| `global_pcg.hpp` | 增量有限 PCG 路径、广筛、确认和局部细化 |
| `two_grid_solver.hpp` | Galerkin 粗算子、对称平滑和两网格循环 |
| `support_expansion.hpp` | 保留的局部支撑研究基础设施，不进入 v4.1 主实验 |

公开实验压缩为三项：

| 入口 | 学术作用 |
|---|---|
| `experiment1_two_grid_comparison` | 尺寸、对比度和拓扑三个轴上的主比较 |
| `experiment2_finite_path` | 能量单调而两网格性能非单调的机制证据 |
| `experiment3_oracle_validation` | 渐进式选择器相对 step--2 离线 oracle 的误差 |

复杂度/谱扫描、pilot 参数消融、压力测试、多 RHS 和所有多层实验均不再作为公开入口。

## 5. 实验 1：两网格主比较

### 5.1 单因素实验矩阵

以 $(1/h,1/H)=(128,16)$、对比度 $10^4$、cross-channel 为中心：

- 尺寸轴：$(32,8)$、$(64,8)$、$(64,16)$、$(128,16)$；
- 对比度轴：$10^2$、$10^4$、$10^6$；
- 拓扑轴：六类通道。

中心问题只计算一次，共 11 个问题。三种方法在每行使用完全相同的矩阵、粗点、右端项、平滑器、容差 $10^{-6}$ 和最多 6000 次循环。

### 5.2 汇总

| 插值 | 收敛 | 累计循环 | 平均密度 | Setup 总和 | Solve 总和 | Total 总和 |
|---|---:|---:|---:|---:|---:|---:|
| adaptive-PCG | 11/11 | 2356 | 24.7827% | 24535.579 ms | 1753.344 ms | 26288.923 ms |
| global-exact | 11/11 | 11074 | 96.8747% | 15170.706 ms | 22768.247 ms | 37938.954 ms |
| geometric | 3/11 | 49843 | 2.1057% | 14.441 ms | 13691.346 ms | 13705.787 ms |

相对 global-exact，自适应的累计循环数减少 78.72%，即 global-exact 约为其 4.70 倍；平均插值密度减少 74.42%，单次完整实验的累计 Total 减少 30.71%。但自适应 Setup 总和约为 exact 的 1.62 倍，说明更可靠的候选确认并非免费。

### 5.3 全部 11 个问题

| 轴 | $1/h,1/H$ | 对比度 | 拓扑 | adaptive $m$ | 循环 adaptive/exact | 倍数 exact/adaptive | 密度 adaptive/exact |
|---|---|---:|---|---:|---:|---:|---:|
| size | 32,8 | $10^4$ | cross | 0 | 40 / 54 | 1.35 | 5.10% / 95.01% |
| size | 64,8 | $10^4$ | cross | 12 | 204 / 264 | 1.29 | 24.67% / 98.79% |
| size | 64,16 | $10^4$ | cross | 18 | 54 / 321 | 5.94 | 21.60% / 92.25% |
| center | 128,16 | $10^4$ | cross | 40 | 234 / 3227 | **13.79** | 27.00% / 97.84% |
| contrast | 128,16 | $10^2$ | cross | 20 | 228 / 756 | 3.32 | 11.62% / 98.41% |
| contrast | 128,16 | $10^6$ | cross | 60 | 279 / 3471 | **12.44** | 44.48% / 96.23% |
| topology | 128,16 | $10^4$ | meandering | 42 | 251 / 759 | 3.02 | 28.60% / 97.38% |
| topology | 128,16 | $10^4$ | diagonal | 42 | 331 / 418 | 1.26 | 28.60% / 97.55% |
| topology | 128,16 | $10^4$ | parallel | 40 | 235 / 421 | 1.79 | 27.00% / 97.40% |
| topology | 128,16 | $10^4$ | branching | 36 | 267 / 615 | 2.30 | 23.60% / 97.45% |
| topology | 128,16 | $10^4$ | winding-ring | 44 | 233 / 768 | 3.30 | 30.34% / 97.30% |

用户此前观察到的“约十倍”现象确实存在：中心问题和 $10^6$ cross-channel 分别达到 13.79 倍和 12.44 倍。v4.0 只有约两倍，主要因为其轮换矩阵混合了精确终点本来就较好的问题，同时较短 pilot 没有充分确认有限优势窗口。

但十倍不是跨问题普遍值。尺寸较小或 diagonal/parallel 拓扑上，global-exact 本来就不差，可改善空间有限。正确结论是：有限 PCG 在具有明显目标错位的高分离问题上产生数量级优势，在完整轴向矩阵上产生约 4.70 倍累计循环优势。

## 6. 实验 2：有限路径机制

| 问题 | 代表有限点 | 有限循环 | exact 循环 | exact/finite | 归一化能量超额 |
|---|---:|---:|---:|---:|---:|
| 64/16, cross, $10^4$ | $m=20$ | 68 | 321 | 4.72 | $3.164\times10^{-4}$ |
| 128/16, cross, $10^4$ | $m=44$ | 244 | 3227 | 13.23 | $2.006\times10^{-4}$ |
| 128/16, cross, $10^6$ | $m=60$ | 279 | 3471 | 12.44 | $6.152\times10^{-5}$ |

完整路径结果保留 $m=0,12,20,28,36,44,52,60$ 和 exact。三个问题中，能量超额持续趋近零，但循环数在有限区间达到最小后可以重新增加。例如 $128/16$、$10^4$ 的循环数从 $m=44$ 的 244 回升到 $m=60$ 的 360，最终 exact 为 3227。

这组结果直接支持理论中的非蕴含关系，而不需要额外谱或复杂度诊断。

## 7. 实验 3：step--2 oracle

离线 oracle 独立求解 $m=0$ 与 $m=12,14,\ldots,60$ 的每个候选，只用于评价选择质量，不计入自适应 Setup，也不是实际竞争算法。

| $1/h,1/H$ | 对比度 | 拓扑 | adaptive $m$/循环 | oracle $m$/循环 | gap |
|---|---:|---|---:|---:|---:|
| 32,8 | $10^4$ | cross | 0 / 40 | 0 / 40 | 0.00% |
| 64,8 | $10^4$ | cross | 12 / 204 | 12 / 204 | 0.00% |
| 64,16 | $10^4$ | cross | 18 / 54 | 18 / 54 | 0.00% |
| 128,16 | $10^4$ | cross | 40 / 234 | 38 / 231 | 1.30% |
| 128,16 | $10^2$ | cross | 20 / 228 | 26 / 226 | 0.88% |
| 128,16 | $10^6$ | cross | 60 / 279 | 60 / 279 | 0.00% |
| 128,16 | $10^4$ | winding-ring | 44 / 233 | 52 / 228 | 2.19% |

平均 gap 为 0.62%，最大 gap 为 2.19%；v4.0 对应结果为平均 8.82%、最大 20%。改善来自更长确认、锚点保护和局部 step--2 细化，而不是更换问题标签驱动的规则。

## 8. 理论主线

`theory.tex` 的 v4.1 主线为：

1. 能量恒等式严格刻画有限 PCG 点到精确能量终点的距离；
2. PCG 正交尾和 Ritz 多项式解释有限路径的谱选择性；
3. 两网格图参数公式与 LMI 判据说明能量最小不等于两网格谱半径最小；
4. 在通道对齐、Ritz 分离和谱裕量条件下，可严格得到有限点优于精确终点；
5. 预测误差传递界、候选覆盖误差分解和 setup 盈亏平衡公式约束自适应算法的可声称范围。

新增的 oracle gap 分解把最终差距拆为“已生成候选中的选择损失”和“有限候选覆盖损失”。理论文件已删除所有多层递推、V-cycle 正定性及多层预条件 CG 分析。

## 9. 结论边界

- 十倍以上优势出现在明确的高分离 cross-channel 问题，不是所有 SPD 系统或所有通道拓扑的普遍保证；
- 单一代表右端项上的选择不能自动推广到任意多 RHS 分布；
- v4.1 的确认阶段提高选择可靠性，但 Setup 成本增加，小问题上 Total 可能不占优；
- 计时是单次 wall-clock 辅助数据，主要可重复证据是收敛性、循环数、候选步数和密度；
- 当前贡献是有限 Krylov 插值路径的目标错位、结构条件与可执行选择策略，而不是通用 AMG 或多层求解器。

## 10. 复现

在 `code` 目录执行：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=ON
cmake --build build --parallel 4
ctest --test-dir build --output-on-failure
./scripts/run_validation.sh full
```

三个实验的完整逐行结果位于 `code/results`。`--quick` 只运行主比较的三个开发案例；正式结果必须使用 `full`。
