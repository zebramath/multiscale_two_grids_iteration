# multiscale_two_grids_iteration v4.7

二维高对比扩散问题中，有限 Krylov 能量插值与两网格非单调收敛研究。

- `research_report.md`：完整研究问题、理论主线、自适应算法和核心结果。
- `theory.tex`：能量路径、粗空间几何、两网格非单调机制与预算化选择理论。
- `VALIDATION_v4.7.md`：严格构建、单元/回归测试、Sanitizer 和完整实验记录。
- `code/`：C++17 两网格实现、四个整合实验、测试和 TXT 结果。

v4.7 保持 `expected_rhs_count=R` 作为策略倾向参数。`R=1` 评估四个候选且每个候选
最多运行 16 次 pilot，以压缩 setup；`R=256` 使用细密候选、每个候选最多 160 次
pilot 和至多两个邻域细化，以降低循环数。实际残量历史响应尺度、对比度和拓扑变化。
代码围绕几何插值、全局能量参考、有限 PCG 路径、自适应选择和两网格求解组织；完整
结果覆盖两个新增大尺度算例、五种系数 seed、六类 RHS 迁移和重复计时。
