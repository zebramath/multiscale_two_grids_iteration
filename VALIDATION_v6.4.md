# multiscale_two_grids_iteration v6.4 验证记录

验证日期：2026-08-31  
数值源码版本：6.4.0  
理论与研究方案版本：6.4  
基线来源：已验证并归档的 v6.3

## 0. v6.4 变更边界

v6.4 保持 v6.3 的 adaptive 规则和正式实验 1--6 数值不变，新增一个次要多层实验：

1. 64/16/8 与 128/16/8 的三层 Galerkin V-cycle；
2. 同一首层 transfer 下配对精确两网格，隔离递归粗解的额外代价；
3. 两个非最粗层分别构造 adaptive、global-reference 和 geometric transfer。

代码新增通用 `MultilevelVCycle`，并把定常迭代停止、状态和收敛因子统计抽成两网格与
多层共用内核。版本数值常量从 v6.3 遗留的错误 `6.2.0` 修正为与字符串一致的 `6.4.0`，
单元测试固定检查二者一致。理论稿新增递归 V-cycle 与精确两网格的误差传播恒等分解、
能量范数稳定界和逐层展开，同时补全记号表与关键证明步骤。

v6.3 已完成的两组证据继续保留：

1. 同一路径上的停止策略消融：adaptive、统一 $m=\operatorname{round}(n/4)$ 和逐列
   相对残量 $10^{-2}$；
2. 固定物理通道宽度、背景分区和 seed 的三层嵌套网格加密。

实验 1--4 的 v6.2 结果和实验 5--6 的 v6.3 结果均原样保留；只有实验 7 由 6.4.0 源码
完整运行生成。

## 1. 验证环境

- Linux 6.18.35，x86_64；
- AMD EPYC 9V74；当前容器提供 9 个逻辑 CPU；
- GCC 13.3.0；
- C++17；
- CMake 在当前环境中不可用，验证脚本使用等价的严格直接构建路径；
- XeTeX 3.141592653-2.6-0.999995（TeX Live 2023）；当前环境缺少 `ctexart.cls`。

## 2. 构建与测试

以下检查全部通过：

| 检查 | 配置 | 结果 |
|---|---|---|
| `unit_core` | 4 线程构建路径 | 通过 |
| `regression_core` | 含固定物理场嵌套一致性与残量停止回归 | 通过 |
| v6.4 严格直编 | 全部七个实验及两项测试，扩展告警全部开启并视为错误 | 通过，无告警 |
| `run_validation.sh` 语法 | `sh -n` | 通过 |
| ASan + UBSan `unit_core` | `-O1 -g -fsanitize=address,undefined` | 通过 |
| ASan + UBSan `regression_core` | 同上 | 通过 |
| 两层退化恒等回归 | 同一 transfer 下比较一次精确两网格与两层 V-cycle 解向量 | 通过 |
| 三层 V-cycle 单元回归 | 16/4/2 小层次、Galerkin 维数与收敛 | 通过 |

Sanitizer 运行设置为遇错立即停止；由于当前容器不支持可靠的 LeakSanitizer 路径，本项
关闭 leak 检查，只记录 AddressSanitizer 与 UndefinedBehaviorSanitizer 结果。

## 3. 新增实验 5：停止策略消融

使用 `TGI_THREADS=4` 运行六个代表问题。三种方法使用相同几何初值、同一全局
Jacobi--PCG 列方程和同一两网格求解容差，两个简单基线在全部案例间不重新调参。

| 策略 | 收敛 | 记录循环和 | PCG 列迭代和 |
|---|---:|---:|---:|
| adaptive | 6/6 | 1147 | 45871 |
| fixed-step | 5/6 | 21098 | 32792 |
| fixed-residual | 5/6 | 25658 | 65687 |

- adaptive 在 5/6 个案例上循环数不高于 fixed-step，在 6/6 个案例上不高于
  fixed-residual；
- 64/16、$10^4$ cross 上 fixed-step 为 60 次、adaptive 为 65 次，保留该真实例外；
- 128/16、$10^6$ cross 上 adaptive 以 273 次收敛，两个固定规则均达到 20000 次
  `slow-limit`；
- fixed-residual 的所有列均达到 $10^{-2}$ 阈值，确认比较的失败不来自阈值未满足；
- 记录循环和包含未收敛案例已经执行的 20000 次，不作未观察收敛时间的外推。

## 4. 新增实验 6：固定物理系数场加密

连续系数定义固定为 cross-channel、对比度 $10^4$、物理通道宽度 $1/16$、$8\times8$
背景分区和 seed 1。网格取 32/4、64/8、128/16，保持 $H/h=8$ 并同步二分加密。

| $1/h,1/H$ | adaptive `m`, $m/n$ | adaptive | reference | geometric |
|---|---:|---:|---:|---:|
| 32,4 | 4, 0.125 | 122 | 193 | 186 |
| 64,8 | 8, 0.125 | 119 | 242 | 178 |
| 128,16 | 43, 0.336 | 145 | 151 | 187 |

- 相邻嵌套层所有共享节点的系数值逐点比较，失配数为 0；
- 三种方法在三层上均达到相对残量 $10^{-6}$；
- adaptive 的循环数保持在 119--145，并在每层不高于另外两个比较对象；
- 三层结果仅作为固定物理几何的紧凑稳健性证据，不宣称证明渐近网格无关收敛。

## 5. 新增实验 7：三层 V-cycle

三层层次为 64/16/8（cross-channel）和 128/16/8（winding-ring），对比度均为 $10^4$。
每层使用一遍前向与一遍后向 Gauss--Seidel，最粗层精确 Cholesky；求解相对残量容差
为 $10^{-6}$。

| 层次 | 方法 | 精确两网格 | 三层 V-cycle | V/TG | 算子复杂度 | 状态 |
|---|---|---:|---:|---:|---:|---|
| 64/16/8 | adaptive | 65 | 101 | 1.554 | 3.0786 | converged |
| 64/16/8 | global-reference | 321 | 321 | 1.000 | 3.7064 | converged |
| 64/16/8 | geometric | 24950 | 30000 | 1.202 | 1.1128 | slow-limit |
| 128/16/8 | adaptive | 237 | 237 | 1.000 | 1.5379 | converged |
| 128/16/8 | global-reference | 768 | 770 | 1.003 | 1.6617 | converged |
| 128/16/8 | geometric | 30000 | 30000 | 1.000 | 1.0276 | slow-limit |

- adaptive 与 global-reference 均 2/2 收敛；adaptive 的 V-cycle 循环数为 reference 的
  31.5% 和 30.8%；
- adaptive 相对同 transfer 的精确两网格只在 64/16/8 增加到 1.554 倍，另一例完全
  保持 237 次；
- geometric 在两例均达到 30000 上限，记录为 `slow-limit`，未外推未观察循环数；
- 该实验不使用单次墙钟时间，也不宣称任意层数下的网格无关或线性复杂度。

## 6. 原有结果的保存与回归

- 实验 1--4 的正式 TXT 文件保持 v6.2.0 版本标记和既有内容，不为 v6.3 重复完整运行；
- v6.3 对 PCG 单步内核的重构用实验 1 quick 子集回归，adaptive/reference/geometric 的
  循环数和状态与 v6.2 正式结果逐项一致；
- `supplemental` 模式只运行实验 5--6，`multilevel` 只运行实验 7，`full` 可重现全部
  七个实验；
- 新实验以独立文件保存，没有覆盖实验 1--4 或 quick 开发结果。

## 7. 理论稿与研究方案检查

- PCG 能量账本、图空间投影、全局稳定界、局部 Pareto 律、反单调尾部和条件性步数
  尺度窗口保持，并新增精确两网格到递归 V-cycle 的恒等分解和逐层界；
- `theory.tex` 共 87 个唯一标签、60 个实际交叉引用，无缺失或重复标签；
- 所有 `\cite` 均有对应 `\bibitem`，无重复文献键；
- 在临时副本中只把缺失的 `ctexart` 文档类替换为标准 `article` 后，XeLaTeX 连续两遍
  通过，未发现未定义控制序列、公式环境错误或悬空引用；
- `research_report.md` 已同步多层理论、实验设计、完整结果和不过度外推的边界；
- README、代码版本、实验名称、容差、循环上限和新增结果相互一致。

## 8. 压缩包复验

最终 ZIP 在独立临时目录解包后执行目录清洁检查、严格构建、`unit_core`、
`regression_core`、脚本语法和结果文件存在性检查。压缩包不包含构建目录、临时日志或
Git 元数据。
