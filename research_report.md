# 多尺度有限 Krylov 能量插值研究方案 v4.7

## 1. 研究定位

本项目研究高对比扩散问题中，全局能量极小插值的有限 PCG 路径与实际两网格收敛
目标之间的非单调错位。中心现象是：插值能量可以随着 PCG 迭代严格下降，而固定
磨光器下的两网格循环数却可能先大幅下降、随后回升，甚至在全局能量极小终点显著变差。

研究集中回答三个问题：

1. 能量距离、粗空间几何和两网格谱因子之间有什么严格联系；
2. 有限 Krylov 误差在什么条件下能改善磨光后最坏误差模态；
3. 如何用一个统一的预算参数，在低 setup 与低循环数之间形成可解释取舍。

二维规则网格、固定粗点、对称 Gauss--Seidel、精确 Galerkin 粗校正和两网格结构构成
受控研究平台，使有限 Krylov 插值对粗空间质量的作用可以被单独识别。

## 2. 模型问题与比较对象

考虑

$$
-\nabla\cdot(a(x)\nabla u)=f\quad\text{in }\Omega=(0,1)^2,
\qquad u=0\quad\text{on }\partial\Omega,
$$

其中 $a(x)>0$，对比度取 $10^2$、$10^4$ 或 $10^6$。节点型有限差分离散得到
对称正定系统 $A_hu_h=b_h$。系数结构包括交叉、曲折、对角、平行、分支和闭合曲环
六类高导通道，并叠加由 seed 控制的块状高导背景。

按细点和粗点分块，写成

$$
A=\begin{bmatrix}B&C\\ C^T&A_{CC}\end{bmatrix},
\qquad
P(W)=\begin{bmatrix}W\\I\end{bmatrix}.
$$

比较三类插值：

1. `geometric`：规则粗点上的双线性几何插值 $P_G$；
2. `global-reference`：把全局能量方程求到相对欧氏残量 $10^{-10}$ 的高精度数值参考；
3. `adaptive-fast/reuse`：从 $P_G$ 出发，在有限全局 PCG 路径上选择中间点。

`global-reference` 表示相对欧氏残量达到 $10^{-10}$ 的高精度数值参考；理论中的精确
能量极小点记为 $W_*$。

## 3. 有限 Krylov 插值机制

能量泛函取

$$
J(W)=\frac12\operatorname{tr}\bigl(P(W)^TAP(W)\bigr).
$$

其唯一极小点与 Schur 补为

$$
W_*=-B^{-1}C,
\qquad
S=A_{CC}-C^TB^{-1}C,
$$

并有精确恒等式

$$
J(W)-J(W_*)=\frac12\lVert W-W_*\rVert_{B,F}^2,
\qquad
P(W)^TAP(W)=S+(W-W_*)^TB(W-W_*).
$$

因此，标准 PCG 生成的 $W_m$ 沿路径单调逼近 $W_*$。但两网格误差传播算子

$$
E_{TG}(W)=G^{\dagger_A}
\left[I-P(W)(P(W)^TAP(W))^{-1}P(W)^TA\right]G
$$

还取决于粗空间相对磨光后误差的方向。能量测量总体距离，最坏可见模态的捕获则由方向
决定。这一差异构成有限步插值优于能量终点的基本来源。

## 4. 统一的粗空间坐标

令

$$
Z=B^{1/2}(W-W_*)S^{-1/2}.
$$

这个单一坐标同时给出三类量：

$$
2\{J(W)-J(W_*)\}=\lVert ZS^{1/2}\rVert_F^2,
$$

$$
P(W)^TAP(W)=S^{1/2}(I+Z^TZ)S^{1/2},
$$

以及候选粗空间与能量极小粗空间之间的主角

$$
\tan\theta_i=\sigma_i(Z),
\qquad
\sin\theta_{\max}=\frac{\lVert Z\rVert_2}
{\sqrt{1+\lVert Z\rVert_2^2}}.
$$

在欧氏能量坐标中，若 $T=A^{1/2}GA^{-1/2}$，则

$$
\rho_{TG}(W)=\lVert(I-\Pi(Z))T\rVert_2^2.
$$

因此 $Z$ 把“PCG 能量尾”“粗空间扰动”和“两网格谱目标”放到同一条分析链上。
全局稳定界说明明显性能增益必须对应非零粗空间距离；终点附近的一阶公式则把性能变化
分解为磨光后最坏模态的可见性与实际 Krylov 误差方向的对齐。

## 5. 自适应选择方法

### 5.1 单参数预算

算法以预期右端项数 `expected_rhs_count = R` 作为策略参数。令

$$
d(R)=\operatorname{clip}\!\left(\frac{\log_2R}{8},0,1\right).
$$

默认最小步数 12、最大步数 60、量化步长 2，并取

$$
\begin{aligned}
\operatorname{stride}(R)&=Q_2(20-12d),\\
\overline m(R)&=Q_2(52+8d),\\
L(R)&=\operatorname{round}(16+144d),\\
\delta(R)&=0.10-0.08d.
\end{aligned}
$$

- `R=1` 的 fast 档检查 $m=0,12,32,52$，每个候选最多运行 16 次 pilot；
- `R=256` 的 reuse 档检查 $m=0,12,20,\ldots,60$，每个候选最多运行 160 次 pilot，并允许至多
  两个相邻 step--2 细化点；
- 中间 $R$ 连续改变候选密度、pilot 上限和局部细化预算。

小 $R$ 的目标是尽量降低端到端 setup；大 $R$ 则允许一次性支付更多选择成本，以换取
多个右端项上的较低循环数。`R` 直接表示工作负载中的复用程度。

### 5.2 稳健 pilot 与选择规则

每个候选使用同一个代表性 RHS 运行短两网格试验；若提前达到正式容差便立即停止。
记实际完成的 pilot 步数为 $\ell_m\le L(R)$。对已有残差历史后半段的连续对数残差比
取中位数和 MAD，构造

$$
\widehat\rho=
\exp\!\left(\min\{-10^{-10},c+0.25s_{\mathrm{MAD}}\}\right),
$$

再从 $\ell_m$ 及其末端相对残量外推达到容差所需的循环数 $\widehat N_m$；已在 pilot
内收敛时直接取 $\widehat N_m=\ell_m$。从

$$
\widehat N_m\le(1+\delta(R))\min_j\widehat N_j
$$

的候选中选择最小 $m$。规则先保持预测循环数近优，再偏向较早、较稀疏的路径点。
尺度、对比度和拓扑变化通过实际残量历史进入选择过程。

若候选上的预测误差统一满足
$|\widehat N_m-N_m|\le\varepsilon$，则选中点满足

$$
N_{\widehat m}\le
(1+\delta)N_{m_*}+(2+\delta)\varepsilon.
$$

该界定量描述 pilot 预测误差向选择损失的传递。fast 用较大的预测误差预算换取较低
setup；reuse 用更长 pilot 和局部细化追求更小的循环数。

## 6. 数值实验设计

所有方法在每个问题上使用相同矩阵、粗点、右端项、前后 Gauss--Seidel 和 Galerkin
粗算子。正式求解均从零初值开始，停止准则为相对欧氏残量 $10^{-6}$，循环上限提高到
12000。

核心证据分为四组：

1. 13 个问题比较尺寸、对比度和通道拓扑，其中 160/16、192/16 构成大尺度扩展；
2. 3 条完整有限 PCG 路径直接比较能量超额与循环数；
3. 7 个问题上的 step--2 离线 oracle 评价选择损失；
4. 5 个系数 seed、6 个迁移 RHS 和 5 次重复计时检验投稿前稳健性。

循环数和收敛状态是主要结果；密度、setup 和 solve 时间用于说明成本结构。达到上限时，
尾部仍收缩的结果标为 `slow-limit`，残量非有限或持续增长的结果标为 `diverged`。
逐行 solve/total 时间仅对已收敛求解给出，跨方法计时汇总采用四种方法共同收敛的子集。
固定问题上的重复计时报告 Q1、中位数和 Q3。

## 7. 跨尺寸、对比度与拓扑的主比较

以 $(1/h,1/H)=(128,16)$、对比度 $10^4$、cross-channel 为中心，中心问题只计算一次。

| 轴 | $1/h,1/H$ | 对比度 | 拓扑 | fast `m/cycles` | reuse `m/cycles` | reference cycles |
|---|---|---:|---|---:|---:|---:|
| size | 32,8 | $10^4$ | cross | 0 / 40 | 0 / 40 | 54 |
| size | 64,8 | $10^4$ | cross | 12 / 204 | 12 / 204 | 264 |
| size | 64,16 | $10^4$ | cross | 32 / 114 | 18 / 54 | 321 |
| center | 128,16 | $10^4$ | cross | 52 / 276 | 42 / 238 | 3227 |
| large-scale | 160,16 | $10^4$ | cross | 52 / 379 | 50 / 376 | 5462 |
| large-scale | 192,16 | $10^4$ | cross | 52 / 1011 | 60 / 545 | 7847 |
| contrast | 128,16 | $10^2$ | cross | 32 / 293 | 20 / 228 | 756 |
| contrast | 128,16 | $10^6$ | cross | 52 / 298 | 52 / 298 | 3471 |
| topology | 128,16 | $10^4$ | meandering | 52 / 336 | 36 / 272 | 759 |
| topology | 128,16 | $10^4$ | diagonal | 52 / 351 | 36 / 297 | 418 |
| topology | 128,16 | $10^4$ | parallel | 52 / 363 | 42 / 251 | 421 |
| topology | 128,16 | $10^4$ | branching | 32 / 347 | 36 / 267 | 615 |
| topology | 128,16 | $10^4$ | winding-ring | 52 / 228 | 44 / 233 | 768 |

汇总循环数为 fast 4240、reuse 3303、global-reference 24383；三者均 13/13 收敛。
几何插值为 3 个收敛、10 个 `slow-limit`、0 个 `diverged`。reference 的累计循环数
分别是 fast 和 reuse 的 5.75 倍与 7.38 倍。160/16 上 reference/fast 为 14.41，
192/16 上 reference/reuse 为 14.40；大尺度扩展延续了有限步优势，同时显示 fast 在
192/16 上的粗候选判断会带来 1011 次循环，reuse 则为 545 次。

fast 相对 reuse 的循环总和增加 28.4%，同时保持四个候选和较短 pilot。
它适合少 RHS 的端到端成本目标；reuse 则在右端项复用时更有价值。

时间汇总采用四种方法共同收敛的三个算例，因而每一列时间都对应相同问题和相同停止
容差；十个几何截断求解由状态与末端残量单独描述。固定问题的重复计时见第 10.3 节。

| 方法 | setup / ms | 单 RHS solve / ms | $R=1$ 总时间 / ms | $R=256$ 估计总时间 / ms |
|---|---:|---:|---:|---:|
| adaptive-fast | 631.851 | 247.270 | 879.121 | 63933.042 |
| adaptive-reuse | 2836.216 | 156.993 | 2993.209 | 43026.520 |
| global-reference | 2486.882 | 3224.483 | 5711.364 | 827954.417 |
| geometric | 2.298 | 743.757 | 746.055 | 190404.158 |

共同收敛子集上，几何插值凭借极低 setup 获得最小的单 RHS 总时间；当插值被 256 个
RHS 复用时，reuse 的估计总时间最低。fast 的 setup 比 reuse 少 77.7%，而 reuse 的
求解时间更低，体现同一选择器中候选预算与 pilot 深度的工作量取舍。

## 8. 有限路径的直接证据

| 问题 | 有限最优点 | 有限循环 | reference 循环 | 归一化能量超额 |
|---|---:|---:|---:|---:|
| 64/16, cross, $10^4$ | $m=20$ | 68 | 321 | $3.164\times10^{-4}$ |
| 128/16, cross, $10^4$ | $m=44$ | 244 | 3227 | $2.006\times10^{-4}$ |
| 128/16, cross, $10^6$ | $m=60$ | 279 | 3471 | $6.152\times10^{-5}$ |

完整路径上能量超额持续下降，但循环数可以在有限区间达到最小后回升。例如
$128/16$、$10^4$ 的循环数从 $m=44$ 的 244 上升到 $m=60$ 的 360，最终数值参考为
3227。能量与循环数沿同一路径的直接对照构成研究现象的核心数值证据。

## 9. 离线 oracle 选择质量

oracle 独立求解 $m=0$ 和 $m=12,14,\ldots,60$，作为在线选择质量的离线评价基准。

| 问题 | fast `m/cycles/gap` | reuse `m/cycles/gap` | oracle `m/cycles` |
|---|---:|---:|---:|
| 32/8, $10^4$, cross | 0 / 40 / 0.00% | 0 / 40 / 0.00% | 0 / 40 |
| 64/8, $10^4$, cross | 12 / 204 / 0.00% | 12 / 204 / 0.00% | 12 / 204 |
| 64/16, $10^4$, cross | 32 / 114 / 111.11% | 18 / 54 / 0.00% | 18 / 54 |
| 128/16, $10^4$, cross | 52 / 276 / 19.48% | 42 / 238 / 3.03% | 38 / 231 |
| 128/16, $10^2$, cross | 32 / 293 / 29.65% | 20 / 228 / 0.88% | 26 / 226 |
| 128/16, $10^6$, cross | 52 / 298 / 6.81% | 52 / 298 / 6.81% | 60 / 279 |
| 128/16, $10^4$, ring | 52 / 228 / 0.00% | 44 / 233 / 2.19% | 52 / 228 |

fast 的平均/最大 gap 为 23.86%/111.11%，reuse 为 1.84%/6.81%。fast 的最坏 gap
来自 $64/16$ 问题，但 114 次仍少于 reference 的 321 次。这一结果清楚刻画两档策略：
fast 强调选择成本，reuse 强调接近 oracle 的循环数。

## 10. 系数 seed、多 RHS 与重复计时

### 10.1 五个系数 realization

| 方法 | 收敛/慢收敛/发散 | 已收敛循环总和 | 已收敛平均 | 已收敛最坏 |
|---|---:|---:|---:|---:|
| adaptive-fast | 5/0/0 | 457 | 91.40 | 114 |
| adaptive-reuse | 5/0/0 | 281 | 56.20 | 63 |
| global-reference | 5/0/0 | 866 | 173.20 | 321 |

seed 3 上 fast 与 reference 同为 95 次，展示有限步优势的 realization 依赖性；五个
seed 的总体结果同时给出跨 realization 的稳定性证据。

### 10.2 固定插值的 RHS 迁移

候选仅由常数 RHS 选择一次，再原样用于常数、两个光滑 RHS、左右局部 RHS 和振荡 RHS。

| 方法 | 收敛/慢收敛/发散 | 已收敛循环总和 | 已收敛平均 | 已收敛最坏 |
|---|---:|---:|---:|---:|
| adaptive-fast | 6/0/0 | 655 | 109.17 | 119 |
| adaptive-reuse | 6/0/0 | 323 | 53.83 | 59 |
| global-reference | 6/0/0 | 1507 | 251.17 | 328 |
| geometric | 0/6/0 | 0 | -- | -- |

localized-left 上 reference 需 42 次，优于 fast 的 89 次和 reuse 的 53 次。这组结果
一方面支持测试族上的整体迁移能力，另一方面展示 RHS 分量对实际循环数的影响。

### 10.3 工作量盈亏平衡

若两种策略的 setup 和单 RHS 求解时间分别为 $(S_a,T_a)$ 与 $(S_b,T_b)$，则

$$
C_a(R)=S_a+RT_a,
\qquad
R_{\mathrm{break}}=\frac{S_b-S_a}{T_a-T_b}.
$$

固定 $64/16$ 问题上的五次交替顺序计时报告中位数与四分位数，并据此估计 fast/reuse
的实际盈亏平衡：fast setup 为 208.476 ms（Q1--Q3：198.707--219.751 ms），reuse
为 1016.957 ms（996.628--1040.430 ms）；单 RHS 求解中位数分别为 44.165 ms 与
13.515 ms。代入上式得到 $R_{\mathrm{break}}=26.38$。该数值是当前机器与固定 workload
上的实测盈亏平衡点，算法内部仍直接使用用户给定的 $R$。

## 11. 理论贡献与算法价值

理论贡献应按以下层次表述：

1. 用 PCG 正交能量尾严格刻画有限插值路径到能量极小终点的距离；
2. 用归一化误差 $Z$ 同时连接能量、Galerkin 粗矩阵和粗空间主角；
3. 给出对称两网格谱目标的投影表达及其对粗空间扰动的全局稳定界；
4. 将终点附近的性能变化分解为“问题可见性 × Krylov 路径对齐”；
5. 在显式余项和逐步方向条件下证明能量下降而两网格因子上升的严格局部尾部；
6. 用单参数预算选择器把上述机制转化为可复现实验算法，并给出条件性选择后悔界。

能量极小插值、最佳同维投影和主角恒等式提供理论基础。本文用同一个 $Z$ 坐标把它们
闭合到有限 PCG 路径，并由此解释、证明和选择非单调两网格优势区间。

## 12. 适用范围与投稿表述

- 有限步优势由粗空间可见性与 Krylov 路径对齐共同决定，理论给出局部充分条件；
- 最坏两网格谱因子控制循环数上界，给定 RHS 的谱分量决定实际循环；
- 代表性 RHS 的适用性通过六类 RHS 迁移实验直接评估；
- fast 与 reuse 分别面向低 setup 和多 RHS 复用，两档结果同时报告；
- 循环数、收敛状态和选择预算构成主证据，重复 wall-clock 刻画固定工作负载成本；
- 二维规则网格、固定粗点和两网格结构提供隔离插值机制的受控验证环境。

面向 Journal of Scientific Computing，论文应突出有限 Krylov 插值的目标错位机制、
严格条件性理论和受控跨问题验证。正式投稿剩余工作主要是英文论文组织、图表制作、
独立逐式数学复核与语言审校。后续工作可沿更一般离散结构与多层机制展开。

## 13. 复现

在 `code` 目录执行：

```bash
./scripts/run_validation.sh quick
./scripts/run_validation.sh full
```

脚本优先使用 CMake，并提供等价的严格 C++17 直接构建路径。四份完整逐行结果位于
`code/results`；`full` 生成正式研究结果，`quick` 用于开发检查。
