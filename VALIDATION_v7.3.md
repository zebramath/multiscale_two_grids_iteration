# multiscale_two_grids_iteration v7.3 验证记录

验证日期：2026-09-01  
数值源码版本：7.3.0  
理论与研究方案版本：7.3

## 1. 变更边界

v7.3 仅收紧理论和方法定位，不改变数值算法、实验矩阵、参数或既有结果：

1. 删除固定有限维 PCG 的 $m\to\infty$ 渐近相干推论，并同步删除 abstract、README、
   研究方案和 conclusion 中的相应外推；保留有限相邻步上可直接核验的严格反单调充分条件；
2. 将原先关于最优步数的条件性 $\Theta(h^{-1})$ 双侧窗口收紧为：在
   $\kappa_h=O(h^{-2})$ 条件下，达到固定能量误差比例的充分步数为 $m=O(h^{-1})$；
3. 将 adaptive 明确定位为
   $\boxed{\text{theory-informed lightweight empirical selector}}$；
   $1/8,1/4,1/3,1/2$ 和 $10^3,10^5$ 均标记为经验参数，不宣称最优或 near-oracle；
4. 窗口受限 sampled oracle 只作为离线描述性比较基准，不进入在线选择，也不构成参数
   最优性证明。

## 2. 数值结果继承

代码中的数值算法与实验参数相对 v7.2 不变。按交付要求，本轮不重新运行完整实验；
`code/results` 中七份 TXT 原样继承自 v7.2.0 最终源码的完整 4 线程复跑。结果中的版本
标识因此仍为 7.2.0（若有），这是有意保留的运行来源记录，而不是版本遗漏。

## 3. 本轮验证

- 严格构建七个正式实验，启用 `-Wall -Wextra -Wpedantic -Werror` 及 GCC 扩展告警；
- 运行 `./scripts/run_validation.sh quick`，确认 7.3.0 文本修订未破坏端到端流程；
- 检查 `theory.tex` 的标签、交叉引用、文献引用和 LaTeX 结构；
- 全项目检索并确认不再包含被删除的渐近相干推论及其标签/公式引用；
- 检查 abstract、README、研究方案、conclusion 和代码说明中的 adaptive 定位一致性；
- 独立解包最终 ZIP，并从解包副本复验版本、目录清洁性、脚本语法与严格构建。

## 4. 保留边界

- $m=O(h^{-1})$ 只是达到固定能量误差比例的充分尺度，不是两网格最优步数定理；
- 经验选择器在当前测试集上的良好结果不外推为参数最优性或 near-oracle 性质；
- 三层实验仍只支持有限案例中的递归可行性，不外推任意层数的网格无关收敛或线性复杂度。
