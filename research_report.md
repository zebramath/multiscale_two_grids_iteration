# 多尺度两网格能量插值研究方案 v3.5

## 1. 项目概述

本项目研究二维高对比扩散方程

$$
-\nabla\cdot(a(x)\nabla u)=f
$$

在规则网格、齐次 Dirichlet 边界、固定几何粗点下的两网格插值构造。离散矩阵记为 $A\succ0$，两网格循环采用一次前向 Gauss--Seidel、Galerkin 粗校正和一次后向 Gauss--Seidel，停止准则为相对残差 $10^{-6}$。

研究的核心不是单独追求插值列能量最小，而是在以下三项之间取得可计算的平衡：

1. 插值算子的构造成本与稀疏度；
2. 两网格求解所需循环数；
3. 层次在多个右端项上的可复用性。

数值结果表明，精确全局能量最小插值并不必然产生最优两网格收敛。在主交叉通道问题上，精确全局插值需要 3227 次循环，而有限 PCG 路径在 $m=38$ 时只需 231 次。有限步插值保留的特定慢模态分量可能改善粗空间方向，继续把能量误差压到零反而会离开有利窗口。

当前实用方案是一种低预算自适应有限 PCG 构造：复用一条增量 PCG 路径，只建立几何初值、最小步锚点、区间中点以及至多一个二分细化候选；每个候选只做 24 步两网格 pilot，不做 setup 阶段的完整收敛确认。算法不读取系数场类型、对比度、随机种子或预先给定的最优步数。

## 2. 研究问题与评价口径

### 2.1 离散问题

按自由点 $F$ 和粗点 $C$ 分块：

$$
A=
\begin{bmatrix}
B&C\\
C^T&A_{CC}
\end{bmatrix},\qquad B=A_{FF}\succ0.
$$

保持粗点单位注入的插值写为

$$
P(W)=\begin{bmatrix}W\\I\end{bmatrix}.
$$

项目提供连续随机场、棋盘场以及交叉、蜿蜒、对角和平行通道场，覆盖对比度 $10^2$、$10^4$、$10^6$，并测试不同 $H/h$、网格尺度和随机种子。

### 2.2 两网格算子

Galerkin 粗算子为

$$
A_c=P^TAP,
$$

粗校正投影为

$$
Q(P)=I-P(P^TAP)^{-1}P^TA.
$$

若对称平滑误差传播算子记为 $S$，则两网格误差传播算子为

$$
E_{TG}(P)=S^TQ(P)S.
$$

理论分析研究其谱半径及扰动；数值实验公开报告给定右端项下的实际循环数。二者相关但不等价，因此所有自适应候选最终都由独立完整求解测量真实 `Cycles`，pilot 预测只参与选择。

### 2.3 公开指标

| 指标 | 含义 |
|---|---|
| `P density %` | 插值非零元数占稠密矩阵规模的百分比 |
| `Setup ms` | 插值、Galerkin 粗算子和选择所需的一次性时间 |
| `Total ms` | setup 加单右端项完整求解时间 |
| `Cycles` | 相对残差达到 $10^{-6}$ 的两网格循环数 |
| `Gap %` | 自适应循环数相对离线候选 oracle 的增幅 |
| `Break-even` | setup 与平均单右端项时间合计优于基准所需的最少右端项数 |

墙钟时间均为当前环境中的单次测量，适合同一实验内比较，不作为跨机器绝对性能结论。

## 3. 理论基础

`theory.tex` 给出当前项目的完整理论分析。本节提炼算法和数值实验直接使用的结论。

### 3.1 能量最小插值

总列能量定义为

$$
J(W)=\frac12\operatorname{tr}\big(P(W)^TAP(W)\big).
$$

其唯一全局极小点满足

$$
BW_*=-C,\qquad W_*=-B^{-1}C.
$$

对任意近似 $W=W_*+E$，有精确恒等式

$$
J(W)-J(W_*)=\frac12\lVert E\rVert_{B,F}^2.
$$

因此局部能量插值、有限步全局 PCG 和精确全局插值都可以理解为对同一矩阵方程的不同近似，而不是互不相关的方法。

### 3.2 空间局部化与支撑残差

每个插值列在给定局部支撑上解 $B$ 的主子系统。局部解是该支撑空间中的最佳能量逼近；在对角缩放条件数有限时，随图邻域层数增加，局部解以 Chebyshev 因子

$$
q=\frac{\sqrt{\widehat\kappa}-1}
{\sqrt{\widehat\kappa}+1}
$$

指数逼近全局能量列。嵌套支撑扩张具有精确能量下降恒等式，支撑外残差又给出局部化误差的对偶能量表示。这说明支撑扩张应优先补充残差能量集中的图方向，而不只是机械增加邻域面积。

### 3.3 有限 PCG 路径

从几何插值 $W_0$ 出发，对所有粗基列求解

$$
BW=-C
$$

并在共同步数 $m$ 取出 $W_m$。PCG 能量误差随步数下降，且误差具有正交尾分解；项目在检查点计算归一化预条件能量残差

$$
\eta_E(m)=
\left(
\frac{\sum_j r_{j,m}^TD^{-1}r_{j,m}}
{\sum_j r_{j,0}^TD^{-1}r_{j,0}}
\right)^{1/2},\qquad D=\operatorname{diag}(B).
$$

$\eta_E$ 是无量纲路径指标，反映所有列在对角预条件度量中的剩余误差。它不被假设为两网格循环数的单调替代量，只用于识别 PCG 路径是否已经进入低能量残差区。

### 3.4 两网格非单调性的机制

两网格收敛取决于 $\operatorname{Ran}(P)$ 是否覆盖平滑器留下的慢模态，而不仅取决于 $J(W)$。`theory.tex` 给出：

- 两网格谱半径的精确表示；
- 任意两个候选的 LMI 比较条件；
- 相邻 PCG 步严格恶化的标量证书；
- 精确能量空间附近的一阶方向导数和有限二阶余项；
- 通道低谱模态、Ritz 分离与有限步优势的充分条件；
- 有限幅度主模态抵消机制。

这些结论解释了 $m\mapsto\rho_{TG}(P_m)$ 和实际循环数可能出现明显非单调窗口。主问题中 $m=38$ 最优、$m=64$ 变差、精确全局极限严重变差，正是该现象的数值实例。

### 3.5 剪枝与稳定性

对一般插值扰动 $P\mapsto P+\Delta P$，理论给出能量增量和两网格谱半径关于图参数的稳定性界，并据此得到带谱裕量的安全剪枝条件。数值实验表明，幅值剪枝可以大幅降低密度，但不能保证修复粗空间方向：连续场在 `drop=1e-4` 时密度从 95.72% 降到 16.39% 而循环数保持 90；通道场同样剪枝后仍约 3226 次循环。

### 3.6 预测损失与多右端项成本

若每个候选的预测循环数满足相对误差界 $\delta$，选择器在预测空间内允许相对松弛 $\gamma$，则条件近似最优界为

$$
N_{sel}\le
\frac{(1+\gamma)(1+\delta)}{1-\delta}N_*.
$$

当前实现取 $\gamma=0.08$，但尚未对所有问题证明统一的 $\delta$，因此离线 oracle gap 是经验质量评估，不是无条件保证。

设某层次的一次 setup 为 $S$，平均单右端项求解时间为 $Q$。相对基准 $(S_0,Q_0)$ 的 break-even 满足

$$
R>\frac{S-S_0}{Q_0-Q},
$$

且只有 $Q<Q_0$ 时才存在有限回本点。

## 4. 当前插值算法体系

### 4.1 几何基准

`GeometricBilinear` 使用固定粗点上的双线性插值，构造便宜、密度低，是所有能量改进和自适应 PCG 的共同初值。它在平滑连续问题上有效，但在部分高对比通道问题上会达到最大循环数仍不收敛。

### 4.2 局部能量插值

局部算法在每个粗基函数的有限支撑上求能量极小列。项目保留三类支撑研究：

- 固定几何层数；
- 强连接图引导的一次扩张；
- 残差能量预算驱动的多轮扩张。

这些方法用于验证空间局部化理论和支撑拓扑的重要性，不与当前全局 PCG 自适应器混为同一策略。

### 4.3 全局有限 PCG

`GlobalEnergyPcgPath` 为每个粗基列保存 PCG 状态，并允许从较小检查点继续推进到较大检查点。候选 $P_m$ 共享几何初值和已经完成的 Krylov 工作，避免为每个 $m$ 从头构造。精确全局插值只作为极限参考，不作为默认实用方案。

### 4.4 自适应有限 PCG

默认参数为 `m_min=12`、`m_max=64`、步长量化 2、pilot 24 步、尾部窗口 8、选择松弛 8%。主 `128/16` 实验把 `m_min` 设为 16，部分矩阵实验把 `m_max` 设为 56；其余规则保持一致。

对候选 pilot 残差范数 $e_k$，算法在尾部计算重叠块对数衰减率

$$
g_j=\frac1b\log\frac{e_j}{e_{j-b}}.
$$

使用最慢观测率与 MAD 不确定度形成保守 $\widehat\rho$，预测达到容差所需循环数

$$
\widehat N=k+
\left\lceil
\frac{\log(\varepsilon/(e_k/e_0))}{\log\widehat\rho}
\right\rceil.
$$

候选生成规则如下：

1. 对几何插值做 pilot；若预测不超过两倍 pilot 长度，直接接受。
2. 沿增量 PCG 路径推进到 `m_min`，建立锚点；若锚点已满足相同容易阈值，直接接受。
3. 计算 `[m_min,m_max]` 的量化中点并做 pilot。
4. 若中点预测仍超过“容易阈值加一个 pilot 长度”，且 $\eta_E\le\sqrt\varepsilon$，在锚点与中点之间二分一次。这表示 PCG 方程已较充分收敛，但两网格 pilot 尚不理想，较小步数可能位于有利窗口。
5. 否则，若中点预测超过容易阈值的四倍，在中点与 `m_max` 之间二分一次。
6. 在最小预测的 8% 范围内选择最小 $m$。若全部预测达到硬上限，只按最小 $\eta_E$ 裁决。

该策略至多建立四个两网格层次，没有割线外推、多阶段状态机或完整确认求解。向后细分判据中的 $\sqrt\varepsilon$ 直接来自求解容差，不增加问题类别相关参数。保存的向后中点只在确定需要时才建立 Galerkin 层次和执行 pilot。

## 5. 实现结构与效率

| 文件 | 职责 |
|---|---|
| `core/linear_algebra.hpp` | 向量、CSR 运算、稀疏 Cholesky 和并行基础操作 |
| `pde/diffusion_problem.hpp` | 结构网格、系数场与扩散矩阵组装 |
| `multigrid/energy_interpolation.hpp` | 几何、局部、全局能量插值，以及强连接距离和参考剪枝 |
| `multigrid/support_expansion.hpp` | 固定强连接扩张与残差预算支撑构造 |
| `multigrid/global_pcg.hpp` | 增量 PCG 路径、能量残差指标和自适应检查点选择 |
| `multigrid/two_grid_solver.hpp` | Galerkin 粗算子、对称平滑、粗解与循环 |
| `experiment/*.hpp` | 统一问题、计时、评估、内部 study 和整合报告接口 |

当前效率措施包括：

- PCG 检查点共享同一条路径，不重复前序 Krylov 步；
- pilot 构造的两网格层次直接保留到最终选择，不重复 Galerkin setup；
- 向后候选的插值快照只保存矩阵，不提前构造粗层次；
- CSR 乘法、转置、Galerkin 乘积和列 PCG 使用 OpenMP 或线程并行；
- 热路径不保留重复尺寸、排列和选项检查，调用者负责合法输入；
- SPD 分解失败、PCG 数值 breakdown 和显式要求收敛但未收敛等数学有效性检查仍保留；
- 源码中无解释性注释，算法说明集中在本报告和实验输出中。

## 6. 数值实验设计

完整测试包含 6 个整合实验和 2 个测试程序。原有 13 个数值测试角度均保留为内部 study，但只通过 6 个连续编号的入口运行，并各自产生一份综合 TXT。默认大问题为 `h=1/128`、`H=1/16`、对比度 `1e4`，除特别说明外使用 4 线程、常数右端项和最多 40000 次循环。

| 实验 | 研究角度 | 当前重要结果 |
|---|---|---|
| 1：局部化 | 支撑半径、局部 PCG 容差、全局剪枝、支撑扩张 | 通道 layers 2/3/4/global 为 6840/6050/5275/3227 cycles；adaptive-budget 为 3553 |
| 2：PCG 路径 | 有限步预算、步长 2 密集扫描、pilot 排序 | 主通道最优 `m=38, 231`；`m=40, 234`；全部粗解通过 long-double 交叉验证 |
| 3：自适应与 oracle | 主问题决策轨迹、6 问题 step-2 oracle | 主问题选择 `m=40, 234`，gap 1.30%；六问题平均 gap 7.81% |
| 4：鲁棒性 | 18 问题网格、对比度、种子与拓扑矩阵 | 自适应 18/18 收敛，总 cycles 1858；固定 `m=40` 为 2586 |
| 5：附加诊断 | 8 问题附加组和 8 问题压力组 | 平均 gap 4.77% 与 3.51%；最大 gap 13.79% 与 28.10% |
| 6：多 RHS | 4 个问题、每个 5 个右端项的摊销 | 相对几何基准均 1 RHS 回本；相对固定 `m=40` 为 34/35/5/74 RHS |

全部原始数值位于 `code/results/`。每个整合实验对应一份完整 TXT；项目不再生成或保留 CSV。

## 7. 关键数值结果

### 7.1 主交叉通道的有限步窗口

| 方法 | m | P density % | Setup ms | Cycles | 相对最优 gap |
|---|---:|---:|---:|---:|---:|
| 固定 PCG | 32 | 20.4547 | 959.452 | 347 | 50.22% |
| 固定 PCG | 36 | 23.5999 | 1034.516 | 268 | 16.02% |
| 离线最优 | 38 | 25.2786 | 1027.978 | 231 | 0.00% |
| 固定 PCG | 40 | 26.9969 | 1187.158 | 234 | 1.30% |
| 固定 PCG | 48 | 33.8960 | 1231.052 | 256 | 10.82% |
| 自适应 | 40 | 26.9969 | 1479.502 | 234 | 1.30% |

自适应决策轨迹为：

| 阶段 | m | $\widehat\rho$ | pilot 相对残差 | $\eta_E$ | 预测 cycles | 选择 |
|---|---:|---:|---:|---:|---:|---|
| initial | 0 | 0.994006 | 5.654 | 1.000 | 2611 | 否 |
| anchor | 16 | 0.990369 | 23.64 | 0.1101 | 1779 | 否 |
| midpoint | 40 | 0.912467 | 3.438 | 0.01112 | 189 | 是 |

该案例不触发第四候选，较复杂的额外搜索被省去。预测 189 低于实测 234，说明短 pilot 仍有系统误差；但候选排序正确并选中接近最优的窗口。

### 7.2 代表性 step-2 oracle

| 网格 | 对比度 | 拓扑 | 自适应 m/cycles | oracle m/cycles | Gap |
|---|---:|---|---:|---:|---:|
| 32/8 | $10^2$ | cross | 0/38 | 0/38 | 0.00% |
| 32/8 | $10^4$ | parallel | 12/54 | 20/45 | 20.00% |
| 64/8 | $10^2$ | diagonal | 12/228 | 12/228 | 0.00% |
| 64/8 | $10^6$ | parallel | 12/160 | 22/144 | 11.11% |
| 64/16 | $10^4$ | diagonal | 34/51 | 30/50 | 2.00% |
| 64/16 | $10^6$ | meandering | 34/58 | 40/51 | 13.73% |

六问题平均 gap 为 7.81%。最大 20% 出现在小网格平行通道，但绝对差为 9 个循环。自适应器的目标是用不超过四个层次逼近密集 oracle，而不是复制离线穷举。

### 7.3 附加诊断和压力测试

| 数据组 | 问题数 | 平均 Gap | 最大 Gap | 零 Gap 问题数 |
|---|---:|---:|---:|---:|
| step-2 代表组 | 6 | 7.81% | 20.00% | 2 |
| 附加种子与场 | 8 | 4.77% | 13.79% | 4 |
| 压力测试 | 8 | 3.51% | 28.10% | 7 |

压力测试的唯一明显偏差是 `64/8, contrast=1e4, cross-channel`：自适应 `m=46` 为 269 次，oracle `m=36` 为 210 次。该结果说明极少候选无法覆盖所有非单调曲线，也界定了当前简化策略的适用边界。

### 7.4 18 问题鲁棒性矩阵

| 方法 | 收敛问题数 | 总 Cycles | 说明 |
|---|---:|---:|---|
| 自适应有限 PCG | 18/18 | 1858 | 同一策略覆盖 3 个网格、3 个对比度、2 个种子和 4 类通道 |
| 固定 `m=40` | 18/18 | 2586 | 在部分小网格上过度构造并增加循环数 |
| 几何插值 | 14/18 | 4242 | 总数仅统计已收敛问题，另有 4 个问题达到 12000 次上限 |

自适应相对固定 `m=40` 减少 728 个循环，即 28.15%。该矩阵用于检验统一策略的稳健性，而不是针对单一主问题调参。

### 7.5 多右端项工作量

| 网格/拓扑 | 自适应 m | 一次 Setup ms | 平均 Cycles | 对几何回本 | 对 `m=40` 回本 |
|---|---:|---:|---:|---:|---:|
| 64/8 meandering | 38 | 129.147 | 189.6 | 2 RHS | 34 RHS |
| 64/16 diagonal | 38 | 400.850 | 49.4 | 1 RHS | 35 RHS |
| 64/16 cross, $10^6$ | 26 | 447.283 | 59.4 | 1 RHS | 5 RHS |
| 128/16 cross | 40 | 1470.724 | 231.4 | 1 RHS | 74 RHS |

当固定 `m=40` 已经接近最优且单 RHS 求解时间相近，自适应选择成本需要较多 RHS 才能回收；当固定步数明显过度构造或产生较慢层次时，自适应可在少量 RHS 后回本。相对几何层次，三个问题在第一个 RHS 回本，`64/8` 蜿蜒通道在第二个 RHS 回本。

## 8. 验证状态

- 使用 C++17、`-O3 -DNDEBUG -pthread -fopenmp` 编译全部 6 个整合实验和 2 个测试程序；
- 使用 `-Wall -Wextra -Wpedantic -Werror` 编译通过，无警告；
- `unit_core` 和 `regression_v3` 均通过；
- `experiment1_localization` 至 `experiment6_workload` 全部由当前源码重新运行，并生成版本 3.5.0 的 6 份 TXT；
- `experiment2_pcg_path` 的所有密集扫描候选均通过独立 long-double 稠密 Cholesky 粗解交叉验证；
- results 目录及全部源码均不包含 CSV 文件或 CSV 写出接口；
- CMake 项目版本和测试目标已同步为 3.5.0；当前执行环境未提供 CMake，因此本次实际验证采用等价的直接 `g++` 编译命令；
- `theory.tex` 保持不变。

## 9. 结论、边界与后续工作

当前项目得到以下结论：

1. 插值列能量、稀疏度和两网格收敛不能由单一标量统一排序；粗空间相对慢模态的方向是关键。
2. 局部能量插值能系统改善几何基准，但在高对比通道上仅增加局部精度常常收益有限。
3. 有限全局 PCG 可经过远优于精确全局能量极限的窗口，密集步长实验和独立粗解检查排除了明显实现假象。
4. 当前中点自适应器用至多四个层次即可在主问题上达到 1.30% oracle gap，并在 18 问题矩阵上显著优于固定 `m=40` 的总循环数。
5. PCG 能量残差适合作为搜索区间诊断，但不能单独替代两网格 pilot；真实选择质量仍需由 oracle 诊断集评估。

适用边界包括：

- 当前只验证二维结构网格、固定几何粗点和两网格算法；
- 选择阶段使用一个代表右端项，多个右端项的共同最优层次可能不同；
- pilot 预测尚无对所有问题统一成立的在线误差上界；
- 最坏压力问题仍有 28.10% gap，表明非单调曲线可能需要更强的慢模态指标；
- 尚未与成熟 AMG 软件在统一硬件、容差和计时口径下做外部基准。

后续研究应优先发展可廉价计算的慢模态投影或 Ritz 分离指标，并尝试为 pilot 预测误差建立在线置信界。若这些量不能显著改善最坏 gap，就不应继续增加经验候选和分支，以免牺牲当前算法的简洁性与 setup 优势。
