# 多尺度两网格能量插值研究方案 v3.3

## 1. 摘要

本项目研究二维高对比扩散方程离散系统

\[
-\nabla\cdot(a(x)\nabla u)=f,
\]

在固定几何粗点、Galerkin 粗算子和一次前向加一次后向 Gauss--Seidel 平滑下的两网格插值构造。核心现象是：精确全局能量最小插值并不必然给出最好的两网格收敛；有限步 PCG 产生的插值可能通过慢模态方向上的有限振幅抵消显著优于其极限。

v3.3 将自适应算法改为低预算、多保真、数据驱动的搜索。算法复用一条增量式全局 PCG 路径，最多建立四个候选层次，不把任何候选确认到真实求解容差。候选位置由短 pilot 的实测收敛斜率、预测不确定度和 PCG 预条件残差能量共同决定，不再固定押注 `m=16/40`，也不按系数场、网格或种子硬编码规则。

在主 `128/16` 交叉通道问题上，v3.3 选择 `m=40`、234次循环；步长2 oracle 为 `m=38`、231次，差距1.30%。选择 Setup 约449 ms。六问题步长2 oracle 平均 gap 8.33%、最大14.00%；额外诊断集平均3.04%、最大12.05%；压力集平均4.63%、最大28.10%。

## 2. 历史版本的有效结论

| 版本 | 保留下来的核心认识 |
|---|---|
| v2.1 | 插值支撑拓扑比单纯增加非零元更重要；放弃不稳定的 ML/LibTorch 路线 |
| v2.2 | 残差预算支撑、多 RHS、参数扫描和可重复测试框架 |
| v2.3 | 所有方法统一为理想能量插值的不同稀疏近似；能量误差不能排序两网格收敛 |
| v2.4 | 三重局部化 `P_{S,m,δ}`：空间支撑、代数迭代、稀疏化 |
| v2.5--v2.6 | 固定大规模实验口径、几何基准、Jacobi/PCG/精确全局对照 |
| v2.7--v2.8 | 问题、构造、评估、报告模块化；指标收敛到密度、Setup、Total、Cycles |
| v2.9 | 步长2密集扫描和独立 long-double 粗解交叉验证揭示非单调窗口 |
| v2.10 | 修正固定步语义和底层线性代数边界行为 |
| v2.11 | 全部实验改用常数 RHS，删除随机系数场下不成立的解析真解假设 |
| v3.0 | 增量 PCG 路径和首个双探针自适应器 |
| v3.1 | 筛选、回退、细化、确认模式提高 oracle 质量，但 Setup 很高 |
| v3.2 | 少量固定候选和短 pilot 显著降低 Setup，但候选位置仍依赖经验 |
| v3.3 | 候选由实测模型投影并按 PCG 能量衰减自适应加密；至多四个层次 |

理论文件的演化为：theory_v1 建立能量最小化、PCG 尾项和两网格精确谱表达；theory_v2 加入支撑残差恒等式、LMI 比较、最优粗空间、扰动界和通道/Ritz 条件；theory_v3 加入预测误差条件界、break-even 和多 RHS 成本公式。本版本不修改 `theory.tex`。

## 3. 数学与算法主线

将自由点与粗点分块后，理想能量插值的自由块满足

\[
B W_*=-C,\qquad W_*=-B^{-1}C.
\]

对任意近似 `W`，理论给出精确能量差

\[
\mathcal E(W)-\mathcal E(W_*)=
\frac12\lVert W-W_*\rVert_B^2.
\]

有限步 PCG 产生 `W_m`。PCG 的能量误差随 `m` 下降，但两网格因子取决于插值误差相对于平滑器慢模态的方向，因此 `m -> rho_TG(P_m)` 可以非单调。主通道实测的最优窗口为 `m=38` 左右，而精确全局插值需要3227次循环。

### 3.1 短 pilot 预测

对某候选的两网格 pilot 残差范数 `e_k`，在尾部使用长度 `w` 的重叠块：

\[
g_j=\frac1w\log\frac{e_j}{e_{j-w}}.
\]

以最慢观测衰减率和 MAD 不确定度构造保守 `rho_hat`，再预测

\[
\widehat N=m_{pilot}+
\left\lceil\frac{\log(\varepsilon/e_{pilot})}
{\log\widehat\rho}\right\rceil.
\]

预测达到硬上限时不再只按 RHS 尾部率比较，而使用 PCG 路径的预条件残差能量代理

\[
\eta_E(m)=
\left(
\frac{\sum_j r_{j,m}^{T}D^{-1}r_{j,m}}
{\sum_j r_{j,0}^{T}D^{-1}r_{j,0}}
\right)^{1/2}.
\]

它对应理论中外支撑残差的对偶能量思想，比最大列残差稳定，并且不需要建立两网格层次。

### 3.2 v3.3 候选生成

1. 对几何插值做24步 pilot；若预测已经不超过两倍 pilot 长度，直接接受。
2. 沿同一 PCG 路径推进到 `m_min`，建立锚点层次并做相同 pilot。
3. 用前两个预测的对数割线斜率投影下一检查点。为避免短暂慢斜率把候选推到搜索端点，投影不超过剩余区间中点。
4. 若投影候选仍超过容易阈值的四倍，向前区间中点再探测一次；若 `eta_E` 相对锚点下降到2%以下，说明可能已越过快速变化区，改测锚点与投影点的中点。
5. 在最佳预测的8%范围内选择最小 `m`，降低构造密度和未来应用成本。

这些判据只使用无量纲相对量。算法不读取场类型、对比度、种子或特定网格，也没有固定 `m=40` 候选。第四层次只在观测曲线给出证据时建立。

### 3.3 与 theory_v3 的关系

theory_v3 的条件结论为：若每个候选预测满足相对误差 `δ`，选择器在预测空间内达到 `1+γ`，则

\[
N_{sel}\le
\frac{(1+\gamma)(1+\delta)}{1-\delta}N_*.
\]

v3.3 直接报告 MAD 型预测不确定度，并将 `γ=0.08` 显式化；但现有实验不能给出对所有问题统一成立的 `δ` 上界。因此 oracle gap 仍是经验验证结果，不是无条件保证。

多 RHS 的 break-even 仍服从

\[
R>
\frac{S_{sel}+S_m-S_{m_0}}
{Q_{m_0}-Q_m},
\]

且仅当选中层次的单 RHS 成本低于基准时才存在有限 break-even。

## 4. 实现与效率优化

- `GlobalEnergyPcgPath` 对每个粗基函数保存 PCG 状态，后续检查点不重复 Krylov 工作。
- 新增最大、RMS 和预条件能量三种 PCG 残差指标。
- 候选两网格层次在 pilot 期间直接复用，不重复建立 Galerkin 算子或 Cholesky。
- 只在可能需要回退时保存一个中间插值快照，不提前构造其粗层次。
- 删除旧 v3.1 staged 分支、固定候选 v3.2 分支及其大量选项状态。
- 删除计算热路径的尺寸、排列、选项等防御性检查；调用者负责合法输入。
- 保留 SPD 分解失败、数值 breakdown、要求收敛但未收敛等数学有效性检查。
- 删除 `solve_two_grid` 中未使用的细网格矩阵参数。
- 所有 C++ 源文件和脚本中的注释已删除；实验输出和本文档承担解释职责。
- CSR 乘法、转置、Galerkin 乘积、列 PCG 构造继续使用 OpenMP/线程并行。

当前核心文件为：

| 文件 | 职责 |
|---|---|
| `core/linear_algebra.hpp` | 向量、CSR、稀疏 Cholesky |
| `pde/diffusion_problem.hpp` | 网格、系数场、扩散矩阵 |
| `multigrid/energy_interpolation.hpp` | 几何、局部、全局能量插值 |
| `multigrid/global_pcg_path.hpp` | 增量全局 PCG 路径和能量指标 |
| `multigrid/adaptive_global_pcg.hpp` | v3.3 自适应选择器 |
| `multigrid/two_grid_solver.hpp` | Galerkin 粗算子、对称 GS、粗解和求解循环 |

## 5. 全部实验的重要结果

所有结果均使用 v3.3.0 源码重新生成。默认大问题为 `h=1/128, H=1/16, contrast=1e4`，求解停止准则为相对残差 `1e-6`。

| 实验 | 角度 | 最重要数值结果 |
|---|---|---|
| 1 | 空间局部化 | 通道：layers2/3/4/global 为6840/6050/5275/3227 cycles；连续场约90 cycles即饱和 |
| 2 | 局部 PCG 容差 | 通道 `tol=1e-2` 到 `1e-10` 仅从5449降至5275，精确化收益很小 |
| 3 | 全局幅值剪枝 | 连续场 drop `1e-4` 将密度95.72%降至16.39%且保持90 cycles；通道剪枝不能修复3227-cycle极限 |
| 4 | 支撑扩张 | 通道基础局部6840，indicator-strong 5592，adaptive-budget 3553 cycles |
| 5 | Jacobi/PCG 路径 | 通道 PCG `m=32` 为347、`m=64` 为366，精确全局为3227；Jacobi `m=64` 仍需4704 |
| 6 | 步长2扫描 | 主通道 oracle 为 `m=38, 231`；`m=40` 为234；全部粗解通过独立 long-double 检查 |
| 7 | v3.3 主案例 | 选择 `m=40, 234`，Setup 448.995 ms；相对 oracle gap 1.30% |
| 9 | 18问题鲁棒性 | 自适应总cycles 1875，固定 `m=40` 为2586；自适应18/18收敛 |
| 10 | pilot 诊断 | `m=38` 在32--160步残差轨迹和真实231 cycles上均位于最佳窗口附近 |
| 11 | 六问题 step-2 oracle | 平均 gap 8.33%，最大14.00%，六例全部收敛 |
| 12 | 八问题诊断 | 平均 gap 3.04%，最大12.05% |
| 13 | 八问题压力集 | 平均 gap 4.63%，最大28.10%；该集合参与了v3.3设计，不称为独立 held-out |
| 14 | 五 RHS 摊销 | 相对几何插值均1 RHS回本；相对固定 `m=40` 为3、4、3、66 RHS |

### 5.1 主通道密集窗口与自适应结果

| 方法 | m | Setup ms | Cycles | 相对 oracle gap |
|---|---:|---:|---:|---:|
| 固定 PCG | 32 | 245.453 | 347 | 50.22% |
| 固定 PCG | 36 | 273.665 | 268 | 16.02% |
| oracle | 38 | 286.557 | 231 | 0.00% |
| 固定 PCG | 40 | 311.707 | 234 | 1.30% |
| 固定 PCG | 48 | 360.840 | 256 | 10.82% |
| v3.3 自适应 | 40 | 448.995 | 234 | 1.30% |

选择轨迹为：

| 阶段 | m | `rho_hat` | pilot相对残差 | `eta_E` | 预测cycles | 选择 |
|---|---:|---:|---:|---:|---:|---|
| initial | 0 | 0.994006 | 5.654 | 1.000 | 2611 | 否 |
| anchor | 16 | 0.990369 | 23.64 | 0.1101 | 1779 | 否 |
| projected | 40 | 0.912467 | 3.438 | 0.01112 | 189 | 是 |

预测189低于实测234，说明预测仍有系统误差；算法只用预测排序，最终公开表始终独立实测真实循环数。

### 5.2 六问题 step-2 oracle

| 网格 | 拓扑 | 选择m/cycles | oracle m/cycles | Gap |
|---|---|---:|---:|---:|
| 32/8 | cross | 0/38 | 0/38 | 0.00% |
| 32/8 | parallel | 16/50 | 20/45 | 11.11% |
| 64/8 | diagonal | 12/228 | 12/228 | 0.00% |
| 64/8 | parallel | 12/160 | 22/144 | 11.11% |
| 64/16 | diagonal | 22/57 | 30/50 | 14.00% |
| 64/16 | meandering | 34/58 | 40/51 | 13.73% |

### 5.3 多 RHS 工作量

| 网格/场 | 自适应m | 一次Setup ms | 平均cycles | 对几何回本RHS | 对m40回本RHS |
|---|---:|---:|---:|---:|---:|
| 64/8 meandering | 30 | 34.337 | 182.0 | 1 | 3 |
| 64/16 diagonal | 22 | 92.961 | 55.0 | 1 | 4 |
| 64/16 cross | 26 | 142.644 | 59.4 | 1 | 3 |
| 128/16 cross | 40 | 441.636 | 231.4 | 1 | 66 |

当已知固定 `m=40` 正好合适且自适应也选40时，额外选择成本很难回收。自适应器的价值主要在候选未知、几何层次可能失败或需要跨多 RHS 复用的场景。

## 6. 验证状态

- 使用 `g++`、C++17、`-O3 -DNDEBUG -pthread -fopenmp` 编译全部13个实验和2个测试程序。
- 使用 `-Wall -Wextra -Wpedantic -Werror` 编译通过，无警告。
- `unit_core` 与 `regression_v3` 均通过。
- experiment1--7、9--14 全部运行完成，并重新生成对应 CSV/TXT。
- experiment6 的全部候选通过独立 long-double dense Cholesky 粗解交叉验证。
- 当前执行环境没有 CMake，因此未在本机复现 CMake/CTest 命令；CMake 配置文件已同步为3.3.0。

## 7. 结论与边界

v3.3 在不使用场类型和固定最佳步数先验的情况下，将主案例保持在oracle的1.30%以内，并把通用测试集的大多数 gap 控制在约14%内。相较 v3.2，算法不再固定依赖 `m_min+12` 和 `m=40`，而是根据观测曲线确定候选；条件第四候选修复了“早期 PCG 反而恶化、后期才进入有利窗口”的情况。

仍需明确以下边界：

1. 当前仅验证二维结构网格、固定几何粗点、两网格和一个代表 RHS。
2. `eta_E` 与两网格因子没有无条件单调关系，它只用于决定搜索方向。
3. 预测误差的统一 `δ` 尚未证明，不能宣称任意 SPD 系统上的固定 oracle 保证。
4. 压力集中仍存在28.10%的 gap，说明三到四个层次不可能覆盖所有非单调曲线。
5. 尚未与成熟 AMG 软件做统一硬件、统一容差和统一计时口径的外部基准。
6. 单次墙钟时间只适合同机比较。

下一阶段最有价值的工作不是增加更多经验候选，而是建立可廉价计算的慢模态投影指标，或为 pilot 预测误差给出可验证的在线上界，从而把 theory_v3 的条件 oracle 界转化为可检验的算法停止准则。
